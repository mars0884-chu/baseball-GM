# ⭐ 前置章：北極星憲法（《職棒GM：百年帝國》設計書 v1.2 全文收錄）

> **v41 起，本設計文件以下列北極星設計書為最高憲法**：所有版本的功能取捨、系統設計與驗收門檻，皆以此文件為準繩。歷史章節（§1〜§40）與未來章節如與憲法牴觸，以憲法為準。
>
> 收錄版本：v1.2（GM/教練分權與可獻策式委任細則定稿版）。憲法本身的修訂由 Mars 拍板後同步更新此章。

---

# 《職棒GM：百年帝國》北極星設計書 v1.2

> **這份文件是什麼**：專案憲法。所有版本範圍拍板前，須通過附錄 A 的自檢表。
> **這份文件不是什麼**：不是開發排程、不是現況對照表。落地順序見附錄 B，取捨另議。

---

## 修訂歷程

### v1.0 → v1.1
| # | 修訂項 | 內容 |
|---|---|---|
| 1 | 權力邊界 | 「不能排打線」由零備援改為**開局身分模式選擇**（純GM／GM兼教練） |
| 2 | 國際賽 | 改為 GM 親自選訓與排陣（v1.2 定案） |
| 3 | 進階數據 | 紅線照收，攤開工程真相；屬性 38 項分批 |
| 4 | 視覺方針 | **AI 協作美術由「否決」改為「預留」**；新增資產層 |
| 5 | 工程政策 | 新增封存策略、鐵則更新、測試門檻 |
| 6 | 百年史冊 | 明訂提早埋 log（資料不回溯） |

### v1.1 → v1.2（Mars 拍板）
| # | 修訂項 | 內容 | 出處 |
|---|---|---|---|
| 7 | **國際賽定案** | GM 自選**方針**（認同度恆 100%，無折射）；natCoach 改為**執行官**；**結果回饋本業**（媒體標籤→自家教練認同度門檻） | 10.1 |
| 8 | **接管機制** | 純 GM 想收權 → **Takeover 事件**（限時、有帳單），不是設定開關 | 2.8 |
| 9 | **不對稱切換** | 放權便宜（正面事件）／收權昂貴（走接管）。引力指向純 GM | 2.4 |
| 10 | **球迷壓力層** | 2.1 拆為**權力表**＋**壓力表**。球迷不進分權（不下令），改立為**支柱一的天敵**與**老闆耐心的乘數** | 2.1 / 第六部之七 |
| 11 | 驗收門檻 | 新增：純 GM 模式全自動 15 年 smoke（教練 AI 合理性） | 14.3 |

---

## 第一部 · 定位

### 1.1 一句話

**你不是教練，你是那個決定球隊十年後長什麼樣子的人。**

比賽由 AI 教練執行。你的樂趣不在贏下今晚這場，而在於：你看見了別人沒看見的價值、你留住了別人留不住的人、你蓋起了別人蓋不起的體系——然後十年後，記分板替你證明。

### 1.2 三根支柱

整份設計的每一個系統，都必須服務於這三根支柱之一。無法歸類的系統就是雜訊，不做。

**支柱一：資訊不對稱（Moneyball）**
球員的真實能力永遠不可見。你只有兩副眼鏡——球探的眼睛與數據的鏡片——而這兩副眼鏡會互相矛盾。遊戲的核心決策時刻，就是「兩副眼鏡打架時，你信誰」。**而第三雙眼睛（球迷）根本不看數據——這是你信仰的代價。**

**支柱二：人的意志**
球員與教練不是數值容器，他們有忠誠、野心、脾氣與記憶。他們會因為你的承諾而留下，也會因為你的跳票而走人。合約桌上談的從來不只是錢。

**支柱三：時間的複利**
單季是雜訊，十年才是訊號。設施、農場、文化、城市、聲望——所有真正重要的東西都需要五年以上才看得到回報，而一旦成形就很難被追上。這是「百年帝國」四個字的意思。

### 1.3 設計紅線

> **紅線一（出口）**：任何屬性、任何系統，都必須能指出「它改變了哪一個玩家看得到的產出」。指不出來就不做。

> **紅線二（真算）**：所有進階數據必須由模擬真實產生，不得由評價分數換皮加雜訊。此線一破，支柱一整根倒塌。

> **紅線三（美術不入機制）**：視覺資產只改變「好不好看」，永不改變任何數值、機率或可玩性。資產缺失時遊戲必須完整可玩。

---

## 第二部 · 權力與壓力

這是整個遊戲的憲法。權力邊界一旦模糊，GM 模擬就退化成一般的棒球遊戲。

**兩個垂直的維度**（v1.2 拆分）：**權力**回答「誰能決定」，**壓力**回答「誰能影響你」。不下令的角色不進權力表。

### 2.1a 權力表：三方分權

| 角色 | 掌管 | 對玩家 |
|---|---|---|
| **老闆** | 目標、預算上限、你的去留 | 給你 KPI，也給你壓力 |
| **你（GM）** | 人、錢、方針、建設 | 你玩的就是這層 |
| **教練團** | 打線、輪值、換投、臨場、育成執行 | 他向你要人，你向他要結果 |

### 2.1b 壓力表：GM 承擔多方壓力

| 壓力源 | 性質 | 可否談判 | 生效路徑 |
|---|---|---|---|
| **老闆** | 慢、明確、有門檻 | ✅ 季初 KPI 可談 | 直接 → 解僱 |
| **球迷** | 快、情緒、不理性 | ❌ 不能談，但**可經營** | 間接 → 票房 + **老闆耐心乘數** |
| **媒體** | 放大器 | ❌ | 放大球迷情緒、貼標籤 |
| **球員** | 個體意志 | ✅ 承諾、目標 | 忠誠、更衣室、求去 |
| **教練** | 專業意志 | ✅ 需求單、方針 | 執行度、求去 |
| **城市** | 極慢、結構性 | ❌ | 球迷世代結構、經濟 |

**核心區分**：老闆給你**合約**，球迷給你**氣氛**。合約可以談，氣氛不能。

### 2.2 你能做的

建隊、交易、合約、球探、農場、教練團人事、財務、行銷、設施建設、長期策略、**球隊戰術方針**。

### 2.3 你不能做的（純 GM 模式）

打線、輪值順序、換投時機、代打代跑代守、臨場戰術。

**設計取捨備註**：連打線都交出去，是本作最反直覺、也最重要的一刀。因為一旦你能排打線，你就會用微操去修補建隊的錯誤，支柱一與支柱二立刻失效——你不需要看懂數據，也不需要留住任何人，你只要每天調整先發九人就好。權力邊界的目的，是**逼你只能用 GM 的手段解決 GM 的問題**。

### 2.4 開局身分模式與不對稱切換

| 模式 | 打線／輪值 | 設計純度 | 目標客群 |
|---|---|---|---|
| **純 GM**（預設） | 教練全權，UI **不出現**手排入口 | 100%，北極星完整體驗 | 主客群 |
| **GM 兼教練** | 沿用現有手排介面 | 支柱一/二打折，玩家自選 | 不願放權者、老玩家 |

**關鍵**：純 GM 模式下玩家是**自己選了**不排打線，而不是被剝奪——這是與 OOTP 26 精簡模式（板凳教練代管深度表、玩家無法介入、評測普遍反映挫折）的決定性差異。舊手排程式碼一行不刪，符合鐵則。

**切換必須不對稱**（v1.2）：

| 方向 | 成本 | 形式 |
|---|---|---|
| GM兼教練 → 純GM（**放權**） | **便宜**：教練 trust↑、聲望標籤「懂得放權」、好教練更願意來 | 正面事件，隨時可為 |
| 純GM → 手排（**收權**） | **昂貴**：走 2.8 接管流程 | 事件，限時，有長期帳單 |

**一旦放權，就回不到「免費手排」**——即使原本是 GM 兼教練模式。不對稱才能讓預設引力指向純 GM，同時保留逃生門。

**存檔相容**：`gameMode` 進存檔；舊檔一律預設 `gm_coach`（v40 玩家本來就在手排）→ 不破壞既有存檔。工程上重用既有 `team.lineupMode`，只加 `gameMode` 與 takeover 狀態。

### 2.5 核心迴圈：需求單

```
教練提交需求單 → 你評估（接受／駁回／協商） → 你去市場找人
    → 交易／簽約／從農場拉上來 → 教練拿到人 → 執行 → 產出結果
        → 結果回頭改變教練的信任與下一張需求單
```

> 📋 投手教練 · 優先度：高
> 「輪值第四號從開季就撐不到五局，牛棚被拖垮了。我需要一個至少能投六局、控球 70 以上的先發，不用是王牌。」
> 　需求：先發投手／Stamina ≥ 70／Control ≥ 70
> 　理由：牛棚使用局數聯盟第 1，中繼群疲勞累積已進紅區
> 　[接受，我去找] [駁回，用現有的撐] [協商：能不能改用牛棚輪替解決？]

**規則**：
- 由各教練依其專長與**性格**產生（保守派要即戰力、育成派要年輕人、數據派引用進階數據）
- 教練的需求**不一定是對的**。爛教練提出爛需求；你照做，結果一樣爛。這是教練聘任的意義。
- **滿足** → 教練信任↑、執行度↑、化學效應↑
- **駁回** → 一次沒事，長期累積 → 不滿 → 執行度打折 → 求去
- **協商** → 需要教練的 Coachability 與你的聲望；成功則以更低成本化解

### 2.6 守位缺口：三層防線

| 層 | 觸發 | 處理 |
|---|---|---|
| 軟缺口 | 守備品質不佳 | 打線頁警告（維持現狀） |
| **硬缺口** | 無捕手／守位無人 | 教練發**限時補強需求單**；期間自行湊陣（最接近守位者頂替＋守備懲罰＋新聞敘事：「二壘手客串捕手，單場三次捕逸」）。**比賽照打，不中斷** |
| 數學無解 | 野手湊不滿 9 人 | 才中斷 |

延續鐵則「任何新機制不得阻擋遊戲進行」。

### 2.7 戰術方針：你唯一的比賽介面

你不下指令，你定方針。方針是**傾向**，不是命令。

| 方針軸 | 選項 |
|---|---|
| 進攻哲學 | 上壘導向 / 長打導向 / 積極跑壘 / 保守 |
| 投手調度 | 先發長投 / 早換保護 / 牛棚分工明確 |
| 守備配置 | 標準 / 數據佈陣 |
| 育成 vs 戰績 | 新秀優先出賽 / 全力求勝 / 平衡 |
| 傷病處理 | 保護優先 / 硬撐 |

**關鍵張力**：
```
實際執行度 = f(方針, 教練能力, 教練性格認同度)
```
你叫一個老派總教練用數據佈陣，他會陽奉陰違。你要嘛換人，要嘛說服他，要嘛接受折扣。**方針不是滑桿，是一場談判。**

### 2.8 接管（Takeover）：收權的帳單（v1.2 新增）

**先教三招**：玩家想收權，九成是因為教練排線爛。那時 GM 手上有三個更純粹的出口，遊戲要先教會他——

1. **調方針**（他冰新秀？下「新秀優先出賽」）
2. **換教練**（有 Dead Money，貴但正當）
3. **把爛選項移走**（他一直派過氣老將？把老將交易掉。他沒有他就沒得選）

第 3 招是整個權力邊界哲學的**教學時刻**：用 GM 的手段解決 GM 的問題。玩家學會這招，他就不想收權了。

**真的還是不行 → 接管**。它是**事件，不是設定選單裡的 toggle**（自由切換會讓委任制張力三秒歸零：玩家會在季後賽第七戰切手排）。

| 面向 | 內容 |
|---|---|
| 發動代價 | 教練 trust 暴跌（他認為你不信任他），可能當場求去或向媒體開炮 |
| 聲望代價 | 媒體貼標籤「凡事插手的 GM」→ **未來好教練不願意來** |
| 球員代價 | Coachability 低的球星可能不服 |
| 時效 | **限時**（本季剩餘或 N 場）。到期還權或續期，**每續一次再付一次聲望** |
| 還權 | trust 部分回復，但**標籤留著**——第九部：事件是你過去決策的帳單 |

**設計意義**：微操繞過建隊沒有被禁止，它只是**變貴了**，而且貴在長期（教練市場、球員信任、聲望），比建隊錯誤本身更貴。玩家可以做，但他會算。這是自檢表第 4 題要的兩難，而不是一個正確答案。

**史冊會記**：「第 7 年，你解除了教練的排線權。那年你贏了冠軍，但接下來三年沒有一個好教練願意來。」

---

## 第三部 · 球員

### 3.1 屬性總則

屬性數量不是深度，屬性**有沒有出口**才是。凡是找不到出口的（Bunt、Hit & Run、推打、拉打、滑壘…），一律不收——它們屬於臨場戰術層，而臨場戰術層不歸你管。

### 3.2 打擊（5）
| 屬性 | 出口 |
|---|---|
| Contact | 接觸率 → K%↓、BABIP↑ |
| Power | ISO、長打分布、全壘打率 |
| Eye | BB%↑ |
| Discipline | 追打壞球↓ → 弱擊球率↓、K%↓ |
| Clutch | 得點圈／高壓情境修正 |

### 3.3 跑壘（3）
| 屬性 | 出口 |
|---|---|
| Speed | 內野安打率、額外進壘、盜壘企圖 |
| Steal | 盜壘成功率 |
| Baserunning IQ | 額外進壘成功率、跑壘失誤率↓ |

### 3.4 守備（5）
| 屬性 | 出口 |
|---|---|
| Range | 範圍出局率 → 對手 BABIP↓ |
| Glove | 失誤率↓ |
| Arm | 抑制對手進壘、外野助殺 |
| Double Play | 雙殺誘發率 |
| Defensive IQ | 站位與接力判斷 → 額外失分↓ |

### 3.5 投手（7）
| 屬性 | 出口 |
|---|---|
| Velocity | K%↑、Hard Hit%↓ |
| Movement | GB%↑、HR/9↓ |
| Control | BB%↓ |
| Stamina | 每局衰退曲線、可投局數 |
| Pitch IQ | 與捕手 Game Calling 疊加 → 失分修正 |
| Hold Runner | 對手盜壘企圖↓ |
| Composure | 危機情境修正 |

### 3.6 捕手專項（6）

捕手是**全遊戲最能體現「被低估的價值」的位置**——他的貢獻幾乎全部不在傳統數據裡。一個打擊率 .220 的捕手可能是聯盟最有價值的球員，而只有你的數據中心看得出來。**這是支柱一的旗艦位置，也是球迷最不可能理解的一次交易（見 6.7）。**

| 屬性 | 出口 |
|---|---|
| Framing | 對手 BB%↓、K%↑（**不進任何傳統數據**） |
| Blocking | 暴投／捕逸失分↓ |
| Pop Time | 對手盜壘企圖↓ |
| Throw Accuracy | 阻殺成功率↑ |
| Game Calling | 全投手群失分修正 |
| Pitcher Handling | 投手 Composure 加成、新秀投手成長加速 |

### 3.7 心理與意志（8）

這組不進 box-score，它們的出口是**其他系統**。

| 屬性 | 出口 |
|---|---|
| Leadership | 隊長資格、更衣室事件、周邊球員成長 |
| Work Ethic | 訓練效率、衰退期延緩 |
| Loyalty | 續約折扣、拒絕交易、退休後回任 |
| Coachability | 教練指導效果、方針認同度、**接管時的服從度** |
| Pressure | 季後賽／高壓情境修正 |
| Personality | 事件觸發、化學效應、經紀人態度 |
| Professionalism | 場外事件機率↓、傷後自主復健 |
| Ambition | 求勝欲／求財欲比重 → 談約與求去動機 |

### 3.8 身體（4）
| 屬性 | 出口 |
|---|---|
| Injury Resistance | 受傷機率 |
| Recovery | 傷癒天數 |
| Durability | 疲勞累積速率、連續出賽上限 |
| Conditioning | Stamina 上限、老化曲線平緩度 |

### 3.9 潛力與成長

- **逐屬性潛力**：每個屬性各有天花板。「這個投手球速能到 95 但控球一輩子只有 50」——這正是球探報告該說的話。
- **成長原型**：早熟／大器晚成／曇花一現／穩定／天花板未知。**不對玩家揭露**，只能從球探判斷與逐年表現推測。
- **Peak Age／老化曲線**：因原型與 Conditioning 而異。
- **隱藏成長**：某些球員的天花板會被特定條件解鎖——遇到對的捕手、對的教練、對的隊友、對的出賽量。這是農場與教練團投資的長期回報。

### 3.10 屬性落地批次

| 批次 | 內容 | 備註 |
|---|---|---|
| A | 投打核心擴充 + **捕手專項 6** | 支柱一旗艦，優先 |
| B | 心理 8 | 大半**已存在**（morale／loyalty／隊長評分）→ 整併而非新建 |
| C | 守備 5 / 跑壘 3 細分 | 需 L3 引擎先落地才有出口 |
| D | 身體 4 | 現行疲勞/傷病系統細化 |

---

## 第四部 · 三層資訊（Moneyball 引擎）

**這是整款遊戲的心臟。**

### 4.1 三層
| 層 | 內容 | 玩家可見 |
|---|---|---|
| **L1 真值** | 球員實際能力 | ❌ 永遠不可見 |
| **L2 球探估值** | 球探眼中的能力（帶迷霧） | ✅ 依球探能力而準 |
| **L3 統計估值** | 從產出反推的能力（帶運氣噪音） | ✅ 依樣本數而準 |

### 4.2 兩層的誤差來源不同——這是設計的關鍵

- **L2 的誤差是系統性的**：球探有偏見。偏愛體格、球速、自己的專長區域、符合他心中「球員該長什麼樣」的人。誤差方向可預測。
- **L3 的誤差是隨機性的**：小樣本會騙人。BABIP、殘壘率、HR/FB——短期全是運氣。誤差方向不可預測，但**會隨樣本收斂**。

|  | L3 數據好 | L3 數據差 |
|---|---|---|
| **L2 球探看好** | 貴。所有人都要。你買不起。 | 陷阱或璞玉：球探看到天賦，數據看到現在。要賭嗎？ |
| **L2 球探看衰** | **💎 這裡。這就是 Moneyball。**（但球迷不認得他——見 6.7） | 對的。他就是不行。 |

### 4.3 市場定價 = 套利空間

- **傳統派**：按 L2 出價 → 你可以撿走所有數據好但球探看衰的球員
- **數據派**：按 L3 出價 → 你撿不到便宜，但可以賣給他們「數據好但你知道是運氣」的球員
- **混合派／土豪派／地緣派**：各有偏誤

**聯盟的數據化程度會隨年份演進**——第 1 年只有你在算 wRC+，第 15 年半個聯盟都在算。**你的套利空間會被時代吃掉。** 你贏在早，然後你必須找到下一個沒人看的數字。

### 4.4 數據中心

**傳統數據**：AVG、HR、RBI、SB、W、ERA、SV、HLD（**＝球迷唯一看的那組**）

**進階數據**：
- 打者：OPS+、wRC+、ISO、BB%、K%、BABIP、Hard Hit%、Barrel%
- 投手：ERA+、FIP、xERA、K/9、BB/9、GB%、Stuff+、Location+
- 守備：Range Runs、Framing Runs、阻殺率
- 綜合：WAR

**設計要求**：
1. 進階數據必須是**真的算出來的**（紅線二）。
2. 必須顯示**樣本數與信賴區間**。「BABIP .380（僅 62 打席，樣本不足）」比裸數字有價值一百倍。
3. 必須提供**運氣校正視圖**：ERA 2.10 但 FIP 4.30 → 他明年會崩。AVG .240 但 Hard Hit% 聯盟前 5% → 他被低估了。**這是你賺錢的地方。**

### 4.5 數據分析主管

分析主管的能力決定：你能看到哪些進階數據、樣本校正準確度、是否提供**主動警訊**（「三號先發的 FIP 與 ERA 落差全聯盟最大，建議趁高賣出」）。

**這是全遊戲投報率最高的一次聘任，而多數 AI 球隊不會這樣想。**

### 4.6 工程真相：引擎換心

- **要真的算出 BABIP／FIP／Hard Hit%，模擬引擎必須產生打席層級的事件與擊球品質**。`03-simulation.js` 核心迴圈需重做一層。
- 與「不做逐球演出」**不衝突**：模擬顆粒度是引擎內部的事，演出層維持 box score。
- **必須新增統計驗證 harness**：模擬十季，檢查聯盟 K%、BB%、BABIP、得分環境、長打分布落在合理區間。701 項斷言驗不出這種錯。
- design_doc 須誠實記錄「這是引擎版本」。

---

## 第五部 · 找人的系統

### 5.1 球探（L2 的來源）

**分區**：北美、中南美、亞洲、歐洲、非洲、大洋洲。

| 屬性 | 出口 |
|---|---|
| 可信度 | 報告誤差幅度 |
| 眼光 | 對「潛力」的誤差幅度 |
| 速度 | 每季可觀察球員數 |
| 人脈 | 獨佔情報、他隊不知道的球員 |
| 專長 | 對特定位置／區域／原型的誤差↓ |
| **偏見** | 誤差的**方向** |

**觀察機制**：同一球員被觀察越多次，L2 誤差越小。球探時間有限 → **你要決定把眼睛放在哪裡**。

**精準球探指令**：GM 下達指令（「教練要高控球投手，去國際市場鎖定控球型 archetype」）→ 球探下次報告精準引薦。球探從「隨機挖菁英」轉型為**找戰術拼圖的獵犬**，把需求單迴圈與球探系統串起來。

### 5.2 農場

**層級**：新人聯盟 → 1A → 2A → 3A → 一軍。**補充**：冬季聯盟、海外派遣、教育聯盟。

- **升太快 = 信心受挫 + 成長停滯；壓太久 = 不滿 + 忠誠↓ + 浪費潛力窗口。** 每個球員都有自己「該上去的時機」，而你只能從 L2/L3 猜。
- 農場產出的是**未來的成本優勢**：自己養的人便宜、忠誠高、還記得是誰把他拉上來的。
- 二軍總教練直接決定農場產出，但他的成績不會出現在任何頭條。**又一個被低估的聘任。**

### 5.3 選秀
- 選秀前只有 L2 → **資訊不對稱最極端的場景**
- 完整球探報告必須在**做決定之前**可見（v40 已落地）
- 報告應該說人話：「球速上限我看到 95，但控球我不覺得能超過 55。他是後段輪值，不是王牌。」

### 5.4 自由市場與交易
- 交易價值由**對方球隊的 persona 眼鏡**決定 → 同一個球員賣給不同球隊價碼不同
- 交易截止日：短期補強 vs 未來，一年一次的攤牌
- **交易謠言**：情報會外洩，球員會有反應，**球迷會有情緒**

### 5.5 經紀人
- 類型（強硬派、務實派、家族型、炒作型）與人脈
- 與經紀人的關係是**跨球員的長期資產**：交好 → 提前情報、獨家談判權、門檻折扣
- 你在合約桌上的行為會被整個經紀人圈子記住

---

## 第六部 · 人的意志

### 6.1 忠誠：合約桌上的貨幣

**忠誠不是好感度，是一種可以花掉的資源。**

高忠誠：薪資折扣（−8%～−15%）、接受短約／後置薪資、拒絕被交易（雙面刃）、願意等重建、退休後回任教練且對文化有加成。

**來源**：出賽機會、成績肯定、冠軍、城市認同、隊長身分、球隊文化、**你兌現的承諾**。
**流失**：冷板凳、被壓在農場、公開批評、交易謠言、**你跳掉的承諾**。

### 6.2 承諾系統

「你會是先發游擊。」「我們今年冬天會補一個王牌。」「三年內我們會進季後賽。」

承諾**不寫進合約，但寫進他的記憶**。

- 兌現 → 忠誠↑↑、經紀人圈子聲望↑
- 跳票 → 忠誠↓↓、更衣室事件、**經紀人圈子聲望↓ → 所有未來談判變貴**
- **公開承諾**（對媒體說的）→ 球迷也會記住（見 6.7）

**這是全遊戲最便宜的資源，也是最貴的債。**

### 6.3 六方關係網

```
        老闆 ←—→ 城市 ←—→ 球迷
          ↓         ↓        ↓
    ┌—→  你（GM） ←————————————┘
    │      ↓
  教練 ←—→ 球員
```

### 6.4 教練信任與去留

- **trust 不新建平行系統**：掛在既有 morale／loyalty 架構與季末 KPI 考核管線
- 需求單達成 +trust／無視 −trust；trust 低 → 新聞跑馬燈抱怨（既有事件系統承載）
- **教練哲學 ×3 起步**：小球流／轟炸流／投手優先流，對應現行 TACTICS_OFFENSE 預設偏好。GM 方針可覆蓋，但覆蓋**消耗 trust**
- **解職違約金（Dead Money）**：中途換教練須從 finance.budget 扣大筆違約金。一個數字就讓「換教練」從免費按鈕變成沉重決策

### 6.5 隊長
- Leadership 高且忠誠足夠者擔任；常駐職位，一季一次任免
- 效果：全隊士氣底線、更衣室衝突機率↓、新秀成長加速、訊息傳達折扣
- **風險**：隊長離隊 = 全隊忠誠震盪；任命錯人 = 更衣室分裂

### 6.6 球員個人目標

先發位置、單季 30 轟、拿冠軍、拿到大約、留在家鄉、進國家隊。

- 你**不會被告知**，要靠球探人脈／經紀人關係／相處時間挖出來
- 達成 → 忠誠↑；長期阻擋 → 求去
- **這是「便宜留人」的第二條路**：有時候他要的不是錢，是一個先發位置。而只有你知道。

### 6.7 球迷：集體的意志（v1.2 新增）

**球迷不進權力表**——他們不下令，他們施壓（2.1b）。

#### 為什麼要做：球迷是支柱一的天敵

如果球迷只是「另一個看戰績的 KPI」，那他是雜訊，違反紅線一，不如不做。球迷唯一值得做的理由是：

> **球迷不看 wRC+。** 他們看打擊率、全壘打、打點、名氣、熟面孔、在地出身。
>
> 你用 Moneyball 撿來一個球探看衰但數據金光閃閃的球員，球迷不認得他。
> 你交易掉那個 .310 的當家門面，換回一個打擊率 .220 但 Framing 聯盟第一的捕手。
> **數據上你賺翻了。球迷暴動。老闆開始問你在幹什麼。**

支柱一因此從「你信球探還是信數據」升級為「**你信數據，但你敢不敢承擔看不懂數據的人給你的壓力**」。Moneyball 從一個聰明解法變成一個要付代價的信仰。

**與 4.3 對稱**：聯盟的數據化程度會爬升，**球迷的數據化程度也會**。第 1 年你簽 Framing 大師被罵到臭頭，第 15 年球迷自己在論壇貼 Framing Runs。**你早年的孤獨，正是你的護城河。**

#### 三條護欄（防止球迷反噬支柱一）

1. **球迷不直接決定你的去留**。球迷是**老闆耐心的乘數**，不是第二個老闆。老闆的 KPI 白紙黑字、季初可談；球迷是決定老闆多快失去耐心的變數。
2. **贏球治百病**。戰績永遠是最大權重。你可以無視球迷，代價是你必須贏——這既真實，也保證 Moneyball 這條路始終走得通，只是走得孤獨。
3. **球迷不可談判，但可經營**。老闆能談 KPI，球迷不能談。但球迷可以被行銷、明星、在地球員、公開承諾、球隊文化長期塑造（支柱三）。這給了「討好球迷」一條 GM 式的路，而不是逼你去簽爛約。

#### 三個維度（取代單一「人氣」數字）

| 維度 | 由什麼決定 | 出口 |
|---|---|---|
| **期待值** | 近年戰績（**贏久了門檻自己會升——王朝的詛咒**） | 未達標時的不滿幅度 |
| **耐心** | 球隊文化、球迷世代結構、城市 | **老闆耐心乘數**（重建容忍度） |
| **認同** | 特定球員、在地出身、球風、兌現的公開承諾 | 票房、贊助、自由球員意願、主場修正 |

#### 呈現方式：不需要新畫面

**郵件中樞（8.3）就是他們的喇叭**——球迷來信、媒體詢問、新聞跑馬燈。既有管線全部重用。

#### 自檢
1. **支柱**：一（傳統派眼鏡＝套利的社會代價）、二（集體意志）、三（世代累積、城市）
2. **出口**：票房／老闆耐心乘數／FA 意願／主場修正
3. **權限**：球迷沒有決策權 → 不進權力表 ✅
4. **決策**：兩難（Moneyball vs 輿論；贏球 vs 討好）✅
5. **尾巴**：球迷世代、城市、文化 ✅

### 6.8 球隊化學

化學 = f(性格組合, 隊長, 世代結構, 方針認同度, 勝負)

- 不是「加總好感度」，是**組合效應**：兩個 Ambition 極高的球星會互斥；老將 + 高 Coachability 新秀會共振
- 出口：高壓情境修正、事件機率、成長速率

---

## 第七部 · 經營與建設

### 7.1 收入
門票、季票、會員、轉播、廣告、贊助、商品、餐飲、停車、命名權、季後賽分潤、國際賽分潤。

### 7.2 支出
球員薪資、教練團、球探、農場營運、醫療、交通、設備、球場維護與折舊、青訓、行銷。

### 7.3 球場與設施
- **設施分級**：訓練、醫療、球探辦公室、宿舍、數據分析、復健、青訓
- **球場**：容量、席位結構、場地特性（尺寸影響長打產出 → 影響你該蓋什麼樣的隊）
- **折舊與重建**：所有東西都會老。帝國需要維護。
- **設施是複利的載體**：今年蓋，第六年才看得出差距——然後就再也追不上了。

### 7.4 行銷
- 行銷企劃：知名度、票房、贊助、球迷結構
- 知名度不只是錢：影響自由球員意願、新秀簽約意願、球探人脈
- **也是球迷認同的主要施力點**（6.7）

### 7.5 春訓基地
不同基地 = 不同訓練加成 × 傷病風險 × 成本 × 國際交流機會。

---

## 第八部 · 賽季

### 8.1 年度時間軸

| # | 階段 | GM 的工作 | 教練的工作 |
|---|---|---|---|
| 1 | 春訓 | 基地選擇、訓練分配、名單決策 | 評估、提交需求單 |
| 2 | 開幕 | 確認方針、KPI 談判 | 定案打線與輪值 |
| 3 | 上半季 | 需求單、傷病、財務、事件 | 執行 |
| 4 | 明星賽 | 曝光、贊助、球員榮譽 → 忠誠 | 休息 |
| 5 | 交易截止日 | **一年一次的攤牌**：買家還是賣家 | 提交最後需求 |
| 6 | 下半季 | 育成 vs 戰績的取捨 | 執行 |
| 7 | 季後賽 | 觀看（你已經做完所有能做的了） | 執行 |
| 8 | 冠軍賽 | 同上 | 執行 |
| 9 | 選秀 | 球探報告、選人、簽約 | — |
| 10 | 自由市場 | 續約、簽人、交易 | 提交冬季需求 |
| 11 | 冬訓 | 自主訓練、傳承、養傷 | 育成計畫 |
| 12 | 國際賽（每 4 年） | **選訓名單、排陣、定方針**（10.1） | — |

**季後賽你什麼都不能做**，這是刻意的。那是你交卷之後的閱卷時間。

### 8.2 KPI 與老闆
- **季初可談判**：重建期就該給重建期的目標。談不下來 = 你自己接受一個不合理的標準。
- 季中檢視：降標（信任↓）或加碼（信任↑↑）
- 未達標 → 信任↓ → 解僱 → 生涯轉折
- **老闆耐心 = f(KPI 達成, 財務, **球迷耐心乘數**)**（6.7）

### 8.3 每日流程：郵件是中樞

```
郵件（決策收件匣）→ 你的所有待辦都在這裡
   ├ 教練需求單    ├ 球探報告      ├ 醫療報告
   ├ 數據主管警訊  ├ 經紀人來電    ├ 交易提案／謠言
   ├ 媒體詢問      ├ 球迷來信      ├ 老闆
   └ 事件
→ 做決策 → AI 模擬比賽 → 隔天
```

**設計要求**：沒有待辦的日子應該可以**連續模擬跳過**；有待辦的日子應該**強制停下**。玩家的時間只花在決策上。（v40 的待辦分頁＋紅點是前身，郵件中樞是完全體。）

---

## 第九部 · 事件與敘事

- **分類**：更衣室、場外、營運、球員、媒體球迷、國際
- **連鎖**：事件的選擇會在數天／數週後長出後續事件。你的決定有尾巴。
- **記憶**：事件結果進入球員／教練／**球迷**／球隊的記憶
- **設計原則**：事件不是隨機的懲罰或獎勵，是**你過去決策的帳單**。忽視需求單的教練會鬧、跳票的承諾會爆、壓在農場太久的新秀會要求交易、**你交易掉的門面球星會讓球迷記你三年**。

---

## 第十部 · 全球生態

- **40 國體系**：分級、球員風格、成本、簽約難度
- **國家羈絆**：長期經營特定國家 → 獨佔新秀、成本折扣、交流賽機會
- **時代演進**：國際化程度、數據化程度（聯盟＋球迷）、球員市場規模隨年份改變

### 10.1 國際賽：唯一的例外（v1.2 定案）

**每 4 年一屆。這是全遊戲唯一一次「你的哲學不被折射」。**

| 項目 | 國際賽 | 本業 |
|---|---|---|
| 選人 | **GM 親自** | 教練 |
| 排陣 | **GM 親自** | 教練 |
| **戰術方針** | **GM 自選，認同度恆 100%（無折扣）** | f(方針, 能力, 認同度) |
| 臨場指令（換投／代打／盜壘） | ❌ **仍不做**（紅線一、第十二部） | ❌ |

**為什麼「無折射」比「多一點自由」更重要**：平常執行度＝f(方針, 教練能力, 認同度)——你信小球，教練不信，打出來的是妥協的產物；你永遠不知道輸掉是因為哲學錯了，還是教練沒執行好。國際賽是**唯一一次無折射的考卷**：人是你選的、線是你排的、風格是你定的。贏了輸了都只能算在你頭上。服務支柱一（你的判斷對不對）與支柱二（拿掉認同度這個變數）。

**natCoach 不消失，改為「執行官」**（鐵則：不刪程式碼）。理由站得住：國家隊時間短、任務單一、沒有長期關係包袱——他不需要跟你磨合，他只需要照做。比「國家隊沒有教練」更合理。natCoach 提供**建議名單**供 GM 參考。

**結果必須回頭餵進本業**（否則國際賽變成離題的遊樂場）：
- 你用小球拿下世界冠軍 → 媒體貼標籤 → **回國後你跟自家教練談方針的說服力提升**（認同度門檻降低）、聲望↑、想找同風格教練更容易、**球迷認同↑**
- 在國際賽被自己的哲學打臉 → **教練更有底氣陽奉陰違**

國際賽因此不是支線，而是**支柱二的槓桿**，且「四年等一次」有了長期尾巴（自檢表第 5 題過關）。

**工程**：既有 TACTICS 系統照用，把執行度公式的折扣項設為 1.0；手動介面 v40⑤ 已保留為備援，把預設值翻回來即可。

---

## 第十一部 · 百年帝國（長期層）

**這一部是這款遊戲和其他 GM 遊戲的分水嶺。**

### 11.1 王朝
連霸階梯：獎金、士氣、忠誠、聲望的**遞增**回饋（不是每年給一樣的東西）。冠軍旗、退休背號、博物館 → 知名度與球迷世代的永久累積。
**注意**：贏久了球迷期待值會自己升高（6.7）——**王朝的詛咒**。

### 11.2 球隊文化

文化 = 你長期行為的沉澱，不是一個你可以直接選的選項。

- 十年重用新秀 → 「育成聖地」→ 新秀簽約意願↑、成長加成、**球迷耐心↑**
- 十年豪砸自由球員 → 「贏球至上」→ 老將願來、新秀不來、忠誠低、**球迷耐心↓**
- 十年兌現承諾 → 「信任」→ 全體折扣
- 十年凡事插手 → 「傀儡球團」→ **好教練不來**（2.8 接管的長期帳單）
- **文化會反過來限制你**：帝國有慣性。這是它的代價。

### 11.3 世代與傳承
名人堂、退休背號；老將退休 → 回任教練／球探；傳承：老將把技能傳給新秀；**二代球員**：你當年那個游擊手的兒子，二十年後出現在選秀名單上。

### 11.4 城市
城市會成長或衰退（人口、經濟、球迷世代結構），而你的成績會影響它。**贏得夠久，城市會變成你的。**

### 11.5 百年史冊（提早埋 log）

自動累積的球隊編年史：每個賽季、每次冠軍、每個名人堂、每次崩盤、**每次接管**。**這是玩家真正在收集的東西。**

> **落地註記**：**資料不回溯**。第 15 年才開始記，前 14 年的王朝就永遠沒了。**史冊 log 必須在 v41 就埋**（純 logging，成本極低），UI 可以晚做。

---

## 第十二部 · 明確不做

| 不做 | 理由 |
|---|---|
| 逐球／逐打席**演出** | 玩家不控制臨場，演出無決策價值（註：打席級**模擬**要做，見 4.6——模擬 ≠ 演出） |
| 代打／代跑／代守 | 教練權限（2.3）。**國際賽也不開放**（10.1） |
| 純 GM 模式下免費排打線 | 微操會繞過建隊，支柱一與二失效。**接管有帳單版除外**（2.8）；GM 兼教練模式除外（2.4） |
| 臨場指令（換投／盜壘／觸擊）| 找不到出口，違反紅線一 |
| Bunt / Hit&Run / 推打 / 拉打 / 滑壘 等屬性 | 同上 |
| 球迷投票決定人事 | 球迷不下令（2.1b）。他們施壓，不決策 |
| 攻略式最佳解 | 所有 L2/L3 都有誤差，不存在「看數字就對」的解 |
| 球員 3D 建模／實時比賽畫面 | 不服務任何一根支柱 |
| 單檔壓縮／混淆 | 鐵則：原始碼完整可讀 |
| ~~AI 生成美術~~ | **解除封印 → 第十三部（改為預留，非否決）** |

---

## 第十三部 · 視覺與資產層

**方針**：AI 協作美術由「否決」改為「**預留**」。現階段**不導入任何資產**，只在架構上預留插槽，讓未來 AI 協作產出的美編可以整批注入而不動機制程式碼。

### 13.1 三條約束
1. **紅線三（美術不入機制）**：資產只改變「好不好看」，永不改變數值、機率或可玩性。
2. **資產缺失必須完整可玩**：每個插槽都有現行 emoji／CSS 後備。抽掉資產包 = 回到 v40 外觀，遊戲照跑。
3. **離線優先、無外部 CDN**：資產隨檔走，不得線上拉取。

### 13.2 插槽清單（Slot Registry）

| 插槽 | 現行後備 | 未來資產 |
|---|---|---|
| `team.logo` | emoji + CSS 配色 | 球隊識別 |
| `player.portrait` | emoji | 頭像（可依國籍/年齡/位置分層） |
| `coach.portrait` | emoji | 教練／球探／分析主管頭像 |
| `stadium.view` | CSS 色塊 | 球場外觀（依等級分級） |
| `facility.icon` | emoji | 六類設施圖示 |
| `event.illustration` | 無 | 16 種事件插圖 |
| `ui.texture` | 牛皮紙 CSS | 背景紋理／邊框 |
| `badge.grade` | CSS 圓形徽章 | 等級徽章美化 |
| `fan.mood`（v1.2 新增） | emoji | 球迷情緒／看板／新聞版面 |

### 13.3 技術規格
- **抽換方式**：`data-skin` 屬性 + CSS 變數。程式碼只認插槽 key，不認檔案。
- **交付形態分岔**：
  - **整合單檔**：維持 emoji 版為**預設**。單檔已約 740KB，內嵌 base64 會拖垮手機載入 → **單檔版不內嵌完整資產包**，只允許極輕量向量／CSS。
  - **模組化 zip**：資產包放 `assets/` 外部目錄，走 PWA cache。**這才是美術版的載體。**
- **資產規格書**：沿用《UI_UX資產導入規格書_v1.md》，v41 起補插槽 key 對照表。
- **AI 協作流程（未來）**：Mars 定風格 → AI 產資產 → 依插槽命名規範入 `assets/` → **不改任何 JS**。

---

## 第十四部 · 工程政策

### 14.1 版本封存（v40 以前）

**理解**：封存的是**歷史交付檔案**，不是整合單檔的程式碼。整合單檔本身**永遠不壓縮、不混淆**。

| 對象 | 處置 |
|---|---|
| v39 及以前的整合 HTML／模組 zip | 打包為 `archive_v39_and_before.zip`，只留封存，不再逐版維護 |
| **v40 三件套** | 保留為**現行基準線**（v41 的 diff 起點） |
| design_doc §1–§40 | **全文保留**（鐵則：誠實記錄）。v41 起新增「北極星憲法」前置章 + §41 起續寫 |
| 測試（701 回歸 + 15年 smoke） | 全部保留，v41 起續加 |

> ⚠️ 若「壓縮或封存」原意是別的（例如壓縮 design_doc 歷史章節、或壓縮整合單檔本身），說一聲，我照改。

### 14.2 鐵則更新

| 鐵則 | 狀態 |
|---|---|
| 繁體中文溝通；檔名一律 ASCII | 不變 |
| 根因診斷 → 設計提案 → 拍板 → 實作 → 測試 → 三件套 | 不變 |
| 回歸 + smoke 全綠才交付；整合單檔本身也要驗過 | 不變，**新增統計驗證 harness**（4.6）**與純 GM 15 年 smoke**（14.3） |
| 離線優先、無外部 CDN；開機防護 | 不變，**延伸至資產層**（13.1-2） |
| 單檔不壓縮／混淆 | 不變 |
| 不主動刪除既有程式碼；改制時舊介面保留為備援 | 不變（2.4 手排、10.1 natCoach 都靠這條） |
| ~~不加 AI 生成美術~~ | **解除 → 現階段不導入，架構預留**（第十三部） |
| — | **新增：所有新功能須過附錄 A 自檢表** |

### 14.3 誠實的風險：兩模式並存＝兩套平衡（v1.2 新增）

> **如果純 GM 模式的教練 AI 不夠好，玩家會全跑去 GM 兼教練模式，北極星就白做了。**

**新增驗收門檻**：smoke 必須驗證「純 GM 模式**全自動**跑 15 年，戰績分布合理、不會排出荒謬陣容（守位錯置、王牌長期冷凍、新秀永不出賽）」。

這條比 701 項斷言更難達成，但**它是純 GM 模式能不能成立的唯一保證**。v41 交付前必須綠。

---

## 附錄 A · 設計自檢表

1. **支柱**：它服務哪一根支柱？（資訊不對稱／人的意志／時間複利）
2. **出口**：它改變哪一個玩家看得到的產出？
3. **權限**：它有沒有越過 GM 的權力邊界？**（若是壓力源：它是不是誤闖了權力表？）**
4. **決策**：它是否製造一個「兩難」，而不是一個「正確答案」？
5. **尾巴**：它三年後還有影響嗎？
6. **資產**：它需要視覺插槽嗎？若需要，後備是什麼？

六題有一題答不出來，就先不做。

---

## 附錄 B · 落地路線（v41–v45，邊做邊修）

原則：每版取一根支柱的一個切片；優先做「重用既有管線」的項目；**先用便宜的支柱二內容驗證委任制好不好玩，再投入引擎重工**。

| 版本 | 主題 | 內容 | 工程性質 |
|---|---|---|---|
| **v41** | 支柱二：權力邊界成立 | 開局身分模式＋**不對稱切換**／**接管 Takeover**／需求單核心迴圈（接受·駁回·協商）／守位缺口三層防線／教練哲學×3＋執行度公式／**史冊埋 log**／**資產插槽登錄表**／**純GM 15年 smoke** | 大半是 v40⑤ 重構 |
| **v42** | 支柱二：記憶與帳單 | 承諾系統／教練 trust 迴圈／解職違約金／個人目標挖掘／**國際賽方針自選＋結果回饋** | 疊在既有事件＋忠誠管線 |
| **v43** | 支柱一：引擎換心 | 打席級模擬／統計驗證 harness／基礎進階數據（OPS+·FIP·BABIP·K%·BB%）／樣本數與信賴區間 | **最重**，玩家可見新東西少 |
| **v44** | 支柱一：套利成立 | 數據分析主管分級／運氣校正視圖／AI persona 定價眼鏡／**捕手專項 6**／**球迷三維度**（傳統數據眼鏡首次咬人） | 中重 |
| **v45+** | 支柱三：時間複利 | 農場多層級／文化沉澱／城市／聯盟＋球迷數據化演進／二代球員與傳承／資產包導入 | 收官層 |

> **球迷落地時機備註**：球迷的張力來自「他們只看傳統數據」，而這要等 v43/v44 的 L3 引擎與捕手專項落地才咬得到人。v41–v42 先埋**耐心乘數**（掛老闆 KPI）即可，完整三維度排 v44。

> **v43 實際落地備註（2026-07）**：v43 依附錄B原定做**郵件中樞**，並一併落地 Mars 六項真機需求（交易完整資料／掛牌釋出／純GM 全教練化／傷兵遞補准駁制／自由度 A案）。原表列 v43「引擎換心（打席級模擬）」與 v44「數據分析主管」順延一版評估；**選秀順位交易**（掛牌含順位條件）明確排 **v44**。詳見 §43。

**backlog（不排程）**：教練留學、戰術模擬室、行銷社會標籤、球風掛鉤票房、其餘 39 國逐場化、多層連鎖、友好度衰退。理由：外圍系統，不改變核心循環，卻會膨脹回歸與已知簡化清單。

---

## 附錄 C · 待拍板／衝突清單

| # | 項目 | 狀態 |
|---|---|---|
| 1 | 國際賽選人與排陣 | ✅ **v1.2 定案**：GM 親自 + 方針自選 + natCoach 改執行官 + 結果回饋本業 |
| 2 | 「壓縮或封存」範圍 | ⏳ **待 Mars 確認**（Claude 解讀 = 歷史交付檔封存，整合單檔仍不壓縮） |
| 3 | 教練哲學數量 | 3 種起步，v41 實作時定 |
| 4 | 舊存檔遷移 | 承 v40 已知簡化（設施不回溯歸零）；`gameMode` 舊檔預設 `gm_coach` |
| 5 | 球迷三維度落地版本 | 建議 v44（需 L3 引擎先到位）；v41 先埋耐心乘數 |

---

*《職棒GM：百年帝國》北極星設計書 v1.2 — 2026/07/18*
*v1.0 由 Mars 起草；v1.1 依市場分析（OOTP 26／ZenGM／Football Manager／栄冠ナイン／Astonishing Baseball）與 v40 實作現況修訂；v1.2 納入 Mars 拍板之國際賽定案、接管機制、不對稱切換與球迷壓力層。*


---

# （以下為歷代開發章節 §1〜§41）

# 職棒GM經營模擬遊戲 — 設計文件（完整版）

> 本文件是整個專案的「說明書」。每次開新對話時，請把這份文件（以及後續階段產生的程式碼檔案）一起上傳，Claude 就能接續開發，不會忘記之前的設計。
>
> **本文件會持續更新**：之後任何時候想新增或調整功能，直接告訴 Claude，文件會同步更新，不影響已經開發完成的部分。

最後更新：第0～7階段完成，第8階段（硬體建設）尚未開始；本次更新為財務系統深化（球員合約年限與市場身價薪資、自由球員市場、選秀新秀合約談判含失敗機制、轉播/贊助年度洽談、賽季中損益預測）、球探擴編為國內/國際/交易三位、現役球員「建議退休轉任教練／球探」連動機制（含賽季中臨時徵詢）、生涯型態標籤（早熟/晚成×早衰/長青獨立組合，退休後揭曉）、遊戲時間軸改為實際年月呈現，以及第7階段行銷企劃系統（年度行銷方案投資）

---

## 一、專案總覽

- **類型**：職棒總管（GM）經營模擬遊戲，文字為主、簡易圖示輔助
- **平台**：PWA（網頁 App，可加到手機主畫面，未來支援離線）
- **核心玩法**：單隊 GM 經營 ＋ 完整聯盟模擬（多隊 AI），並整合行銷、財務、球探選秀、交易、球員成長衰退、國際賽事、硬體建設、固定與隨機事件，全部互相連動
- **開發方式**：不寫程式碼，由 Claude 直接產生可執行的網頁程式，使用者測試回饋，逐階段疊加功能

---

## 二、聯盟架構

- 單一聯盟，共 **20 隊**
- 分為 **2 個聯盟區（A區／B區）**
- 每區再分 **2 個分組**，每個分組 **5 隊**
- 例：A區 → A1分組（5隊）、A2分組（5隊）；B區 → B1分組（5隊）、B2分組（5隊）

---

## 三、球季架構

- 例行賽場數：約 **120～130 場**（由賽程演算法依球隊數、對戰公平性自動決定固定場數，每季一致）
- 季後賽：**各分組前2名晉級，共8隊，交叉對戰**
- 冠軍賽：季後賽最終勝出隊伍對決

---

## 四、年度時間軸（賽季行事曆）

遊戲以「固定時間軸」推進，每年依序發生以下事件，時間到即自動觸發對應階段：

| 順序 | 事件 | 說明 |
|---|---|---|
| 1 | **春訓** | 季前調整，評估陣容、熱身賽；可選擇春訓基地（見第十二節） |
| 2 | **例行賽開幕** | 球季正式開打 |
| 3 | **例行賽（上半季）** | 期間可進行球員交易 |
| 4 | **明星賽** | 球季中段，全聯盟明星球員賽事 |
| 5 | **交易截止日** | 例行賽期間交易的最後期限 |
| 6 | **例行賽（下半季）** | 持續進行至球季結束 |
| 7 | **季後賽** | 各分組前2名，共8隊交叉對戰 |
| 8 | **冠軍賽** | 季後賽最終勝出隊伍對決；賽後頒發年度個人獎項 |
| 9 | **新人選秀會** | 休賽季，補充新秀 |
| 10 | **自由市場／休賽季交易** | 合約到期球員轉隊、球隊補強 |
| 11 | **冬訓** | 休賽季培訓，2軍/新秀重點養成期 |
| 12 | **國際賽事**（每4年一次） | 國家代表隊賽事，徵召旗下具備該國國籍的球員與教練參賽，影響球隊備戰與人員調度 |

完成第12步後，時間軸回到第1步，進入下一個年度。此時間軸為後續**選秀、交易、財務結算、行銷活動、球員成長判定**等系統的觸發依據。

---

## 五、球員屬性系統（0-100 數字制，50 為聯盟平均）

### 打者
| 屬性 | 說明 |
|---|---|
| 接觸力 Contact | 打到球、減少三振的能力 |
| 長打力 Power | 揮出長打/全壘打的能力 |
| 選球眼 Eye | 辨別好壞球、保送能力 |
| 對左投打擊 vs LHP | 面對左投時的打擊修正能力 |
| 對右投打擊 vs RHP | 面對右投時的打擊修正能力 |
| 跑壘速度 Speed | 跑壘、內野安打、破壞雙殺 |
| 盜壘 Steal | 盜壘成功率與意願 |
| 守備 Fielding | 防守範圍與穩定度（介面顯示為「守備成功率」，數值可理解為守備成功的百分比） |
| 臂力 Arm | 傳球力量與精準度 |
| 體力 Stamina | 連續出賽的抗疲勞程度，影響單場/連戰表現下滑速度 |
| 耐久度 Durability | 長期抗受傷體質，影響受傷機率 |
| 抗壓性 Composure | 關鍵時刻（比分接近的緊張局面）攻守表現的穩定度；比賽比分接近時，雙方球員的抗壓性平均值會影響最終誰能拿下關鍵一分，模擬「關鍵時刻容易失常或容易扛起球隊」的效果 |
| 潛力值 Potential | 未來成長空間上限 |

**多重守位系統**：每位打者可同時具備多個守備位置資格，每個位置各自有獨立的「熟練度」評分（0-100），例如：游擊手 85、二壘手 70、三壘手 55。熟練度越低，守備該位置時容易有額外失誤修正。

**捕手專項屬性**（僅捕手適用）
| 屬性 | 說明 |
|---|---|
| 配球引導 Game Calling | 帶投能力，影響投手臨場發揮穩定度 |
| 接捕框架 Framing | 接捕好壞球邊緣球的能力，影響裁判好球判定 |
| 阻殺跑壘 Caught Stealing | 反應與傳球精準度，影響阻殺盜壘成功率 |

### 投手
| 屬性 | 說明 |
|---|---|
| 球速 Velocity | 主要速球初速 |
| 控球 Control | 整體好球帶掌控度（基礎命中好球帶能力） |
| 抗壓性 Composure | 關鍵時刻（救援／中繼情境，以及比分接近的緊張局面）表現穩定度，影響救援與中繼成功率；野手也有對應的抗壓性數值（見上方打者屬性表），兩者共同構成比賽close game時的「關鍵時刻攻守投綜合表現」修正 |
| 體力 Stamina | 單場可承受的用球量 |
| 耐久度 Durability | 抗疲勞、抗受傷體質 |
| 潛力值 Potential | 未來成長空間上限 |

**牛棚角色**：先發、中繼、布局、終結四種角色，先發採5人輪值制循環登板；布局與中繼優先取得中繼點，終結負責救援點，兩者皆受抗壓性影響成功率。

**球路系統**：每位投手擁有 2~5 種球路組合（從下方球種清單中選擇），每種球路各自獨立擁有兩個數值：
- **球威**（0-100）：該球路的品質、欺騙性、造成揮空/弱擊的能力
- **控球**（0-100）：該球路投到指定位置的精準度

球種清單：四縫線速球、二縫線速球（伸卡）、卡特球、滑球、曲球、指叉球、變速球、蝴蝶球

例：投手A → 四縫線速球（球威78／控球65）、滑球（球威82／控球70）、變速球（球威60／控球75）

### 共同基本欄位
姓名、年齡、位置、慣用手（左打/右打/左投/右投）、所屬球隊、國籍、生涯與單季數據

### 先建欄位、之後階段才會啟用內容
薪資與合約（第6階段：財務）、士氣/化學效應（第10階段：事件系統）

**設計原則備註**：球員成長／衰退曲線（第3階段開發）將參考公開的棒球統計研究規律（如巔峰年齡、傷病機率分布等）作為生成演算法依據，不會複製特定真實球員的可辨識生涯軌跡，以避免肖像權／人格權爭議。每位球員在生成時會帶有個人化的「顛峰年齡」與「長青體質」隱藏參數，因此早熟短命型、晚成長青型等不同生涯樣貌都會自然出現，退休並非固定年齡到就強制發生，而是依年齡、能力衰退程度、個人體質綜合機率判定。

> **成長速度修正備註**：成長演算法已修正為「把潛力與現在能力的差距，平均分攤到距離顛峰年齡的剩餘年數」，並設有單季成長幅度上限，避免新秀在1~2年內就從評等D暴衝到天花板S的不合理現象。年輕、離顛峰年齡越遠的新秀，每季成長幅度會更平均分散在較長的養成期間內。

### 特殊技系統（獨立於0-100基礎屬性之外）

球員可額外擁有0個以上「特殊技」標籤，代表特定情境下的加成效果，部分特殊技需透過訓練／教練傳承習得（見第八節教練系統）。

範例（實際清單於第3階段細化）：
- 打者：短打高手、盜壘專家、關鍵時刻強打、長打鍛造、對左投剋星、對右投剋星、開路先鋒
- 投手：控球職人、伸卡名手、關鍵局守護神、耐久鐵臂、對左打剋星、對右打剋星

---

## 六、球隊基本欄位

球隊名稱、城市、所屬聯盟區/分組、戰績（勝/敗/和）
先建欄位：預算（第6階段）、球場與設施等級（第8階段）、行銷/品牌值（第7階段）

---

## 七、球隊組織架構（1軍／2軍）

每支球隊由兩層級組成：
- **1軍**：正式出賽名單，**28人**
- **2軍**：培養／傷兵調養層級，**固定32人**，用於新秀養成、傷後復健、狀態調整。1軍與2軍球員可依戰況與養成需求相互升降（可自由升降，不強制1對1交換；超編/缺編不會導致遊戲錯誤，僅會在名單不符出賽條件時暫停模擬）

> **實作狀態備註**：先發棒次（含守位分配）與投手輪值管理已實作，並持續擴充。本聯盟採**指定打擊制（DH）**，投手不用打擊，先發打線需排滿「8個守位＋DH」共9人；系統會自動避免同一守位出現兩位先發（例如同時有2位捕手先發），手動調整守位時若與其他先發重複，會自動與對方互換守位。**球員也可以「移防」到非本職守位出賽（例如讓游擊手臨時去守二壘或外野），系統會依守位族群的落差程度扣減實際守備成功率（同族群如內野對內野、外野對外野落差較小；跨族群或涉及捕手落差較大），並直接反映在比賽防守評分上，介面上會用「移防」標示與星號提醒。** 投手輪值除**先發**（5人輪值）外，**中繼、布局、終結**三個牛棚角色也各自有可調整的登板優先順序（越前面越優先被派上場），皆可一鍵「自動排列」恢復系統推薦。球員名單、先發打線、投手輪值、教練團四個畫面之間已整合共用導覽列，可直接互相切換，不需每次返回主控台；球員名單表格也會直接標示該球員目前是否為先發（棒次/守位或輪值/牛棚順位），不需另外切換畫面查詢。AI球隊則持續採即時最佳化陣容，不需手動管理。

---

## 八、教練系統

每支球隊的1軍、2軍**各自配置獨立教練團**，職位包含：
總教練、投手教練、捕手教練、內野守備教練、外野守備教練、打擊教練、跑壘教練、體能教練（2軍另設「2軍監督」統籌培養方向）。

> **實作狀態備註**：教練系統已完整實作，包含專精領域對應加成（各職位精準加成對應能力，而非籠統平均）與約20%機率的特殊能力（額外加成），詳見教練屬性表。**玩家自己球隊的教練合約到期時，系統會先暫時自動指派一位人選頂上（避免職位空缺造成其他邏輯出錯），但會列入「待確認」清單並在教練團畫面明顯提示，玩家可隨時點「更換」開啟並排比較介面，從2~3位退休球員轉任候選＋2位新聘教練候選中挑選（各自顯示指導力、特殊能力、合約年限、與現任的優劣差異），而非過去那種完全隨機、玩家無從置喙的自動指派。**（AI球隊的教練合約到期仍維持原本的自動邏輯，不影響效能。）玩家也可隨時手動指派任一退休球員擔任任一教練職位，並可將同一職位的1軍教練與2軍教練直接互換職務（例如把表現優秀的2軍打擊教練拉上1軍）。

### 教練屬性
| 屬性 | 說明 |
|---|---|
| 指導力 Teaching | 整體培養效率，影響受訓球員屬性成長速度 |
| 專精領域 Specialty | 該教練特別擅長培養的能力類別（如長打力、控球、變化球開發、跑壘、心理素質等），受訓球員在該領域成長獲得加成 |
| 特殊能力 Special Ability | 教練獨有效果（例如「魔球開發」：加速投手學會新球路；「鐵血調教」：加速耐久度成長） |
| 可傳承特殊技清單 | 該教練具備哪些特殊技，可透過訓練傳授給符合基礎屬性門檻的球員 |

### 特殊技傳承機制
特殊技除了球員自然成長習得外，也可透過「教練（或隊內具備該特殊技的資深球員）指導＋球員符合對應基礎屬性門檻＋訓練機率判定」的方式傳承習得，機制細節於第3階段（成長衰退系統）具體設計。

### 教練合約
教練具備合約欄位：**合約年限、年薪、續約/解約條件**。合約到期後可續約、轉隊或退休，機制與球員合約系統（第6階段財務）共用邏輯。

---

## 九、球探系統

球探為獨立於教練之外的專職角色，負責評鑑球員能力，其**評鑑準確度**會影響GM在選秀、交易、國際簽約時「看到的數值」與「真實數值」之間的落差。

### 球探屬性
| 屬性 | 說明 |
|---|---|
| 評鑑準確度 Accuracy | 數值越高，回報的球員能力評估越接近真實值 |
| 負責範圍 Coverage | 負責的區域（國內／特定國家或地區），影響對該範圍球員的情報質量與更新頻率 |
| 專精領域 Specialty | 擅長評估的類型（如打者潛力、投手潛力、國際球探等） |

### 球探報告內容（已實作）
選秀畫面呈現的球探報告包含：現在能力評等（S/A/B/C/D）、天花板評等（潛力值的球探估計）、**各項細分工具評估值**（打者：接觸/長打/選球/速度/守備；投手：球速/控球）、成熟度說明（依年齡判斷養成期長短，文字已同步反映修正後較寫實的養成年數）、評估信心度說明（依球探準確度呈現「高度可信／中等可信／落差可能較大」，並附上球探準確度數值本身供對照）。

> **實作狀態備註**：目前僅有「國內選秀球探」職務生效，且已擴充用於**交易系統**——瀏覽交易對象的球員時，同樣套用己方球探準確度產生評估值。國際球探（評估外籍球員）需等**第9階段（國際化）**建立外籍球員後才有意義，屆時會為球探系統擴充對應職務分工，而非另立新系統。

### 球探合約
球探具備合約欄位：**合約年限、年薪、續約/解約條件**，機制與教練合約相同。

球探系統實際的評鑑誤差機制、選秀運作於**第4階段（球探與選秀）**具體開發，本階段先定義角色資料結構。

---

## 十、個人獎項與成績記錄系統

> **實作狀態備註**：本節已實作，並持續擴充。完整獎項清單：MVP、打擊王、全壘打王、安打王、打點王、盜壘王、防禦率王、勝投王、三振王、救援王、中繼王（依中繼成功次數）、最佳新人（皆league-wide單一得主）；另有依**聯盟區**分組（各1人）的**金棒獎**（最佳打者）與**金臂獎**（最佳投手）；以及依**聯盟區×守位**分組的**最佳9人**與**金手套獎**。由於本聯盟採指定打擊制，外野守位已拆分為**左外野手／中外野手／右外野手**各自獨立評選（不再合併為單一「外野手」），金手套獎涵蓋捕手/一壘/二壘/三壘/游擊/左外野/中外野/右外野共8個守位；最佳9人在此8個守位之外，再加上**指定打擊（DH）**類別（由該區打擊分數最高、且尚未獲得其他守位最佳9人的球員入選），共9個類別，DH因不需守備故不計入金手套獎。介面上「守備」數值已統一標示為「守備成功率」並以百分比呈現，避免評估意涵不清。單季/生涯成績自 **v52** 起改採**逐打席模擬**：每一個打席由打者屬性、投手屬性、捕手專項與守備評分共同決定結果（三振／四壞／觸身／全壘打／一二三壘打／失誤／出局，含雙殺打、高飛犧牲打、盜壘、暴投捕逸），比分由這些事件累積得出，投手換人由局數／體力／疲勞／牛棚順位決定，非自責分由守備失誤直接產生。v51 以前的「依球員能力值統計分配（非逐球模擬）」模型已停用；年度頒獎典禮已併入年度時間軸，於冠軍賽產生冠軍後、進入選秀會前呈現。

### 成績記錄
- **單季成績**：每位球員每個球季獨立記錄一份數據（打者：打擊率/上壘率/長打率/全壘打/打點/盜壘等；投手：防禦率/勝敗/救援/三振/被上壘率等）
- **生涯成績**：所有球季數據累加彙總，並保留生涯逐季紀錄可供查閱
- 成績記錄與第十四節「長期營運架構」的歷史資料歸檔壓縮機制連動：近期球季保留完整細節，久遠球季保留數據摘要

### 個人獎項
於球季末（冠軍賽結束後）依成績與戰績頒發年度獎項，例如：
- 最有價值球員（MVP，可分投手/野手或不分）
- 打擊王、全壘打王、打點王、盜壘王
- 防禦率王、勝投王、救援王、三振王
- 金手套獎（各守位最佳防守）
- 最佳新人
- 名人堂（生涯累積成就達標後入選，屬長期營運架構的一部分）

獎項紀錄會永久保留於球員的生涯資料中，作為球員身價／薪資談判（第6階段財務系統）與名人堂資格的重要依據。

---

## 十一、開局設定

新遊戲開始時，提供三個輸入欄位：
- **GM姓名**：玩家自訂總管名稱
- **球隊名稱**：玩家自訂所屬球隊
- **聯盟名稱**：玩家自訂整個聯盟的名稱

三個欄位皆可留空，留空的項目由系統自動隨機生成（需建立對應的隨機姓名庫／球隊名庫／聯盟名庫）。

---

## 十二、世界觀與國籍系統

- 本聯盟所屬一個**虛構國家**，本土球員／教練使用**華人姓名庫**生成
- 其餘球員來源分兩類，統稱「外籍球員」：
  - **現實國家風格**：如美式、日式、韓式、拉丁美洲式等姓名風格庫（僅模仿命名慣例生成虛構姓名，不使用任何真實球員本人的姓名或資料）
  - **另創虛構國家**：可自訂1個以上的虛構國家，各自有專屬姓名風格庫
- 註：第1階段MVP若要提前有基本可玩性，至少需準備「華人姓名庫」；其餘國家姓名庫可在第9階段前陸續補齊

**姓名生成架構**：採「姓氏庫 × 名字庫」隨機組合生成，而非固定名單，大幅降低重複率（例如150個姓氏×300個名字＝4萬5千種以上組合）。各國籍風格（華人、西方、拉丁、日式、韓式等）各自建立獨立的姓氏／名字庫。每位球員另配有系統內部唯一編號，即使生成同名同姓也不影響資料辨識，反而更貼近現實。

### 國家規模與棒球發展程度

- 世界觀共設定約 **20個現實風格國家**（美式、日式、韓式、拉丁美洲式等命名風格）＋約 **20個虛構國家**，合計約40個國籍來源
- 每個國家各自擁有：
  - **棒球發展程度**（分級，如：頂尖／強豪／發展中／初階）：影響該國球員產出的整體素質分布與人才數量
  - **擅長領域**（與教練專精領域共用同一套分類，如：力量訓練、控球精修、跑壘敏捷、投手開發、心理素質等）：影響該國球員的能力傾向

### 外籍球員／教練標記

- 球員、教練資料皆標註**國籍**，凡非本聯盟所屬虛構國家者，介面上明確標示「外籍」註記
- **外籍教練**在第4步「國際賽事」（每4年一次）期間，同樣可被徵召**返回祖國代表隊效力**，與外籍球員的國際賽徵召機制相同，此時該教練會暫時離隊，球隊需自行調度應對

### 春訓基地系統

- 球隊可選擇**春訓基地地點**（對應各國家），選擇依據為該國的「棒球發展程度」與「擅長領域」
- 選擇特定國家作為春訓基地，受訓球員可獲得對應領域的訓練加成（邏輯與教練專精加成類似，兩者可疊加）
- 此系統實際規則於**第9階段（國際化）**與**第3階段（成長衰退系統）**共同開發

國際化的多國姓名庫、外籍球員/教練生成邏輯、國際賽事實際運作於**第9階段（國際化）**開發，本階段先定義資料結構。

---

## 十三、存檔方式

- 第一版：**本機瀏覽器存檔**，技術上採用 **IndexedDB**（而非容量過小的 localStorage），以支援長期累積的大量資料
- 之後可視需求升級為雲端同步（需加帳號登入機制）
- 需提供「匯出存檔備份／匯入還原」功能，避免瀏覽器資料被清除導致進度遺失

---

## 十四、長期營運架構（支援100年以上長期經營）

要讓遊戲能穩定玩超過百年不卡頓、不爆存檔，除了基本資料結構外，還需要以下機制：

| 機制 | 用途 |
|---|---|
| 歷史資料歸檔壓縮 | 近期球季保留完整細節，較舊球季只保留摘要數據，避免存檔無限膨脹 |
| 球員自動生成引擎 | 每年自動產生新秀（選秀/國際），含姓名庫、屬性隨機分布、潛力生成邏輯，維持聯盟球員供給 |
| 退休與名人堂系統 | 球員退休後生涯數據封存為歷史紀錄，不再佔用現役運算資源 |
| 經濟通膨模型 | 薪資、票價、預算基準隨年份緩慢調整，避免長期經營後數字失真 |
| 模擬引擎效能分級 | 玩家球隊可逐局/逐球呈現；其他AI球隊採機率統計模型快速運算，只保留結果數據 |
| 資料格式版本控制 | 新階段功能陸續加入時，舊存檔資料結構仍可相容讀取 |

---

## 十五、後續功能擴充原則

本文件會持續迭代，日後若想新增或調整任何功能，只要在對話中提出，Claude 會：
1. 直接更新本說明書對應章節
2. 若功能影響既有資料結構，會同步設計「舊存檔相容方案」（依第十四節版本控制機制）
3. 若功能屬於尚未開發的階段，會先記錄在對應階段位置，待該階段開發時落地實作

---

## 十六、財務策略系統（第6階段，已實作第一版）

**球隊財務狀態**（`team.finance`）：預算（現金結餘）、票價等級、人氣值（0-100）、當季薪資總額、上季收支報告。

**收入來源**（球季結束時統一結算）：
- **門票收入**＝球場容量（暫定固定18,000人，第8階段硬體建設可再擴充座位數/設施等級）×進場率×票價×主場場次。進場率由人氣與戰績共同決定，票價越高則進場意願（需求乘數）越低。
- **轉播收入**：依人氣值浮動的固定＋比例收入。
- **贊助收入**：依人氣、戰績（勝率）、是否晉級季後賽（有加碼）浮動。
- **周邊商品／球場販賣部收入**：暫時歸類在本階段，以「依人氣穩定成長」的簡化模型呈現被動收入；**球場設施升級（可提升販賣部/周邊營收上限）規劃於第8階段「硬體建設」，主動行銷活動/品牌操作規劃於第7階段「行銷企劃」**，屆時本階段的被動收入模型會被那兩階段的機制擴充，而非重做。

**支出項目**：
- 球員薪資：依球員目前綜合能力與年齡估算「市場身價」，逐季往身價平滑靠攏（避免暴起暴落）。
- 教練／球探薪資：沿用既有的教練/球探合約年薪欄位。
- 選秀簽約金：選秀選中球員的當下**立即**從預算扣除（輪次越前面、球探天花板評等越高，簽約金越貴），並非球季結算時才扣。
- 奢侈稅：若球隊薪資總額超過**全聯盟平均薪資的1.3倍**，超出部分課徵50%奢侈稅，於球季結算時扣除。

**票價策略**：可在每個新球季開幕當下（尚未模擬任何一場比賽前）於「財務」畫面調整票價等級（親民／中等／高檔／頂級4個級距），球季開打後即鎖定，需等下個休賽季開幕前才能再調整。

**財務健康度與續約意願**：預算為負或薪資超過奢侈稅門檻時，「財務」畫面與主控台會顯示非阻擋性警示；退休球員被指派/聘用擔任教練時，若球隊財務吃緊（預算為負或偏低），婉拒機率會提高，反映「球隊財務不佳，人才較不願意加入」的真實感。

---

## 十七、球員生涯型態、合約與職涯轉任系統（本次更新）

**生涯型態（早熟/晚成 × 早衰/長青，退休後才揭曉）**：每位球員的「顛峰年齡」（21-34歲）與「長青體質」是完全獨立生成的兩個隱藏參數，因此「早熟+早衰」「晚成+早衰」「早熟+長青」等各種組合都會自然出現，不會互相綁定。退休後會在退休名單卡片顯示「生涯型態標籤」（如：早熟型・長青型），讓玩家事後回顧驗證。開發中的成長曲線也已修正為把潛力差距平均分攤到距離顛峰年齡的剩餘年數，避免不合理的暴衝式成長。

**遊戲時間軸（實際年月）**：不再只顯示抽象的「第X天」，改用實際年月呈現：遊戲從西元2025年10月開始（代表GM剛接手、上一季剛結束），之後每年循環「11月休賽季→12月選秀→3月春訓（可調票價/洽談轉播贊助/規劃行銷）→4月開幕→4~9月例行賽→10月季後賽」。

**球員合約**：球員現在有明確的合約年限與年薪（依當前能力/年齡估算「市場身價」，簽約時鎖定，不再每季重新平滑調整），會顯示在名單表格與球員詳細頁。合約到期時（僅套用於玩家球隊，AI球隊維持自動續約以節省效能）約92%機率自動續約，財務吃緊時球員婉拒續約機率提高，婉拒續約者會離隊成為自由球員。

**自由球員市場**：新增「自由球員市場」畫面，可簽下市場上的自由球員（需支付約年薪30%的簽約金），名單需有空位。

**新秀合約談判**：選秀選進球員時會即時談判簽約：依輪次訂出薪資上限（新秀合約天花板）、最少綁約5年；天花板評等越高的新秀，開價超出上限、談判破局的機率越高（S級最高35%），談判失敗的球員會回到選秀池讓其他球隊選擇，本次選秀該順位視同落空。

**球探擴編為三位**：國內球探（負責選秀評估準確度）、國際球探（第9階段國際化開放後才會實際發揮作用，先建立編制）、交易球探（負責評估交易對象的準確度），可個別更換。

**現役球員「建議退休轉任教練／球探」連動機制**：在球員詳細頁可對自己球隊的現役球員發動：
- **休賽季（春訓等離線期間）**：先詢問是否願意接受退休建議（機率隨年齡提高），若拒絕可選擇「繼續留隊」或「釋出至自由球員市場」；若接受退休，再詢問是否願意就任教練或球探（機率受球隊財務健康度影響，財務吃緊時婉拒機率提高），若拒絕可選擇「讓他回歸球員」或「維持退休」。
- **賽季中**：球員完全不考慮退休，只能詢問是否「臨時兼任」教練/球探（機率很低，通常會拒絕），一旦接受會直接離開現役名單就任該職務。

**教練/球探聘僱並排比較**：教練團與球探室的「更換」功能都會列出多位候選人（含退休球員與新聘人選）並排比較指導力/特殊能力/合約條件再決定，取代過去的盲目自動指派。

---

## 十八、財務深化與行銷企劃系統（第6～7階段深化）

**轉播與贊助合約（每年可重新洽談）**：春訓期間可從3種方案（保守/標準/積極）中選擇一項，鎖定該季的轉播/贊助base金額（贊助另有依戰績浮動的獎金）；不特別選擇則套用預設估算公式。

**賽季中損益預測**：「財務」畫面隨時可查看「本季預估損益」（依目前人氣/戰績/已簽合約估算），不用等到季末結算才發現大幅虧損，虧損時會有明顯提示建議提早因應。

**行銷企劃（第7階段）**：新增「行銷企劃」畫面，春訓期間可投入行銷方案（基礎/標準/全力，費用立即從預算扣除），效果為提升該季「人氣成長速度」與「周邊/販賣部收入」，不直接影響戰績。球場設施升級（可再提升周邊/販賣部收入上限與球場容量）規劃於第8階段「硬體建設」。

---

---

## 二十、第7→8階段之間的重大修正（本次會議）

本次修正九項既有系統問題，均已實作並通過node模擬回歸測試（開局→選秀→談約→開幕→整季模擬→季後賽→隔年休賽季→財務裁員→續約談判→自由球員→票價）：

1. **投手球速改回真實km/h顯示**：`velocity`內部仍是0-100評等值（驅動成長/衰退/戰力等既有公式，避免牽動模擬平衡），但所有介面（名單、輪值、球員詳細頁、選秀球探報告、交易畫面）一律用`velocityKmh()`換算成真實公里/小時顯示（約116~169km/h）。

2. **門票分級擴充為5級距＋自訂金額＋逐年上限成長**：`TICKET_PRICE_PRESETS`提供5個預設級距（庶民150／親民300／中等450／高檔650／頂級900），也可自行輸入金額，介面即時顯示AI估算的進場成數；進場意願比例固定落在100%（票價=下限）~50%（票價=目前上限）之間。每支球隊有各自的票價上限（起始1000元），若當季票價開在上限附近仍接近滿場，隔年會自動調高上限，最終不超過5000元。

3. **談約系統全面改為互動式**：新增統一的談約模組（`startNegotiation`/`submitNegotiationOffer`/`finalizeNegotiation`/`failNegotiation`），涵蓋在役球員續約、自由球員簽約、新秀簽約三種情境。AI會先依球員身價/等級提供建議開價，並顯示球員的心理期望薪資與年限，玩家可自行調整後送出，最多5次來回機會：
   - 在役球員續約談不成 → 自動離隊、進入自由球員市場。
   - 自由球員談不成 → 轉投別隊、離開市場。
   - 新秀簽約談不成（5次都談不成）→ **這位新秀直接放棄加盟、退出本屆選秀**（不會回選秀池被其他球隊選走，也不會隔年再出現）。

4. **修正退休球員被自動轉任教練的問題**：教練合約到期時的「暫代」邏輯，一律用全新生成的教練頂上空缺，**不再消耗任何真實退休球員的身分**。只有玩家自己在教練團畫面主動挑選某位退休球員來聘用時，那位球員才會真正轉任教練、失去「留任」資格；玩家想留住的退休球員永遠不會被系統偷偷徵用。

5. **選秀天花板等級配額**：每屆選秀（120人）的真實潛力值改為配額制產生——S級每年僅1~2人、A級4~8人，B級以下依比例遞增、不設固定上限，避免超頂級新秀氾濫。球探評估仍有準確度誤差，不會100%精準對應真實等級。

6. **選秀放棄支援一次跳過剩餘所有順位**：按「放棄本輪選秀權」後可選擇「只跳過這次」或「放棄本次選秀剩餘所有順位」，選後者之後每輪不會再詢問，自動跳過到選秀結束。

7. **季後不續約選項＋財務赤字強制裁員**：休賽季合約到期改為玩家逐一決定「談約」或「不續約」（不續約直接進自由球員市場），也提供「全部依市場行情自動續約」快速鍵；若球隊財務出現赤字（預算為負），會先進入「財務赤字．強制裁員」畫面，列出薪資排序的球員供玩家釋出，直到薪資總額回到聯盟平均的1.15倍以內才能正常繼續（也提供「維持現狀、自行承擔風險」的例外繼續選項，避免無解卡關）。

8. **交易畫面補上守位與完整投打數據**：交易清單現在會顯示每位球員的守位（守位為公開資訊，真實不變），自己球隊球員顯示真實能力值，對方球隊球員顯示球探評估值（含守備與速度，不再只有2個數字）。

9. **修正開局時間軸**：新開局代表「西元2025年10月，上一任GM的球季剛結束」，選完球隊後會先進入「休賽季異動摘要」畫面，接著才是選秀會（代表11~12月的活動），選秀＋談約全部完成後才會進入「2026年3月春訓」畫面（`beginFirstSeason()`），不再一開局就跳過整段休賽季直接顯示春訓。後續每年的休賽季轉換流程也統一套用相同的「摘要→(強制裁員)→(續約談判)→選秀」順序。

---

## 十九、開發階段進度表

- [x] 第0階段：地基設計（本文件）
- [x] 第1階段：MVP核心迴圈（選隊、簡易模擬比賽、戰績榜）
- [x] 第2階段：完整聯盟（20隊AI、完整賽程、季後賽）
- [x] 第3階段：球員屬性、成長與衰退
- [x] 第4階段：球探與選秀
- [x] 第5階段：交易系統
- [x] 第6階段：財務策略
- [x] 第7階段：行銷企劃
- [x] 第8階段：硬體建設
- [x] 第9階段：國際化
- [x] 第10階段：事件系統（v36 完成；v38 深化為16種＋連鎖事件）
- [x] 第11階段：打磨與PWA完成度（v37 完成：manifest／SW／圖示／加入主畫面）

> 12 階段路線圖至此全部完成。v38 起進入 backlog 梯次（逐屬性潛力／事件深化／國際賽逐場化／C・D國家互動），
> 進度以版本號續推，不再新增階段編號。

## 二十一、第8階段：硬體建設

球場硬體分5個等級（簡易球場→標準球場→現代化球場→旗艦球場→地標級巨蛋），只能逐級升級、不能跳級，升級只能在春訓期間（開幕前）投資，費用立即從預算扣除、當季就會生效：

| 等級 | 名稱 | 容量 | 周邊/販賣部收入倍率 | 升級費用 |
|---|---|---|---|---|
| 1 | 簡易球場 | 18,000 | x1.0 | －（起始等級） |
| 2 | 標準球場 | 22,000 | x1.15 | 8,000萬 |
| 3 | 現代化球場 | 26,000 | x1.3 | 18,000萬 |
| 4 | 旗艦球場 | 31,000 | x1.5 | 32,000萬 |
| 5 | 地標級巨蛋 | 36,000 | x1.75 | 50,000萬 |

等級越高，球場容量越大（直接影響票務收入的滿場人數上限）、周邊/販賣部收入倍率也越高（與行銷企劃的周邊收入加成疊加計算）。新增「球場硬體建設」畫面（`renderFacilities`），可在此查看目前等級與下一級的升級條件並直接投資。

## 二十二、本次會議修正（談約金額顯示／球場容量重新分級／第9階段國際化）

**談約金額改用「萬元」為輸入單位**：談約畫面（新秀／自由球員／國際球員／續約）的年薪輸入欄位改為以「萬元」為單位輸入（例如輸入500代表500萬元），並即時顯示格式化後的完整金額（如「500萬元」），大幅減少要輸入/閱讀的位數；其餘畫面本來就統一透過`formatMoney()`以萬/億為單位顯示，不會出現一長串數字。

**球場硬體重新分級**：起始容量下修為1萬5千人、最高可蓋到5萬人，並細分為7個等級（簡易球場15,000人→社區球場20,000→標準球場25,000→現代化球場30,000→大型球場37,000→旗艦球場44,000→地標級巨蛋50,000），周邊/販賣部收入倍率也隨等級提高（x1.0～x1.8）。

**修正選秀等級配額邊界重疊bug**：先前A級天花板數值區間上界（75）剛好落在`gradeFromValue()`判定S級的門檻（v≥75即為S），導致S級新秀配額（1~2人）偶爾會超標。已修正為A級上界改為74，與S/A/B/C/D門檻完全對齊、不再重疊。

### 第9階段：國際化

- **虛構海外4國姓名庫**：赤陽國（日式音譯風格）、藍岸共和國（拉美音譯風格）、北原聯邦（歐美音譯風格）、白熊聯盟（斯拉夫音譯風格），各自有獨立姓氏/名字庫，生成的外籍球員姓名風格明顯區隔於本土球員。
- **國際球員市場**：新增「國際球員市場」畫面，每年休賽季重新開放一批（15~25位）外籍自由球員供簽約，未簽約者視為留在原聯盟效力、不會留到隔年。能力數值為「國際球探」的評估值（球探室的國際球探職務正式生效），準確度越高落差越小。
- **簽約流程**：與其餘談約情境一樣走互動式談約系統（AI開價／玩家調整／最多5次機會），簽約金比例較高（年薪的40%，反映跨海挖角成本）；1軍最多可同時註冊4位外籍球員（超過上限的簽約會安排到2軍）。
- **每4年一次的國際賽事**：球季開幕時若逢西元年份可被4整除的年度（`S.seasonYear % 4 === 0`），會自動召集各國旅外最強的球員（每國最多3人）返國代表隊效力，這些球員會標記「國際賽事服役中」且在服役天數內（10~18場）無法被排入打線或投手輪值（含自動排陣），名單畫面會顯示服役剩餘場次，服役結束後自動歸隊。
- **已知簡化範圍**：AI球隊目前不會主動簽約國際球員（規劃留待後續事件系統/AI強化階段再補強），國際球員市場目前主要提供玩家使用；外籍教練的國際賽事召集（設計文件原規劃項目）本次僅記錄於文件、尚未實作對應的教練暫時離隊機制，留待後續打磨階段。

## 二十三、本次會議修正（v23：拒絕轉教練流程重構）

**背景**：先前「建議現役球員退休轉任教練／球探」與球員詳情頁的「強制退休」按鈕是兩套互不相關的機制，造成操作混淆（使用者回報「按鍵無用」），且婉拒時會出現兩張不同的處理卡片（「留任／釋出」與「回歸球員／維持退休」），流程分裂。本次統整為單一流程：

- **移除獨立「強制退休」按鈕**：球員詳情頁不再有任何強制退休入口（含二次確認卡）。原`forceRetirePlayer()`改為內部函式`retirePlayerToList()`，僅供轉任流程在球員「同意」後呼叫，不再標記`forcedRetirement`，也不再有任何非自願退休路徑。
- **統一婉拒結果**：休賽季建議退休轉任時，不論球員是「婉拒退休建議」（`reason: "declinedRetire"`）還是「同意退休但婉拒職務邀約」（`reason: "declinedRole"`），球員都**維持現役身分**（不會再先被退休再問要不要回歸），GM一律只有兩個選項：「繼續留隊」或「釋出至自由球員市場」（統一走`UI.retireOffer`卡片，文案依婉拒原因區分）。舊版「回歸球員／維持退休」卡片（`UI.coachRefusal`）僅保留給「從退休名單聘用教練遭婉拒」的既有情境使用。
- **新增現任人選能力比對**：球員詳情頁選擇建議轉任的職務時（下拉選單，選擇會即時記憶於`UI.suggestRole`），會即時顯示`renderRoleComparison()`比對表——教練職務比對「現任教練的指導力／特殊能力／年薪／合約」vs「該球員轉任後的指導力（＝教練潛力）／預估薪資區間」；球探職務比對「現任球探的評估精準度／年薪／合約」vs「轉任後的預估評估精準度」，並明確提示就任後現任人選將被解除職務。
- **賽季中臨時徵詢兼任**：行為不變（高機率婉拒、婉拒僅顯示訊息不出卡片、不涉及退休）。
- **回歸測試**：Node.js vm沙盒完整流程測試（開局→選秀→整季→季後賽→休賽季）＋轉任流程7大場景專項測試（婉拒退休／婉拒職務／兩關同意就任／比對卡渲染／詳情頁渲染／賽季中徵詢／退休名單聘用婉拒）＋三季連續模擬煙霧測試，全數通過。

## 二十四、本次會議修正（v24：七大需求②～⑦一次完成）

延續v23（①拒絕轉教練流程重構），本版本一次完成剩餘六項需求，依既定順序②→③④→⑤→⑥→⑦實作並全數通過回歸測試。

### ②投手輪值彈性化
- `autoRotation()`不再寫死5人：預設自動排入所有健康的「先發」角色投手（幾人就排幾人）；若1軍完全沒有先發角色投手才以綜合能力取前4名頂替。
- 輪值畫面（先發/中繼/布局/終結四分頁）全面支援**自由增減人數**：每列新增「移除」按鈕、底部新增「新增人選」按鈕；先發至少保留1人，牛棚各類別可為空。
- `ensureBullpenOrder()`改為：玩家球隊只在第一次初始化時自動補人，之後**尊重玩家手動移除的名單**不再自動補回；AI球隊維持自動補滿確保可運作。
- 名單警告從「建議補滿5人」改為只在「輪值中沒有任何可出賽投手」時警告，並新增傷兵警示。

### ③選秀談約薪資改依天花板等級分級
- 廢除依輪次的`ROOKIE_SALARY_CAP`，改為`ROOKIE_GRADE_SALARY_CAP`：**S 800萬／A 700萬／B 500萬／C 350萬／D 200萬**（維持設計指定的8:7:5遞減比例，並對齊遊戲既有薪資經濟量級——聯盟平均資深球員年薪約數百萬）。輪次退居次要修正因子（第1輪x1.0遞減至第6輪x0.85）。後段輪次撿到的S級新秀薪資上限仍以S級計算。
- 簽約金`draftSigningBonus()`同步改為等級為主（S 1500萬基準遞減至D 200萬）、輪次小幅修正。

### ④金額顯示統一稽核與邊界進位修正
- `formatMoney()`修正1億邊界進位bug：先把數字以「萬」為單位四捨五入後再決定顯示單位，9999.5萬（99,995,000元）現在正確顯示為「1.00億元」而非「10000萬元」。
- 全面稽核金額顯示：所有金額欄位均經過`formatMoney`；順手修正既有缺陷——AI球隊休賽季`refillRoster`補進的球員先前沒有薪資/合約欄位（交易畫面等處會顯示「-」），現於`finishDraft`收尾時統一`refreshPayroll`補齊。

### ⑤受傷機制（全新系統）
- 出賽球員每場有低機率受傷（基礎0.35%，**耐久度越低、年齡越大機率越高**），傷勢分**輕度3~10天／中度11~30天／重度31~90天**三級（耐久度低者偏向重傷），各有具體傷名（腿筋拉傷、手肘韌帶損傷等）。
- 傷兵（`p.injury`）**無法出賽**：打線、先發輪值、牛棚調度全部自動跳過；名單/輪值/詳情頁顯示「🩹傷名・剩餘天數」標記，主控台新增傷兵名單卡，詳情頁顯示恢復進度條。
- 每模擬1天恢復1天；自家球員受傷時跳出傷兵通報；**休賽季所有傷勢痊癒**（於成長結算時清除）。已知簡化：季後賽期間傷勢不遞減（傷兵持續缺陣至休賽季痊癒）。

### ⑥硬體建設擴充（球場之外新增三大類設施）
- **訓練基地**：10個獨立升級項目（各Lv.0~5，費用500萬→1.2億逐級加倍）——投手項目（球速/控球/變化球）、打者項目（打擊/長打/選球※）、捕手訓練室（配球引導/接捕框架/阻殺跑壘）、守備訓練場（守備/臂力）、跑壘訓練場（速度/盜壘）、心理抗壓中心（抗壓性）。每級+0.06成長加成，**與教練指導加成疊加**作用於年度成長結算。※設計原案的「觸擊」目前無對應球員屬性，暫以「選球」替代，如需新增觸擊屬性請另開需求。
- **醫療室**：Lv.0~5（800萬→1.8億），每級受傷機率-8%、恢復天數-6%（滿級-40%／-30%），與⑤受傷機制連動。
- **球探辦公室**（Claude補齊提案項目）：Lv.0~5（1000萬→2.4億），每級三位球探有效評估精準度+2，並擴大國際獨家人脈名額，與⑦連動。
- 硬體建設畫面改為四分頁（球場/訓練基地/醫療室/球探辦公室），全部維持「僅春訓期間可投資、逐級升級」規則。AI球隊開局隨機擁有0~2級設施且不再升級（已知簡化，玩家可靠投資取得長期優勢）。

### ⑦外籍球員資訊完整化＋球探獨家人才機制
- 國際球員市場改為**比照本土選秀的完整球探評估卡片**：現況/天花板等級徽章、類型原型、成熟度、信心度，投手顯示球速/控球/體力/抗壓/球路數，野手顯示接觸/長打/選球/速度/盜壘/守備/臂力/抗壓，並顯示守位與投打習慣。
- `attachScoutedEstimates()`擴充評估欄位（投手+體力/抗壓；野手+盜壘/臂力/抗壓），本土選秀同步受惠。
- **獨家人才機制**：新增「有效球探精準度」＝球探能力＋球探辦公室加成（選秀/國際/交易三類評估統一採用）。國際球探有效精準度達63以上開始解鎖**獨家人選**（63→1位、71→2位、79→3位、87→4位、95→5位/年），獨家人選為能力與潛力顯著較高的菁英（標示「獨家情報」徽章），代表其他球團接觸不到的人脈；精準度不足則只能與市場共搶公開名單。

### 測試紀錄
Node.js vm沙盒：既有全流程回歸＋①七場景專項全數通過；新增②~⑦共約40項專項斷言全數通過；壓力測試含**舊存檔相容（缺新欄位lazy補齊）、全投手傷兵極端情境、10季連續模擬**（無異常薪資、無殘留傷勢物件、獨家名額與公式一致）全數通過。

## 二十五、本次會議修正（v25：40國世界系統・春訓・季末國際賽・遊戲靈魂第一波）

### A. 40國世界系統（Mars定案：現實20國＋虛構20國，台灣取代巴西；玩家母國＝虛構國「青雲國」）
- 新增`NATIONS`（40國）：等級 S×4／A×8／B×12／C×10／D×6。現實20國含美國、日本、多明尼加、韓國、**台灣**、古巴、委內瑞拉、墨西哥、波多黎各、巴拿馬、加拿大、荷蘭、澳洲、哥倫比亞、尼加拉瓜、義大利、中國、捷克、德國、菲律賓；虛構20國含玩家母國**青雲國（A級）**、北原聯邦（S）、赤陽國、藍岸共和國、白熊聯盟、金沙王國、翡翠海聯邦、南嶼群島、烈日邦聯、蒼穹國、極光公國、銀月王國、雪嶺聯邦、風岬國、珊瑚環礁國、曙光合眾國、黑森公國、霧谷國、沙洲聯盟、星港城邦。
- 每國定義：等級、姓名風格（13種`NAME_POOLS`：日/韓/拉丁/英美/斯拉夫/荷/義/德/菲/島嶼/北歐/沙漠/華文）、訓練專長（對應春訓菜單鍵）、風味文案。
- 國家等級影響：①國際市場球員素質（S+6 → D-5的屬性與潛力位移）②春訓費用與專長強度③國際賽事實力基準。
- 本土球員存檔仍記`nationality:"本土"`（相容），顯示層以`nationDisplay()`統一呈現「青雲國」；外籍球員名單與市場加國籍徽章（含國家等級）。

### B. 春訓系統（新常設階段：選秀會之後、開幕之前）
- 流程：選秀完成 → `prepareSpringCamp()` → 春訓畫面（選國家＋逐人菜單）→ `executeSpringCamp()` → 成果報告 → 開幕。主控台在春訓未完成前以「前往春訓」卡取代模擬按鈕。
- 地點：40國任選、**整個球團同行**；母國免費，海外依等級收費（S 4000萬／A 3000萬／B 2000萬／C 1200萬／D 700萬）。
- 操作：1軍與2軍各自分頁，皆有「AI一鍵建議」與「逐人手動下拉」雙模式；投手4項菜單（球速球威/變化控球/體力/抗壓）、野手8項（打擊/長打/**觸擊**/速度盜壘/守備臂力/選球/抗壓/體力），國家專長項目以★標示。
- 成效公式：基礎1~2＋國家專長（S 2~3／A 1~3／B 1~2／C·D 1）＋教練加成≤2＋設施加成≤2＋練習狂特質+1，單項合計≤+5、**不超過潛力天花板**；報告逐人列出「控球 50→55」明細。
- 海外特殊事件（1~2件，好壞約7:3、等級越高好事越多；S/A/B完整8種池、C/D精簡池）：交流賽勝利、媒體報導、追加贊助金、球迷朝聖、**名門友誼**（隔年國際市場獨家名額+1且必有該國菁英）、年輕球員開眼界（潛力+1）／水土不服（狀況↓）、訓練過度（輕傷3~6天）。
- AI球隊簡化春訓：預算>3億時35%機率赴B級以上國家（實際扣款），全員套用建議菜單。

### C. 國際賽事改「季末制」（廢除季中徵召）
- 舊制「4年一度季中徵召、缺陣10~18場」**全面刪除**（`runInternationalCallups`移除、`internationalDutyGamesLeft`載檔清除）。
- 新制「世界棒球錦標賽」：首屆**遊戲第5年（2030年）**、之後每4年（`isIntlYear`）；時間軸＝季後賽冠軍 → 年度頒獎 → **國際賽** → 休賽季。
- 賽制：40國全參賽，8組×5隊分組循環（實力差邏輯機率）→ 各組第1晉8強單淘汰 → 冠軍。母國實力＝聯盟本土最強24人實際能力+5；在本聯盟效力的外籍球員為其母國+2。
- 母國成績效果：奪冠/4強 → 全聯盟人氣+6/+3、國手抗壓+2~3、**隔年贊助與周邊收入×1.15/×1.10**（`S.intlBoost`接入`settleSeasonFinance`）；8強止步 → 國手抗壓+1~2；小組淘汰 → 國手背負罵名，隔年開季6~10場**低迷鎖**（`slumpLockDays`）＋負面新聞。玩家球員入選會列名並推播新聞。

### D. 狀況機制（絕好調～絕不調五級）
- `p.condition ∈ [-2,2]`，表現係數每級±4%（打擊/投球戰力計算皆乘）；每日馬可夫漂移（12%機率、偏向回歸普通；「情緒起伏」特質24%）。
- 受傷連動：低迷×1.3、絕不調×1.6。UI以↑↗→↘↓箭頭呈現於名單/輪值/詳情。快熱型開季自帶好調、慢熱型開季壓在低迷。

### E. 事件系統第一波：新聞跑馬燈＋贊助商任務
- `S.newsFeed`（封頂30則）＋主控台「聯盟快訊」卡（預設5則、可展開15則）：連勝連敗、中重度傷兵、生涯里程碑（100轟/1000安/100勝/1000K/100SV等，`milestonesDone`去重）、國際賽、春訓事件、贊助結果。
- 贊助商任務：每季開季隨機1個（勝場/季後賽/全隊全壘打/全隊盜壘/平均進場率，獎金1000~3000萬），主控台進度條即時顯示，季末於`enterOffseason`結算入帳並寫入休賽季摘要。

### F. 建議轉任教練流程重寫（Mars定案）
- 球員詳情頁固定兩鈕：「**釋出至自由球員市場**」（確認卡→`releaseActivePlayer`直接執行、無拒絕）＋「**建議退休轉任**」（含現任比對、**單次判定**）。
- 婉拒 → `roleOfferDeclinedYear`記錄本季，按鈕鎖住顯示「本休賽季已婉拒過」；同時彈出「繼續留隊／釋出」卡。成功 → 就任並跳回名單。連按洗骰漏洞根除。

### G. 其他修正與新內容
- **選秀潛力校準**（`calibrateProspectToCeiling`）：新秀現況一律＝天花板－8~25點成長空間（屬性±8個體差），並保證潛力≥現況；`ageAdjustRating`加保護：現況≥潛力時顛峰前**維持不衰退**。
- **新秀薪資調降**：年薪上限 S 80萬／A 70萬／B 50萬／C 35萬／D 20萬（元）；簽約金 S 600萬→D 80萬。
- **談約人性化**：期望薪資/年限固定存於球員（`negoDesired`，同季不重擲、取消重開不洗骰）；**出價≥期望必成交**；年限不足可溢價彌補（每少1年+28%）；特質「大物志向」+15%／「重情義」-8%。
- **觸擊屬性**：野手新增`bunting`（生成/成長/春訓/球探評估/選秀顯示），戰力計算佔打線7%權重、關鍵時刻抗壓混入15%；訓練設施新增「觸擊訓練」、選球訓練正名。
- **球員特質系統（12種）**：練習狂/玻璃體質/鋼鐵之軀/大賽型/慢熱型/快熱型/導師/人氣王/大物志向/重情義/觸擊職人/情緒起伏；生成0~2個（55%/35%/10%），連動成長、受傷、關鍵時刻、談約、春訓、狀況。
- **牛棚疲勞**：投手`fatigue 0~100`，先發+32/後援+22，每日恢復10+體力×0.08；表現×(1−疲勞/350)、受傷×(1+疲勞/160)；**疲勞>70的後援自動跳過**改用次順位；名單/輪值顯示疲勞欄（>70紅字）。
- **排序全面雙向**：投手/野手/國際市場排序皆可切換正反向，新增薪資、體力、疲勞、選球、觸擊、守備、國籍等排序鍵。
- 舊存檔相容：`ensureV25()`載入時一次性補齊（特質播種、觸擊推估、狀況/疲勞=0、新聞feed、春訓旗標依進度判定、清除季中徵召殘留）。

### 測試紀錄
Node.js vm沙盒重建：`test_regression.js` 97項斷言全數通過（40國結構、生成/特質分布、成長保護、開局→選秀→春訓→球季→轉任鎖→釋出→談約必成交→跨6年含第5年國際賽→v24舊存檔升級）；`smoke_multiyear.js` 連跑15年（第5/9/13年三屆國際賽如期舉辦、值域/名單/市場國籍健全）＋全部21個畫面渲染測試通過。

### 已知簡化（誠實記錄）
- 國際賽為實力值機率模擬（非逐場box score）；國手僅取聯盟本土前24人、不含旅外虛構球員。
- AI球隊春訓不逐人報告；AI仍不簽國際球員、不升級設施（沿用既有簡化）。
- C/D級國家春訓事件池較簡（設計如此）；季後賽期間狀況不漂移（季後賽以系列賽快速結算）。

## 二十六、本次會議修正（v26：季中訓練指派・硬體三新設施・傷病系統深化）

> 設計主軸：讓「操練 ↔ 風險 ↔ 恢復」形成完整閉環——季中特訓提升戰力但推高受傷風險、傷病深化讓受傷有真實代價（手術抉擇/降評/病史）、宿舍與復健中心提供投資解方。三新設施依Mars定案一律**5級**。

### A. 季中訓練指派（提案2定案實裝）
- 球季進行中可為球員指定**一個**加練項目（球員詳情頁指派/停止；主控台有「季中特訓中」總覽卡；名單球員名旁顯示🏋️）。
- 項目10種：投手＝球速特訓/控球特訓/變化球特訓/體力強化；野手＝打擊特訓/力量強化/選球特訓/跑壘特訓/守備特訓/觸擊特訓（`MID_TRAINING_ITEMS`）。
- 機制：每個比賽日累積訓練點，滿**100點**該項屬性+1（沿用春訓天花板規則：不超潛力、已超過者不降；**單次指派本季最多+3**，達標自動結訓；練到頂天也自動結訓並發新聞）。
- 累積速度＝基礎10點 ×(1+對應訓練設施6%/級) ×(1+對應教練加成) ×練習狂1.25 ×**2軍1.3**（出賽壓力小） ×**30歲以上0.5**。
- 代價（與傷病系統咬合）：受訓中出賽受傷機率×1.15（**玻璃體質受訓×1.3**）；投手每日疲勞恢復**-20%**；另有每日0.15%微量「特訓傷」風險（涵蓋不出賽的2軍受訓者，玻璃×2/鋼鐵×0.6）。受傷一律自動退訓＋新聞。
- 同時指派名額＝**4＋最高訓練設施等級**（`midTrainingSlots`）；休賽季`developPlayer`自動清除；新聞類型「訓練🏋️」。

### B. 硬體三新設施（提案4定案實裝，各5級、僅春訓期間投資）
| 設施 | 費用（萬,Lv0→5） | 效果 |
|---|---|---|
| **選手宿舍** | 900/2700/5400/10800/20000 | 每日狀況漂移偏正向（每級+2%，低檔回升加速1.5倍）；投手每日疲勞恢復**+2/級**；23歲以下年度成長**+2%/級**（走coachBonus通道） |
| **情蒐分析室** | 1000/3000/6000/12000/24000 | 三球探有效精準度**再+1/級**（與球探辦公室+2/級疊加，統一入口`effectiveScoutAccuracy`）；比賽期望得分**+0.05分/級**（雙方各自計算）；Lv.1起主控台「🔍情蒐報告」卡（下一戰對手戰績/連勝敗/主客場，**Lv.3起揭露對手攻投戰力數值**） |
| **復健中心** | 800/2400/4800/9600/18000 | 恢復天數**-8%/級**（與醫療室-6%/級相乘疊加）；保守治療降評機率**-3%/級**（下限5%）；傷病史復發係數削減**8%/級** |
- `FACILITY_TABS`擴為7分頁；AI隊開局隨機0~1級且不再升級（沿用既有簡化）。

### C. 傷病系統深化（未排定項目6提前併入）
- **傷勢部位化**：三級傷勢池全面改為`{n,part}`物件（肩部/手肘/手腕/手指/背部/腹部/大腿/小腿/膝蓋/腳踝），受傷邏輯統一抽出`applyInjury(p,team)`（比賽傷與特訓傷共用）。
- **傷病史**：`p.injuryHistory`（封頂20筆）記錄年度/傷名/部位/等級/天數/治療方針/後遺症，**跨季終生保留**；球員詳情頁顯示傷病史表；中重度病史≥2筆顯示「⚠️傷病體質」標籤（`injuryProneTagHtml`）。
- **舊傷復發**：同部位有中重度病史再傷 → 傷名冠「舊傷復發・」且天數×1.15、病史標🔁。
- **傷病史係數**（`injuryHistFactor`）：受傷機率×min(2, 1+0.12×加權病史)（重傷1.0/手術重傷0.4/中傷0.5/輕傷0.15），復健中心每級削減8%。
- **重傷手術決策**：玩家隊球員重傷 → 主控台出「⚕️治療方針待決定」卡，**決策前恢復凍結**（名單顯示「待決定治療方針」）。🔪手術＝天數×1.4、降評機率僅2%、病史復發權重低；🩹保守＝天數照舊、降評機率25%（復健中心-3%/級）、復發權重高。玻璃體質降評×1.4／鋼鐵之軀×0.5。AI隊即時自動決策（≥50天傾向手術）。
- **傷癒降評**：重傷痊癒時擲骰，中獎則「耐久度」或「部位對應屬性」-1~3（肩/肘→球速或臂力、腕/指→控球或接觸、膝/踝/腿→體力或速度、背/腹→體力或長打；`INJURY_PART_ATTR`），寫入病史「後遺症」欄與新聞；未中獎且為玩家隊 → 「完全康復」好消息新聞。跨季未癒的重傷視同休養成功（不降評）。
- **復健進度條**：主控台傷兵卡與球員詳情顯示復健%（術後標「術後復健」）。

### D. 舊存檔相容與測試
- `ensureV26()`（04定義、06 init於ensureV25後呼叫）：`injuryHistory`補陣列、舊傷勢補中性部位、跨季殘留`midTraining`清除、全隊三新設施lazy補值。
- `test_regression.js`擴充至**129項斷言**（+32：新設施等級/升級/球季擋下/效果數值/精準度疊加、特訓指派/點數/滿點成長/天花板/投野項目互鎖/季上限自動停訓/受傷退訓、傷勢部位化/病史寫入/待決策凍結/手術數值/傷癒降評強制路徑/降評0保護/病史係數封頂與復健削減/AI自動決策、v25舊檔升級模擬），全數通過。
- `smoke_multiyear.js`：15年長跑改為**每季實際指派特訓＋季末補做手術決策＋前5年逐年升滿三新設施**，新增6項v26健全性檢查（病史封頂/待決策僅玩家隊/設施滿級/病史有累積/特訓無跨季殘留）＋新設施三分頁、特訓中詳情、傷病史詳情、手術決策卡等渲染測試，全數通過且連跑5輪穩定。

### 已知簡化（誠實記錄）
- 訓練名額取「最高」訓練設施等級（非逐項對應）；切換特訓項目會重置進度且各項目季上限獨立（理論上可分項各+3，受球季長度自然限制）。
- 情蒐報告只看下一戰對手的總體數值，不含逐球員弱點；AI隊仍不升級設施（含三新設施，開局0~1級固定）。
- 手術/保守只在「重度」傷勢出現；跨季未癒重傷直接視同痊癒且不降評（避免休賽季黑箱懲罰）。

## 二十七、本次會議修正（v27：經紀人個性・AI球團個性・GM信任度與KPI解職）

延續v26，本版聚焦「讓談判桌與交易桌活起來、讓GM有真正的成敗後果」。Mars拍板三大主軸：經紀人與AI球團個性**各7型**、GM信任度歸零＝Game Over含生涯總結；「東山再起」與「代理人事務所」順延至v28。

### A. 經紀人個性系統（7型，01檔`AGENT_TYPES`）
每位球員生成時（`applyTraitOnGen`）配一名專屬經紀人`p.agent={type,name}`，談判桌對面坐的是經紀人。七型與其手段：
| 型 | 手段 |
|---|---|
| **強硬派** | 期望薪資×1.12、年限不足溢價率0.40、談判次數僅4次 |
| **好說話** | 成交機率斜率×1.35、所需門檻×0.97（更容易點頭） |
| **愛錢派** | 期望×1.05、溢價率0.45、機率斜率×1.5（加錢效果放大） |
| **重情派** | 續約期望×0.92；但若曾被本隊釋出（`p.lastTeam===userTeamId`）自由市場期望×1.15（心結） |
| **求穩派** | 期望×0.95、期望年限+1、溢價率0.45（偏好長約、薪資略讓） |
| **投機派** | 期望×1.10、期望年限-2；給超出期望+1年以上的長約時所需×1.06（不願被綁死） |
| **愛名氣** | 期望隨球隊人氣與勝率浮動（clamp 0.9~1.3）：強豪名門可砍價、弱隊要加錢 |
- 每型含成交/拒絕台詞（`quoteAccept`/`quoteReject`），顯示於談判紀錄。
- **鐵則不變**：期望值固定存於`negoDesired`不逐次洗骰；出價≥所需必成交（個性只改「所需」門檻與機率曲線，不改鐵則）。個性效果對新秀/自由/國際/續約全通道適用（`loyal`/`fame`僅非新秀）。
- UI：談判畫面新增經紀人列與說明、紀錄附台詞；球員詳情特質列後接經紀人標籤（`agentTagHtml`）。

### B. AI球團個性檔案（7型，01檔`TEAM_PERSONAS`）＋GM記憶系統
每隊配經營風格`t.persona`（`buildLeague`以洗牌後的`PERSONA_KEYS`輪配20隊，確保7型分布均勻）：
| 型 | 特徵 |
|---|---|
| **重建型** | 估年輕×1.25/老將×0.75/潛力×1.2（愛年輕、肯賣老將） |
| **豪購型** | 估老將×1.15/明星×1.25、簽國際球員機率0.6（最積極） |
| **養成型** | 估年輕×1.3/潛力×1.25、成交門檻1.18（惜售農場） |
| **精算型** | 純數字：帳面≥1.03即成交、無隨機、不看交情（`chanceMult=0`,`affinityW=0`） |
| **保守型** | 門檻1.28、機率×0.5、國際簽援機率0.05（極少交易） |
| **賭性型** | 門檻1.08、機率×1.3、8%機率接受小虧（`lowballChance`） |
| **人情型** | 交情權重×2（好感度對門檻的影響是別隊兩倍） |
- **交易估值**改以「對方球團的眼光」計價（`personaTradeValue`＝`tradeValue`×個性加權：年輕≤25/老將≥30/潛力≥70/明星trueOverall≥65），`evaluateTrade`改為`(give,get,partnerTeamId)`三參數並套用個性門檻`acceptHi`與交情調整`adjRatio=ratio+aff×0.01×affinityW`。
- **GM記憶**`t.gmMemory={affinity(-10~+10),events(封頂10),rejects}`：無償送人+2、成交ratio≥1.3記人情+2、≥1.15+1、侮辱性報價(ratio<0.6)-2、同季反覆被拒每3次-1、被搶簽國際好手-2。每年休賽季`decayGmMemories`好感×0.7取整、`rejects`歸零。
- UI：交易選隊卡顯示個性標籤＋交情標籤；交易編成頂部「對手檔案」卡（個性描述＋交情＋記憶事件簿前5筆）。

### C. GM信任度／KPI系統（04檔）＋解職Game Over
- `S.gmCareer={trust:50,startYear,seasons[],championships,fired,firedYear}`。
- 每季開幕（`beginFirstSeason`/`finalizeNewSeason`）`generateSeasonKPI`依`teamStrengthRank`（1軍平均trueOverall全聯盟排名）開2目標（1成績＋1經營）：rank≤4→奪冠/冠軍賽/季後賽；≤12→季後賽/勝率5成/分區前2；其餘→勝率4成5/55勝/黑馬季後賽。經營類：不赤字/人氣+3/季末1軍≥3名23歲以下。目標帶難度`diff`。
- `settleSeasonKPI`在`enterOffseason`財務結算後呼叫（赤字目標需看結算後預算）：達成+7/+10/+13、未達-11/-8/-5（越難的目標沒達成扣越少），奪冠另+8。信任clamp 0~100，**歸零→`fired`→`UI.screen="gameOver"`**。
- `renderGameOver`生涯總結：執掌年數/生涯戰績/季後賽次數/冠軍數/逐年考核表，「重新開始」呼叫`resetGame()`。`render()`頂部守衛：`fired`後鎖定GameOver（僅setup/teamSelect例外）。
- 主控台`renderKpiCard`：信任條（good/mid/bad三色）＋各目標即時進度（`kpiProgress`）＋信任<30警語；休賽季摘要頂部年度考核卡。新聞新增「高層🏛️」類型。

### D. 待辦補完：AI依球團個性簽國際球員
- `aiSignInternationalPlayers`（`enterOffseason`內、`decayGmMemories`後執行）：每休賽季全聯盟≤3人，依`persona.intlChance`決定積極度，不碰玩家獨家人選、不破外援上限`FOREIGN_ROSTER_CAP`、預算需≥薪資×2。**1軍滿編時釋出最弱本土球員騰位**（洋將須比最弱者trueOverall+5才換血，被釋出者進自由市場讓市場更活）；AI簽的洋將合約3~4年。

### E. 舊存檔相容與測試
- `ensureV27()`（04定義、06 init於`ensureV26`後呼叫）：`gmCareer`補值、各隊`persona`（依隊序輪配）與`gmMemory`補值、三池球員補發`agent`、進行中存檔補生成本季KPI。升級鏈：`ensureV25→ensureV26→ensureV27`。
- `test_regression.js`擴充至**160項斷言**（+31：經紀人7型/期望差異/年限偏好/次數/鐵則不變；球團7型/估值加權/記憶封頂與clamp/侮辱報價扣好感/精算型確定性/淡化/AI不碰獨家與外援上限；KPI 2目標/即時進度/結算更新信任/不重複結算/歸零解職/GameOver渲染；v27舊檔升級四項），全數通過。
- `smoke_multiyear.js`：15年長跑新增v27健全性（經紀人有效/個性記憶值域與封頂/15年考核紀錄齊全/信任值域/AI累計簽下foreign/外援上限）＋解職時GameOver渲染驗證後復活續跑（信任重置50）＋談判經紀人列/交易對手檔案卡/GameOver渲染測試。**連跑10輪穩定全過**。

### 已知簡化（誠實記錄）
- AI國際簽援每季≤3人、換血門檻固定+5、洋將估值僅取最強候選（非逐隊需求配對）；AI隊之間不互相交易（沿用既有簡化）。
- GM記憶事件種類有限（送人/划算成交/侮辱報價/反覆騷擾/被搶簽），不含比賽結果或言論等更細緻情境；好感度僅影響交易門檻，暫不影響AI開價或談判態度。
- KPI目標池固定（每級各3選1），未依對手陣容或市場動態客製；「愛名氣」經紀人的浮動只看本隊人氣與勝率，不含聯盟聲望或歷史戰績。
- 解職為純Game Over；「東山再起」（被解職後轉任他隊或降級重來）與「代理人事務所」（主動聯繫經紀人、談約前情蒐）順延v28。

## 二十八、本次會議修正（v28：東山再起・代理人事務所）

延續v27，本版把「解職」從純Game Over升級為可延續的職業生涯，並為經紀人系統補上談約前的情蒐與人脈經營。Mars拍板：東山再起提供「結束遊戲／接受招募」二擇一；生涯數據跨球團完整累積且履歷影響新東家開的信任；代理人事務所基礎（情蒐）與進階（關係經營）都做。

### A. 東山再起與生涯延續（04＋05檔）
- **生涯段落**：`gmCareer`新增`stints[]`（每段執掌：隊名/起訖年/戰績/冠軍）與`teamName`。`archiveCurrentStint()`在解職或轉隊時把當前`seasons`封存進`stints`並清空當前段（但`championships`總冠軍數不歸零）。`careerAllSeasons()`彙整所有段落＋當前段供跨球團統計。
- **業界聲望**`careerReputation()`（0~100）：基準50＋生涯勝率(±40)＋冠軍(每座+12)＋季後賽(每次+3)。決定邀約數量、可及隊伍強度、新東家起始信任。
- **聘僱邀約**`generateJobOffers()`：解職後依聲望產生1~3份邀約（rep≥70→3份/可及中上游12隊、≥40→2份/8隊、否則1份/最弱5隊）。每份含`startTrust`（基準40＋聲望加成＋弱隊求才加成，clamp 20~75）。弱隊優先（依`teamStrengthRank`由弱到強取池）。
- **接手**`takeJobOffer(teamId)`：封存前段→卸下舊隊`isUser`→接手新隊→信任重置為該offer的`startTrust`、`fired`解除、`startYear`設為隔年→產生新東家KPI→進入其休賽季摘要（含`rehired`歡迎卡）。
- **UI**：`renderGameOver`改版顯示跨球團生涯總結＋執教履歷＋業界聲望＋聘僱邀約卡（「接受」按鈕）與「結束遊戲」按鈕；休賽季摘要頂部東山再起歡迎卡（`.rehirecard`）。

### B. 代理人事務所（01＋02＋05檔）
- **資料**`S.agency={scouted:{playerId:{year}}, rel:{agentType:affinity(-10~10)}}`。
- **情蒐（基礎）**：談判畫面「委託情蒐」按鈕→`scoutAgentInNegotiation()`花費`agentScoutCost`（球員身價6%，clamp 80萬~1200萬）＋佔當季紀錄→揭露經紀人性格全文與期望底線（薪資/年限/溢價率）。`isAgentScouted`同季有效、跨季失效。
- **關係經營（進階）**：`recordAgentRel(type,delta)`——成交該類經紀人+1、談崩-1（僅玩家隊計入，封頂±10）。`agentRelPerks(type)`：正關係→門檻每點-1.2%（`requiredMult`再乘）、機率斜率每點+4%；負關係反向懲罰（較輕）。接進`submitNegotiationOffer`。
- **UI**：主控台「🕵️ 代理人事務所」入口→`renderAgency`（七型經紀人人脈網＋各自優惠幅度＋本季已情蒐名單）；談判畫面情蒐卡（未探→委託按鈕；已探→揭露報告；恆顯示與該型經紀人的交情）。

### C. 舊存檔相容與測試
- `ensureV28()`（04定義、06 init於`ensureV27`後呼叫）：`gmCareer.stints/teamName`補值、`agency`補值、`jobOffers`補null。升級鏈：`ensureV25→26→27→28`。
- `test_regression.js`擴充至**184項**（+24：聲望值域/彙整、解職產生邀約/邀約合理性、接手換隊/信任重置/前段封存/冠軍保留/新段歸零/進休賽季、關係優惠方向/情蒐標記當季有效跨季失效/花費區間/成交升關係、v28三項升級）。連跑12次全過。
- `smoke_multiyear.js`：15年長跑的解職處理改為走**真實東山再起流程**（產生邀約→接手→驗證換隊/冠軍保留/前段封存），考核紀錄斷言改用`careerAllSeasons()`跨球團累計；新增v28健全性（事務所結構/關係值域/生涯段落結構/聲望值域）＋agency畫面渲染；設施升滿斷言改為條件式（換隊後改驗值域）。連跑15輪全過。
- 整合版沙盒抽驗（開新局50天＋事務所/情蒐/東山再起全流程）全過。

### 已知簡化（誠實記錄）
- 東山再起接手新東家的「現有陣容」即該AI隊當下名單，不另做補強或談判緩衝期；邀約隊伍以戰力弱者優先，未考慮地緣、風格契合或球隊財務。
- 新東家起始信任僅由聲望與隊伍強弱推算，未反映該隊高層個性或歷史耐心；轉隊後與原球團球員/經紀人的既有關係一併沿用（不因換隊重置）。
- 情蒐揭露的是「當下」期望底線，若跨季重談需重新情蒐；經紀人關係為全域（同型經紀人共享一個好感值），非逐一經紀人個體。
- 關係優惠僅影響談約門檻與機率，暫不影響經紀人初始開價或AI球隊簽人行為。
- 附帶調整：AI國際簽援的預算門檻由「預算≥薪資×2」放寬為「×1.5」（v27原設定偏保守，長期運轉下AI簽援偶爾整季停擺；放寬後AI國際市場明顯更活躍，且不影響外援上限與獨家保護）。

## 二十九、本次會議修正（v29：體驗修補版——26版問題清單中小項＋新聞跑馬燈）

> 目標：一次清掉 Mars「目前至26版的問題」清單中的九個中小型項目，加上待辦第一梯的新聞動畫。純體驗/互動性/資訊透明修補，不動大架構。（球場格位＋主客場已定案為 v30 大系統版；教練球探續約與自主訓練傳承定案為 v31。）

### A. 手術治療費用化（03＋05檔）
- **`surgeryCostFor(p, team)`**：費用＝300萬＋原始缺陣天數×15萬（以`injury.baseDays`＝手術延長「前」的天數計價，避免手術延長40%天數反而墊高帳單），醫療室每級-5%（Lv5=-25%），取整到10萬。
- **`decideSurgeryFor`**：選手術時實際從球隊預算扣款（記錄於`injury.surgeryCost`）；**全聯盟含AI都要付費**。
- **玩家端互動**：主控台決策卡顯示手術費、醫療室折抵、目前預算；預算不足時手術按鈕禁用＋flash提示（保守治療永遠可選、不會卡死）。flash與新聞都帶費用資訊。
- **AI端**：自動決策改為「缺陣≥50天 **且** 預算≥手術費＋2億緩衝」才開刀——只有真正有錢的球團捨得開刀，中小球團一律保守治療（緩衝保護AI的國際簽援與運營預算，實測AI經濟與v28等價）。

### B. 先發棒次調換（06檔）
- **`moveLineupSlot(idx, dir)`**：把第idx棒與相鄰棒次「整槽互換」（球員＋守位綁定移動），不打亂守位配置、不觸發守位重複自動互換。打線表每列新增 ▲▼ 按鈕（首尾禁用），與既有「更換」按鈕並列。

### C. 轉播／贊助合約重製（02檔）
- **方案結構**：由「三檔定額」改為真實機制差異——`{base保證金, winBonusPer10戰績分潤, playoffBonus季後賽加碼}`。轉播：保守＝行情×1.05定額／標準＝×0.92＋小分潤／積極＝×0.72＋大分潤＋加碼。贊助：×1.02定額／×0.90＋中分潤／×0.68＋最大分潤與加碼。
- **`dealSeasonRevenue(deal, winPct, madePlayoffs)`**：保證金＋(勝率-50%)/10%×分潤＋季後賽加碼，下限0；**傳入數字時視為舊存檔定額合約**（向下相容）。季末結算與賽季中損益預測都改走此函式（預測以「目前勝率打完、未進季後賽」估算，浮動條款即時反映）。
- **UI互動化**：每方案一張卡——條款說明＋保證金/分潤/加碼三行＋**三情境試算**（勝率45%無季後賽／55%無季後賽／65%＋季後賽），已簽方案金框標示（`.dealchosen`）。

### D. 行銷企劃重製（02檔）
- 由「三選一大方案」改為**七項活動自由複選**（`MARKETING_CAMPAIGNS`）：社群經營300萬／電視網路廣告800萬／明星代言1500萬（隊上有「人氣王」特質再+2人氣）／周邊商品開發600萬／跨界聯名1200萬／主題日活動500萬／家庭日900萬。
- **效果三軸**：popBoost（季末人氣成長）、merchPct（周邊收入%）、**attPct（新軸：主場進場率%，接進`teamAttendanceRate`）**。`toggleMarketingCampaign`點選投入/再點取消全額退費（僅春訓期間）；`recomputeMarketingEffects`重算三軸加總。
- UI：面板顯示已投入活動數/總花費/三軸加成合計；每活動一張卡（費用＋說明＋效果明細）。

### E. 談約資訊完整化（01＋02檔）
- **`growthPhaseLabel(p)`**（01）：依年齡vs隱藏顛峰年齡標示 成長期（<peak-1）／巔峰期（±1）／衰退期，附說明文字與色彩class。
- **`p.lastSeasonStats`**：`finalizeNewSeason`歸零前快照上季成績（含year，僅有出賽者）。
- **`negotiationScoutBlock(p, neg)`**：談約畫面新增「球探完整報告」卡——續約顯示真實能力格＋綜合現況評等；FA/國際顯示**預測天花板**（`scoutCeilingReport`：依有效精準度霧化、同季快取不重擲、標註精準度）；新秀沿用scouted評估值＋現況/天花板徽章＋熟成度。下方帶生涯階段行＋本季/上季成績行（`negoStatLine`：投手 場次勝敗防禦率K局數／打者 場次打擊率轟打點盜）。

### F. 選秀談約權收緊（02檔）
- `rookieDesired`等級倍率由 S1.15~D0.85 改為 **S1.0~D0.85（全部≤1）**——新秀期望絕不超過`ROOKIE_GRADE_SALARY_CAP`定額上限。
- `startNegotiation`在特質（大物志向+15%）與經紀人加成後**再夾一次上限**；`submitNegotiationOffer`出價超額自動壓回；UI薪資輸入帶max、上限說明卡（「只能往下談、砍太兇談崩5次直接放棄加盟」）。

### G. 國內球探獨家新秀（01＋02＋06檔）
- **`exclusiveDraftSlots(team)`**：國內球探有效精準度63/75/87→每屆1/2/3位獨家名額（門檻比國際獨家高、名額少）。
- **`generateExclusiveDraftProspects`**：等級偏高（S 30%／A 45%／B 25%）、18~22歲、`exclusive=true`，於`startDraft`時**額外加進**選秀池——不佔公開池S/A級固定配額（Mars指定：發現S級獨家不影響池內S級數量）。
- **`aiDraftPick`**：跳過`exclusive`標記（AI接觸不到玩家球探的人脈）。選秀畫面帶「獨家情報」徽章（沿用國際市場的`.exclusivetag`樣式）；球探辦公室面板新增「國內選秀獨家名額」行、升級說明補國內線。

### H. 春訓限制與資訊（02＋05檔）
- **海外限B級以上**（Mars指定）：畫面國家下拉僅列S/A/B（母國青雲國為A級本就在列）；`setSpringNation`後端防呆——C/D級被擋並flash說明。
- **菜單顯示數據**：球員表改為 綜合/天花板 欄＋能力現況欄（投手：速控體壓／野手：打長選速守觸）＋每個訓練選項標示「（現X）」與「已達頂」——弱點與練滿項一眼可辨。
- **特性連動提升**（Mars指定「其他能力也會依特性相對提升」）：主練之外——練習狂必得1項連動+1；≤23歲60%機率、其他30%機率再得1項；觸擊職人偏好觸擊、導師/大賽型偏好抗壓，其餘隨機（僅挑未達天花板的非主練屬性，同受potential封頂）。既有主練連動（球種威力/對左右/盜壘/臂力）全部改為**記錄到成果報告**（`linked`標記）；報告畫面區分「(連動)」與「🔗(特性連動)」並附圖例說明。

### I. 新聞真・跑馬燈（05檔＋CSS）
- 快訊卡頂部新增水平無縫循環跑馬燈：最新8則串接、內容複製兩份做無限循環（`translateX(-50%)`關鍵幀）、時長依總字數自適應（18~90秒）、懸停/觸摸暫停、`prefers-reduced-motion`時停用動畫。下方原有列表與展開按鈕保留。

### J. 舊存檔相容與測試
- **`ensureV29()`**（04定義、06 init於`ensureV28`後呼叫）：行銷舊三方案→新活動組合對應（`LEGACY_MARKETING_MAP`，費用已付不重扣、效果重算）＋`marketingAttPct`補值；當季舊格式offers（含`.amount`）→重生成新方案物件；**已簽的舊純數字合約保留為定額**（結算端相容）；`lastSeasonStats`補null。
- `test_regression.js`擴充至**240項**（+56：手術扣款/記錄/baseDays計價/保守免費/醫療折扣、棒次互換/復原/邊界、方案結構/浮動方向/強隊積極賺爛隊賠/定額相容/簽約存物件、行銷複選/扣退費/三軸/進場率、新秀上限帶入/特質壓不破/出價壓回、獨家名額門檻/標記/AI跳過、C/D擋下B可選/練習狂必連動/不破天花板、成長階段三段/天花板同季固定/上季快照/成績行格式、v29五項升級）。**連跑15次全過**。
- **既有測試抖動根治**（v28基準也存在）：①解職三連斷言——玩家隊在隨機模擬恰好奪冠時champion目標意外達成（+13+8信任）不會解職→測試前固定冠軍為他隊；②傷癒降評強制中獎——屬性已在20下限時扣值不可觀測→測試前抬至60。
- `smoke_multiyear.js`兩處強化：①AI簽援觀測型斷言加**機制型後備驗證**——15年間全聯盟恰好只有低簽援意願個性的球團有錢時（機率約3~5%、v27起即存在的邊界情境）觀測會是0，此時強制給一支AI隊豪購個性＋充足預算＋清出名單位驗證機制本身；②名單健全檢查排除「玩家曾執掌的前東家」（東山再起後前東家帶著測試情境的萎縮名單變回AI隊）。**連跑24輪全過**。
- 整合版沙盒抽驗（開新局＋九項v29流程逐一操作）全過。

### 已知簡化（誠實記錄）
- 手術費用不分傷勢部位與名醫等級，僅按天數線性計價；AI的2億緩衝是平衡參數（保護AI簽援預算），非現實映射。
- 轉播/贊助的「戰績分潤」以最終勝率線性結算，未逐場計算收視波動；季後賽加碼不分輪次深度。
- 行銷活動效果為當季線性加總，無邊際遞減、活動之間無綜效/互斥。
- 獨家新秀的球探評估仍走一般精準度霧化（獨家≠看得更準，只是別人看不到）；AI球團沒有自己的獨家新秀（僅玩家享有，屬玩家側優勢設計）。
- 特性連動提升每次至多2項、各+1，不受國家專長加成影響。

---

## 三十、本次會議修正（v30：球場經濟與主客場版——單一大系統）

> 目標：把「球場」從單一等級數字，升級為完整的**格位設施經濟＋主客場賽制**。周邊收入與實際進場人次掛鉤、球場等級決定格位數、12種設施可自選重複建造、主客場逐場實結門票並加入客場贏球分潤，AI球團設施升級一次到位（隔離發展金，不破壞既有簽援經濟）。承v29交接路線拍板，v31為人事與傳承版。

### 本版拍板（Mars 五點確認）
1. **格位表**：Lv1~7＝3/4/5/6/8/10，巨蛋為 **12＋地標獎勵3＝15格**。
2. **類型池12種**照案（六指定＋六補提）。
3. **主場優勢**＝主隊期望得分 +0.15分（基準4.5分的約+3%），常數化`HOME_ADVANTAGE_RUNS`。
4. **設施可重複建造**同類（效果線性疊加，博物館人氣加成除外），每座**年維護費＝建設費×8~12%**。
5. **客場贏球分潤**：客隊贏球抽該場主場門票**8%**、輸球**2%**（由主隊支付）。

### A. 球場等級表重構（02檔 `FACILITY_LEVELS`）
- **`merchMult`廢除**（周邊改由格位設施＋實際人次決定）；等級改帶`slots`（格位數）。
- 容量微調：15000/20000/25000/30000/37000/44000/50000；格位：3/4/5/6/8/10/15。

### B. 設施類型池（02檔 `STADIUM_FACILITY_TYPES`，12種）
- 三軸效果：**spendPct**＝人均消費%（消費/體驗類）、**attPct**＝主場進場率%（舒適類，直接進`teamAttendanceRate`）、**popBoost**＝季末人氣成長（博物館專屬，同類多座不疊加）。
- 清單（建設費／維護率／主效果）：販賣部🛒(4000萬/10%/消費+18%)、小吃街🍜(5000萬/10%/消費+22%)、飲料吧🥤(3000萬/9%/消費+14%)、廁所擴建🚻(3500萬/8%/進場+1.5%)、哺乳室🍼(3000萬/8%/進場+1%)、VIP包廂💺(2億/12%/消費+45%)、大型計分螢幕📺(8000萬/10%/消費+15%進場+1%)、兒童遊樂區🎠(5000萬/9%/消費+8%進場+1.5%)、球隊旗艦店🧢(1.5億/11%/消費+40%)、立體停車場🅿️(1.2億/8%/進場+3%)、球隊博物館🏛️(1億/10%/消費+12%人氣+1)、啤酒花園🍺(6000萬/11%/消費+25%)。
- 可重複建造；拆除退回建設費**40%**（`STADIUM_DEMOLISH_REFUND`）。均為一次性建設費＋年維護費，無其他隱藏成本。

### C. 周邊收入重構（02檔 `stadiumPerCapitaSpend`／`settleSeasonFinance`）
- 新公式：**主場總進場人次 × 人均消費**。
- 人均消費＝`(MERCH_BASE_SPEND 12 ＋ 人氣×0.06) ×(1＋設施spendPct總和)×(1＋行銷merchPct)× 國際賽merchMult`。
- 校準：空格位下人均約12~18元、接近改制前水準；蓋滿消費類後人均可破50元，投資有感但不通膨（10年長跑AI營運預算未被此重構推向異常）。

### D. 主客場賽制（03檔）
- **主場優勢常數化**：`HOME_ADVANTAGE_RUNS=0.15`（原為simulateGame內硬編碼0.15，本版正式命名並註記＝基準+3%）；同分52%偏主隊的再見分機制保留。
- **逐場實結**（`applyGateEconomy`，每日模擬每場呼叫）：依當下進場率結算該場門票、記錄主客戰績（`homeWins/homeLosses/awayWins/awayLosses`）、計算客場贏球分潤（`GATE_SHARE_WIN 0.08`／`GATE_SHARE_LOSE 0.02`）。
- **主場帳ledger**（`team.finance.gateLedger`）：逐場累計`homeGames/visitors/gateRevenue/shareIn/sharePaid`；季末`settleSeasonFinance`採實帳（舊檔季中升級者按實際主場數等比例外插補全）；換季`resetHomeAwayLedger`歸零。
- **賽程平衡修正**（`buildSeasonSchedule`）：原循環賽每個循環主客分配固定，導致部分球隊主場數極端偏低（v29前財務假設對半故無妨，v30實結後暴露）→**偶數循環對調主客**，每隊主場數從0~極端值收斂至61~65場（實測19隊×126場）。
- **面板**：主控台記分板新增「主X勝X敗・客X勝X敗」；財務報表門票列標主場場次、加列客場分潤收入/支付客隊分潤/設施維護費；上季報表附註帶主客戰績。

### E. AI球團設施投資（02檔 `runAiFacilityInvestments`，春訓期執行）
- **關鍵設計——隔離設施發展金**：實測AI營運預算長期接近收支平衡（10年中位數趨近0甚至小赤字），若從`finance.budget`扣設施費會排擠簽援/補強、破壞v27~v29平衡。故AI設施投資走**獨立額度`team.facility.fund`**，每年注資`AI_FACILITY_FUND_BASE 4億 × 個性倍率`（豪購/賭性1.3、精算1.1、重建/養成/人情1.0、保守0.7），**完全不動`finance.budget`**。
- 每年最多升1級（fund≥升級費）＋建2座（fund≥建設費）；選型依個性：豪購/賭性→消費類、保守/養成→舒適類、精算→CP值(spendPct/cost)最高消費設施、其餘混合。
- 實測10年後AI平均球場Lv6.8+、平均14格，且AI外籍簽約數（16~19）與名單健全度維持v29水準——經濟中立性達成。

### F. 舊存檔相容（04檔 `ensureV30`，06 init於`ensureV29`後呼叫）
- 格位陣列`stadiumSlots`／主客戰績／主場帳ledger／AI設施金`fund`補值。
- **merchMult廢除補償**：舊檔球場等級≥2者（玩家與AI一視同仁），按「每級補發1座基礎設施」（順序：販賣部→飲料吧→小吃街→廁所擴建→大型計分螢幕→啤酒花園，巨蛋封頂6座）換算過去為周邊倍率付出的升級費，避免改制後周邊收入斷崖。僅在尚無`stadiumSlots`欄位時執行一次，重跑不重複補發。升級鏈 ensureV25→26→27→28→29→30。

### G. 測試
- `test_regression.js`擴充至**293項**（+53：格位表/巨蛋15格/merchMult廢除、12種類型池/維護率區間/正效果、效果線性疊加/博物館不疊加/人均公式/舒適拉抬進場率、建造扣款/重複建造/格位滿鎖/預算不足鎖/開季鎖/拆除退款、主場優勢常數/單場門票帳/分潤8%2%/收支對帳/主客戰績/逐場累計/換季重置、結算實帳/分潤列/維護費/周邊人次公式/總支出/季中外插、ensureV30換算補發/不重複/巨蛋封頂/Lv1不補、AI隔離金不動營運預算/升級/建造/跳過玩家/個性倍率守恆/精算CP值選型）。**連跑16次全過**。
- `smoke_multiyear.js`新增v30健全性6項：主客戰績加總＝總戰績、分潤全聯盟收支守恆、全隊主場帳累計中、多數AI球團投資設施、無球團超建格位、季末報表含v30欄位。**連跑12次全過**。
- 整合版沙盒抽驗12項（12種設施/巨蛋15格/建造/主場帳/AI投資/ensureV30/主場優勢常數＋建造設施/主場帳累計/財務/球場/主控台三畫面渲染）全過。

### 本次根治的既有問題（非v30新增，但v30實結後才暴露）
- **賽程主客場極端不均**：`buildSeasonSchedule`循環賽固定主客分配，某些球隊整季主場數趨近0。v29前財務假設對半故隱藏，v30逐場實結後門票收入會嚴重失真→偶數循環對調主客修正（見D）。

### 已知簡化（誠實記錄）
- 人均消費與設施spendPct為線性加總，無邊際遞減、設施間無綜效/互斥；VIP/旗艦店的高消費未區分客層容量上限。
- 主場優勢為全聯盟統一+3%，未依球場規模/氣候/主場氛圍differentiate。
- 客場分潤按當場門票固定比例，未反映對戰吸引力（強隊客場帶動票房）；分潤僅門票、未含周邊。
- AI設施發展金為隔離平衡參數（保護簽援經濟），非現實映射；AI選型依個性偏好但不做ROI最適化（除精算型）。
- 設施維護費為建設費固定比例，不隨使用率/通膨變動；拆除退款率固定40%不折舊。

## 三十一、本次會議修正（v31：人事與傳承版——教練球探續約談判、季後自主訓練/傳承，含三項球場經濟平衡修正）

> 目標：承 v30 交接路線，落地「人事與傳承版」。主體為 v31-A 教練/球探到期續約談判（不許暫代、空缺歸零）與 v31-B 季後自主訓練/傳承（老將傳授後天絕技給後輩）。同時依本次設計會議定案，對 v30 球場經濟做三項平衡修正（維護費、AI 建設速度、折舊/重建循環＋聯盟均衡稅）。全程繁體中文、六模組歸位、ASCII 檔名、寫 `ensureV31` 串入升級鏈。

### 摘要更正（承 v30）
- v30 摘要「聯盟 19 隊」為誤植，實際為**聯盟 20 隊**（4 分區×5 隊，含玩家隊；19 支為 AI 球團），例行賽 126 場，每隊主場 61~65 場（以 `buildLeague` 實測確認）。

### A. 平衡修正①：球場設施維護費（`02-finance.js`）
- 頂級收益設施課重稅作為豪門懲罰：**VIP包廂 0.12→0.20、球隊旗艦店 0.11→0.20**。其餘 10 種維持 8~12%（收益型偏高、舒適型偏低）。

### B. 平衡修正②：AI 建設速度放慢（建設期）
- `AI_FACILITY_FUND_BASE`：4億→**1.4億**；倍率改 `{splash:1.55, gambler:1.55, analytics:1.2, rebuild:1.05, farm:1.05, human:1.0, conservative:0.95}`。
- `runAiFacilityInvestments`：每年**建造上限 2→1 座**，升級維持 1 級/年。
- 沙盒驗證（120 年）：豪門 y10≈73%（≤80）、節省 y20≈92%。

### C. 平衡修正③：折舊/重建循環 ＋ 聯盟均衡稅（含玩家對稱）
- **折舊**：每座格位設施以平行陣列 `facility.slotBuilt` 記建成年（與 `stadiumSlots` 同索引對齊，非改物件結構→保留既有以字串key為主的所有邏輯與回歸相容）。壽命 `STADIUM_LIFE=30` 年；屋齡達壽命→「⚠老舊」：三軸效果（spendPct/attPct/popBoost）**×0.5**、維護費照收（懲罰）。**重建費＝建設費×0.6**（`STADIUM_REBUILD_COST`），屋齡歸零、效果恢復；球場等級不折舊；每隊每年重建上限 1 座（`facility.rebuiltYear` 把關）。
- **完備度** `stadiumCompleteness`：0.5×(等級-1)/6 + 0.5×有效格位/15（老舊格位以 0.5 計入有效格位）。
- **AI 蓋滿後**：年度發展金由建設模式切「翻新模式」＝**1300萬×個性倍率**（`AI_RENOVATION_BASE`，沙盒調校鎖定）；`facility.fund` 囤積上限 **8億**（`AI_FACILITY_FUND_CAP`，>6.8億最貴升級）；AI 每年最多重建 1 座老舊（優先最貴者）。
- **聯盟均衡稅**（`runLeagueBalanceTax`，季末結算前執行）：
  - AI：完備度≥95% 且發展金>1.5億 → 課走超過部分 **50%** 匯入均衡池（只動隔離發展金）。
  - 玩家（對稱但溫和）：完備度≥90% 且營運預算>6億 → 繳 (預算−6億)×**12%**、單年上限 **2億**，匯入同一均衡池（由財報折入 net，不暗扣）。
  - 均衡池平分給完備度**最低 4 支**球隊（AI進發展金／玩家進營運預算）。
  - 財務報表新增「聯盟均衡稅」收/支列，休賽季摘要明列。
- **沙盒調校（19 支 AI・120 年）**：建設期豪門 y10≈73%、節省 y20≈92%；長期（y50~120）完備度 min≈88~91%、max≈98~100%、**Δ≈8~10%**，弱隊不崩、高端不永久凍結 100%——中等分化達標。

### D. v31-A：教練／球探到期續約談判
- 球探沿用既有 `contractYears`（randInt 1~4）；`processScoutContracts` 新增逐年遞減。教練 `processCoachContracts` 改寫：**玩家隊到期不再自動暫代**，改進入休賽季續約佇列 `S.pendingStaffRenewals`；AI 隊維持自動補人。
- **staffRenewal 談判通道**（複用 `startNegotiation`）：`startStaffRenewal`/`submitStaffOffer`/`finalizeStaffRenewal`/`failStaffRenewal`，沿用「出價≥期望必成交、短約溢價（每少1年+25%）、期望固定不重擲」。談判畫面 `renderStaffNegotiation` 附**現任 vs 市場 3~5 名可簽人選數據比較**。
- **不許暫代**：不續約或談判破局→該職位空缺（`staffVacancies`）：教練該項加成、球探該區精準度**歸零**（`specificCoachBonus` 該類回傳 0；球探設 null 回退盲評基準 50）。玩家須到「教練團／球探」畫面自由市場**簽人補上**（`hireCoachCandidate`/`replaceScout` 清除空缺旗標、球探簽約金依能力）。
- 流程：合約續約完成→`proceedToStaffOrDraft`→staffRenewal 畫面（逐一處理）→選秀。便利函式 `autoRenewAllStaff` 供快速流程/測試。

### E. v31-B：季後自主訓練／傳承
- 時機：休賽季（獎項/國際賽後）新增獨立「自主訓練窗」小階段（`runSelfTrainingWindow`，於 `finalizeNewSeason` 執行、`renderSelfTraining` 報告後前往春訓）。
- **自主訓練**：全員依生涯型態小幅成長；有機率獲得新**後天特質**（`ACQUIRED_TRAITS`：練習狂/大賽型/導師/觸擊職人）；標竿老將（≥28歲、招牌屬性突出）有機率**領悟稱號/特殊技**（`LEGACY_SKILLS` 9 種，取得給一次性屬性提升）。
- **傳承**（`runTeamInheritance`）：資深老將（≥32歲且持有可傳承項目）**自動配對**隊內最年輕高潛力後輩（≤23歲、潛力最高），機率把後天特質/稱號傳給後輩並產生新聞。規則：僅後天型可傳（先天如鐵人/玻璃體質不傳）；每隊每年至多 1 次成功；後輩疊加上限 **3 項**。稱號/特殊技/型態可疊加累積。

### F. 舊存檔相容（`ensureV31`，06 init 於 ensureV30 後呼叫）
- 補球場屋齡平行陣列 `slotBuilt`（既有設施視為當年落成、逐年自然老化）、均衡稅欄位、教練/球探空缺旗標、`pendingStaffRenewals`；為既有球探補 `contractYears`；為既有球員補 `specialSkills`。升級鏈：ensureV25→…→30→31。

### G. 測試與驗收
- 回歸測試 **328 項**（v30 為 294；新增 v31 專項 34 項：折舊/重建/均衡稅、教練球探續約與空缺、自主訓練/傳承）**連跑 5 次全過**。
- 15 年 smoke 長跑＋全畫面渲染（新增 staffRenewal/staffNegotiation/selfTraining/球場折舊 4 項渲染）**連跑 4 次全過**。
- 平衡③以獨立沙盒模擬 120 年鎖定翻新/稅率數字（見 C）。
- 整合單檔 HTML 沙盒抽驗 9 項全過。

### H. 已知簡化（誠實記錄）
- 折舊為硬門檻（屋齡達 30 年瞬間老舊、效果直接×0.5），非連續遞減。
- 均衡稅門檻/稅率/補貼隊數為沙盒調校之平衡參數，非現實映射；玩家稅僅於完備度＋預算雙門檻觸發。
- 自主訓練成長與傳承機率為固定平衡值；傳承採自動配對、不開放玩家指定對象；特殊技的屬性提升為取得時一次性套用。
- 教練/球探自由市場「補人」的球探端以重新生成＋依能力簽約金呈現（3~5 名並列比較集中於續約談判畫面）。

## 三十二、本次會議修正（v32：聯盟生態版——AI互相交易、風聲情報與反應措施、AI主動提案、KPI動態化）

### A. 需求與拍板（Mars 決策）
- 第二梯剩餘路線開工：AI 互相交易、好感影響開價、KPI 動態化。
- **交易時程**：改為單一連續窗口——**選秀會結束後開放，至「季後賽前一個月」截止**（例行賽最後 30 個比賽日鎖死；玩家與 AI 同窗口）。
- 活躍度中等（每年撮合嘗試上限 6 組）；**每隊每年 AI 交易成交上限 5 筆**；風聲提前 **5~7 天**；AI 可依個性與隊形**主動向玩家提案**；KPI 季中修訂**降標／加碼／止血三者皆做，降標由玩家選擇接不接**。
- 風聲反應措施三選一（Mars 核准照提案執行）：①插隊搶人 ②慫恿破局 ③靜觀其變。

### B. v32-A：AI 互相交易（01-data-engine）
- **窗口**：`tradeDeadlineDay()` 改為 `schedule.length − 30`（`TRADE_DEADLINE_BUFFER_DAYS`）；`tradeWindowOpen()` 休賽季須 `S.draftDoneYear === S.seasonYear`（`finishDraft` 設旗標）。
- **狀態容器** `S.aiTrade`（`ensureAiTradeState`，依年份自動重置）：`counts`（每隊成交數）、`rumors`、`attempts`、`proposal`、`proposalCount`。
- **姿態** `aiTradeStance`：rebuild/farm 或戰力排名≥16 → 賣；splash/gambler 或排名≤5 → 買；analytics → 價值型；其餘持有。
- **撮合** `tryMatchAiTrade`：賣方出「29 歲以上、能力≥55 老將」，買方以 1~2 名「年輕（≤26）或高潛（≥65）」籌碼（跳過自家最大籌碼）交換；**雙方以各自個性眼光計價都須 ≥0.9 划算**，且中立比率落在 **0.85 ~ 1/0.85** 防坑殺區間。撮合成功產生風聲。
- **節奏**：球季內每日 `tickAiTrades`（03 simulateDay 呼叫）以 4.5% 機率嘗試（期望全季 3~4 組）；休賽季批次 `runOffseasonAiTrades`（finishDraft 呼叫）再撮 1~2 組、於 `finalizeNewSeason` 開幕前 `settleOffseasonAiTrades` 定案；合計中等活躍 4~6 組。
- **定案** `resolveAiRumor`：球員仍在原隊且雙方年度上限（5 筆）未滿才成交；成交發布含雙方個性口吻的新聞。任何交易成立後 `invalidateRumorsForPlayers` 使涉及球員的其他風聲失效。
- 長跑健全性：15 年 smoke 後 **19 支 AI 隊最少仍有 51 人**，陣容不被掏空（smoke 玩家隊人數偏低為測試助推器不選秀不簽人之既有現象，v31 基線相同）。

### C. v32 風聲情報與反應措施（01/05）
- **情報分級**（玩家的交易球探，含空缺回退）：`rumorLeadDays` 提前 7/6/5 天（精準度≥70/≥55/其餘）；`rumorDetailLevel` 2＝完整名單＋撮合比率、1＝知道目標球員、0＝只知兩隊在談（球探空缺亦為 0）。
- 主控台情報卡三選一：
  - **①插隊搶人** `interceptRumorUi`：開交易畫面預載目標；`evaluateTrade` 對「風聲中的賣方讓出球員」啟用**硬門檻**——報價比率須 ≥ max(該隊門檻, AI 撮合比率＋5%)，不走隨機成交。搶成 `settleHijack`：原交易取消、賣方好感 +1、被搶買家好感 **−2** 並發新聞。
  - **②慫恿破局** `persuadeRumor`：每筆風聲限一次；代價賣方好感 −1；成功率 `clamp(0.2＋好感×0.03, 0.2, 0.5)`，**人情型賣方加倍（上限 0.9）**。
  - **③靜觀其變**：關閉情報卡，倒數到期照常定案。
- 休賽季批次風聲標記 `resolveAtSeasonStart`，玩家在休賽季畫面期間同樣可插手/慫恿。

### D. v32-B：好感影響開價（01）
- **B1 AI 主動提案** `generateAiProposalToUser`（每日 2% 機率、每年上限 2 次、好感≥+3 者優先）：依姿態決定送出老將或籌碼，從玩家陣中找 AI 眼光價值相近者；**開價讓利幅度＝好感×1%（人情型×2），封頂±10%**。主控台提案卡 3 天內回覆：接受 `acceptAiProposal`（成交、對方好感+1）／婉拒 `declineAiProposal`（不扣好感）。
- **B2 客隊讓利**：`evaluateTrade` 中好感 ≥+5 的球團成交門檻 `acceptHi` **打 95 折**（精算型不看交情，維持原門檻與決定性，既有回歸相容）。

### E. v32-C：KPI 動態化（04-state-core）
- **季中檢視** `tickKpiMidSeason`（03 每日呼叫；賽程過半觸發一次）：
  - **大幅超前**（分區龍頭且勝率≥.55，或勝率目標超標 8%）→ 追加**加碼目標**（`bonus:true`）：達成信任獎勵**加倍**、未達成**不扣分**。
  - **嚴重落後** → 高層召見卡二選一：**接受降標**（`acceptKpiReduction`：主目標依 `KPI_EASIER_MAP` 換易版，達成獎勵×0.5，且本季信任正向加分×0.75）／**硬拚原目標**（`declineKpiReduction`：附贈**止血考核**「近 15 戰 8 勝」，滿 15 戰即時判定，達成結算時 +3、未達不扣）。評語附本季聯盟 AI 補強交易數（純風味）。
- **連續性記憶** `S.kpiStreak`：連 2 年正規目標全達成 → 隔年 `escalated`（達成各+2 加成、高層胃口新聞）；連 2 年全滅 → `probation` **留校察看**（未達成扣分×1.5）。新目標鍵值：winpct55／winpct40／win50／win70。
- 開季仍固定 2 項目標（加碼/止血為追加項，不動既有 KPI 回歸）。

### F. 舊存檔相容（`ensureV32`，06 init 於 ensureV31 後呼叫）
- 補 `S.aiTrade`、`S.kpiStreak`、`S.draftDoneYear`（舊存檔視為選秀已過，休賽季窗口不被鎖死）、當季 `seasonKPI` 的季中檢視/止血/降標欄位。升級鏈：ensureV25→…→31→32。

### G. 測試與驗收
- 回歸測試 **372 項**（v31 為 328；新增 v32 專項 44 項：窗口新制/撮合與防坑殺/上限/風聲定案/插隊門檻與好感結算/慫恿限一次/AI提案與讓利封頂/95折門檻/季中檢視三態/結算修正係數/連續性/ensureV32）**連跑 3 次全過**。
- 15 年 smoke＋全畫面渲染（v32 每日 tick 全程參與）**連跑 3 次全過**；另以帶統計 smoke 抽驗 AI 陣容完整性（見 B）。
- 修正一項既有脆弱斷言：v31 傳承測試原以寫死球員驗證受贈者，v32 新增隨機數消耗使聯盟生成序列位移後失效；改為追蹤 `runTeamInheritance` 回傳的**實際受贈後輩**（配對規則本就挑全隊潛力最高者，測試意圖不變）。
- 整合單檔 HTML 沙盒抽驗 9 項全過。

### H. 已知簡化（誠實記錄）
- AI↔AI 撮合為「1 老將 ↔ 1~2 籌碼」定型包裹，未含多對多、選秀權或現金；成交上限 5 筆僅計 AI 交易（玩家自提交易維持不限）。
- 風聲倒數以「比賽日」計；休賽季批次風聲統一於開幕定案（無逐日倒數）。
- 慫恿成功率、插隊溢價 5%、提案讓利封頂±10% 為平衡參數，後續可依 Mars 實測調整。
- 季中檢視「超前/落後」以勝率與分區排名門檻判定，未含賽程強度；加碼/止血目標各限一次。

---

## 三十三、本次會議修正（v33：生涯深化版——東山再起深化＋代理人事務所深化）

> 目標：承 v32 交接路線，深化兩條既有系統的「生涯縱深」。A 系列讓「被解職→再起」從單一 fallback 長成一條有代價、有抉擇、有終點的職涯線（委任制邀約、聲望事件、沉潛充電、奪冠跳槽），並依本次會議拍板收束為**東山再起生涯僅此一次、再遭解職即永久 Game Over**。B 系列把 v28 的代理人交情從「談約折扣」擴展為可主動經營的人脈網（休賽季應酬、莫逆之交引薦獨家、交好透露動向）。全程繁體中文、六模組歸位、ASCII 檔名，寫 `ensureV33` 串入升級鏈。

### A. v33-A：東山再起深化（04-state-core／05-ui-dashboard）

**A1 委任制邀約（`MANDATE_META`／`rollOfferMandate`／`generateJobOffers`／`mandateActive`）**
- 每份聘僱邀約附帶新東家的**建隊委任**（為期 2 季），落地為 `c.mandate = { type, startYear, years:2 }`，於接任隔年生效。
- 三型委任（依球隊體質擲出：財務流血 70%→止血；戰力排名≥13→重建；≤8→爭冠；其餘 50/50 重建/爭冠）：
  - **重建委任**：起始信任 **+5**；委任期 KPI 改養成向——`winpct40`（勝率守 4 成）＋`youth5`（季末 1 軍至少 5 名 24 歲以下）。
  - **爭冠委任**：起始信任 **−5**；KPI 直接掛 `playoffs` 且 `mandateHard`（未達扣分 **×1.25**）＋一項經營目標；交換條件是**奢侈稅門檻 ×1.15**（02 `settleLeagueFinance`，僅玩家隊、僅委任生效期）。
  - **止血委任**：KPI 改 `profitPos`（年度損益轉正）＋`attendUp`（進場率 +5 個百分點），二者皆 `mandateDouble`（達成信任**加倍**）。
- 委任期間 `generateSeasonKPI` 走**委任目標池**（不走一般池，以 `else` 包住原邏輯）；委任過期後自動回歸一般 KPI。

**A2 聲望事件（`careerReputation` 擴充）**
- 聲望公式新增三項：解職紀錄 `firedCount × (−6)`、奪冠後主動跳槽 `champJumps × (+8)`、沉潛充電 `sabbaticals × (+5)`。
- 聲望 **≥85「名帥」**：`generateJobOffers` 可及**全聯盟**（`reachTop = ranked.length`）。
- **功成身退（`offerChampJumpMarket`）**：奪冠年的休賽季摘要出現探詢卡，可主動探詢跳槽市場（`S.champJumpYear` 防同年重抽）。跳槽走 `takeJobOffer(teamId, voluntary=true)`——**不消耗**東山再起機會、話題聲望 +8；不點則正常往下走流程＝留任。

**A3 沉潛一年（`runSabbaticalYear`）**
- 解職後可選擇拒絕所有邀約、沉潛充電一年。無人駕駛跑完整季：自動續約/續幕僚→`startDraft`＋`confirmSkipAllRemaining`→`finalizeNewSeason`→母國春訓→`while(simulateDay)`→舊東家待決策重傷自動採保守療法→`generatePlayoffs`＋季後賽跑完→國際年走 `runIntlTournament`／否則 `enterOffseason`。
- 歸來：`sabbaticals++`、聲望 +5、邀約重抽且可及範圍 **+3** 名、回到 GameOver 抉擇畫面。**生涯限用一次。**

**東山再起僅此一次（本次會議拍板）**
- `generateJobOffers` 開頭守衛：`c.fired && (c.rehires||0) >= 1` → 回空陣列。GameOver 畫面對已用過機會者顯示「🚪 業界的大門已經關上」永久出局卡。
- 奪冠自願跳槽（`voluntary=true`）記 `champJumps`、**不累加** `rehires`，故不消耗這唯一機會。被動再起（`voluntary` falsy）才累加 `rehires`。

### B. v33-B：代理人事務所深化（01-data-engine／02-finance／05-ui-dashboard）

**交情稱號門檻上調（與功能門檻對齊）**：`agentRelLabel` 改為**莫逆之交 ≥10**（好感滿級，解鎖引薦獨家）、**交好 ≥4**（解鎖動向情報）。

**B1 應酬經營（`isOffseasonNow`／`agentWineCost`／`canWineAgent`／`wineAndDineAgent`）**
- 休賽季（`seasonKPI.settled` 且年份相符）可對每類型經紀人**應酬一次**。費用 `(300 + round((rel+10)/20 × 500)) 萬` ＝ **300~800 萬**（交情越深越貴）。
- 擲骰：成功 70% 好感 +1、大失敗 10% 好感 −1、其餘 20% 錢花了交情原地踏步。事務所畫面每列附「應酬」按鈕（`S.agency.wined[type]` 記年、`canWineAgent` 控 disabled）。

**B2 引薦獨家（`generateAgencyOffseasonPerks`／`referralFor`）**
- `enterOffseason` 於 `refillInternationalMarket` 後、`aiSignInternationalPlayers` **之前**呼叫（獨家才來得及生效）。
- 對每個 **rel≥10** 的類型，引薦旗下最強 FA/國際 FA→`S.agency.referrals`。效果：①該球員本休賽季**獨家談判權**——AI 簽國際球員時 `cand` filter 排除引薦對象；②談約門檻**再打 95 折**（02 `submitNegotiationOffer` 於 `perks.reqMult` 之後掛 `referralDiscount` 旗標）。

**B3 動向情報（同 `generateAgencyOffseasonPerks`）**
- 對每個 **rel≥4** 的類型，透露一名旗下**他隊客戶**（26~33 歲、`trueOverall≥55`）「想換環境」→`S.agency.intel`＋新聞，作為交易目標線索（純情報，不預載交易）。

### C. 舊存檔相容（`ensureV33`，06 init 於 ensureV32 後呼叫）
- 回填 `firedCount = stints.length + (fired?1:0)`、`rehires = stints.length`（v28 stints 唯一來源為「解職→再起」）、`champJumps=0`、`sabbaticals=0`、`mandate=null`、`S.sabbatical=false`、`S.champJumpYear=null`；呼叫 `ensureAgency` 補 `wined/referrals/intel` 容器。升級鏈：ensureV25→…→32→33。

### D. 測試與驗收
- 回歸測試 **417 項**（v32 為 372；新增 v33 專項 45 項：ensureV33 回填、聲望三事件差值、三型委任判定/生效/過期、委任 KPI 目標池、止血加倍與爭冠×1.25 結算、youth5/profitPos/attendUp 判定、邀約附委任＋一次機會空邀約、應酬費用上下限/成功/扣款/年限/大失敗、引薦與情報門檻（10/4/3）、AI 國際簽援避開獨家、談約 95 折、自願跳槽不耗機會＋委任隔年生效、爭冠稅檻×1.15、沉潛守衛＋完整跑季）**連跑 2 次全過**。
- 另以完整 E2E 腳本複驗沉潛真實路徑：**自然結算解職**（trust 歸零觸發）→沉潛推進一季（年+1、`sabbaticals=1`、不增 GM 年資季）→歸來邀約重抽→接任記錄委任→二次解職邀約空（永久出局）。
- 15 年 smoke＋全畫面渲染**連跑 3 次全過**；該存檔恰觸發 1 次解職＋1 次東山再起（rehire-once 邊界），smoke 既有 fallback 可接住二次解職情形。

### E. 已知簡化（誠實記錄）
- **引薦獨家對本土 FA 無實質差異**：AI 本就不簽本土 FA（僅 `aiSignInternationalPlayers` 簽國際），故「獨家談判權」的排他效果只對國際 FA 引薦有意義；本土引薦僅剩 95 折折扣。
- **B3 為純情報線索**：只發新聞與 `S.agency.intel` 記錄，未做交易畫面預載（有別於 v32 風聲的 `interceptRumorUi` 預載）。
- **委任一律 2 季、接任隔年生效**：不因球隊體質微調年限；委任型於接任當下擲定，不中途改判。
- **沉潛期間舊東家由 `userTeamId` 代管**：名單掛在玩家 team id 之下自動運轉（選秀全跳過、傷勢保守），該季不產 KPI、不計 GM 年資；此為簡化，非讓玩家實際操作舊隊。
- 應酬成功率 70%、引薦 95 折、委任信任 ±5、爭冠稅檻 +15%、沉潛聲望 +5 均為平衡參數，後續可依 Mars 實測調整。

### F. 下一梯路線提醒（v34+）
- 第四梯構想：國際賽逐場化（目前為整段模擬）、C/D 級春訓事件深化。

---

## 第三十四節：v34「體驗強化與存檔版」——實測回饋九項修正／新功能＋潛在測試缺陷根除

> 本版來源：Mars 對 v33 真機實測後提出的九項回饋，經根因診斷與拍板後全數實作。
> 開發途中並揪出一個**基準版即存在、約 1/5 機率**的回歸測試閃爍當機（根因在 v29 舊測試碼，非遊戲邏輯），已一併根除。

### 1. 中繼王／救援王顯示 0 次（統計漏洞，03＋04）
- **根因**：`simulateDay` 中終結者／布局投手只累計 `G/SV/HD`、從不累計 `IP`；而 `computeSeasonAwards` 的投手池過濾 `IP > 0`，所有純後援被排除，救援王／中繼王實際頒給 SV=0 的先發。
- **修正**：後援登板同步 `IP += 1`，新增 `applyRelieverLine`（每次登板附帶三振 0–2、保送 30%、被安打 0–2、失分機率 `clamp(0.3−(抗壓−50)/250, 0.08, 0.45)`——讓後援防禦率合理而非 0.00，也不至於搶走最佳十人）。獎項候選池新增 `pitchersAny`（`G>0`），救援王／中繼王改從中選出且需 `SV>0`／`HD>0`。

### 2. 球員詳細頁成績一覽（06）
- 新增 `statCells`／`playerStatsSectionHtml`：詳細頁能力值下方顯示**本季／上季（含年份）／生涯通算**三列。打者欄位：出賽/打數/安打/全壘打/打點/盜壘/打擊率；投手欄位：出賽/勝/敗/救援/中繼/局數/三振/防禦率。資料源沿用既有 `seasonStats`／`lastSeasonStats`（v29 快照）／`careerStats`，純 UI。

### 3. 應酬時間窗（05）
- **根因**：事務所入口只掛在主控台；休賽季畫面鏈無入口，且選秀後 `finalizeNewSeason` 的 `seasonYear++` 立刻關閉 `isOffseasonNow()` 窗口——玩家回到主控台時應酬季早已結束。
- **修正**：休賽季摘要新增「🕵️ 應酬季節」卡與「前往代理人事務所」按鈕（僅在 `isOffseasonNow()` 成立時顯示，開局首個摘要不出現）；以 `UI.agencyReturn` 記錄來源，事務所返回鍵會回到休賽季摘要。應酬窗口定義不變。

### 4. 春訓「undefined威力」（02）
- **根因**：球路資料結構鍵為 `pt.type`（01 生成、06 顯示皆用 type），02 的 `applySpringTraining` 成長訊息誤寫 `pt.name`。
- **修正**：兩處改為 `pt.type`。

### 5. 票房經濟重平衡（02，Mars 拍板 A＋B 並用）
- **A. 人氣調節彈性**：`ticketDemandRate(price, cap, popularity)` 懲罰係數由固定 0.5 改為 `clamp(0.5 − (人氣−50)/200, 0.2, 0.6)`。人氣 50 維持舊制（上限價＝5 成意願）；人氣 99 時上限價意願升至 **74.5%**——「長期經營人氣→撐得起高票價」成為有效策略。三個呼叫點皆傳入球隊人氣。
- **B. 上限調升條件放寬**：票價上限調升由「票價開在上限 95% 以上＋上座率 ≥93%」改為**季均上座率 ≥90% 即調升**（上限仍 5000 元封頂），讓「低價養客→調高上限→逐步漲價」的循環真正跑得動。結算訊息同步改為「因上座踴躍」。
- 舊制鎖死機理（供回顧）：900 元/上限 1000 → 意願 ×0.556，人氣 99 的基礎值 ~0.70，相乘 ≈39% 恆定；且高票價下永無滿座、上限永不成長。

### 6. 自由球員市場改版（06，Mars 拍板：真實數值＋球探前瞻評估）
- `renderFreeAgents` 全面改為與國際市場同款卡片：守位／投打習慣／**現況與天花板等級徽章**／屬性格（真實數值——本土球員資訊公開）／上季成績行（`negoStatLine`）／預估年薪與簽約金，附排序列（綜合/年齡/類型/前瞻/身價，雙向）。
- **前瞻評估 `faTrendReport`**：由本土球探判讀。以「球員id＋年度」做確定性雜訊（同季不重擲），精準度越低對顛峰年齡的判讀誤差越大（acc100→0 歲、acc50→±2 歲）。三檔：📈 看漲（未達顛峰且潛力空間 ≥5）／➡️ 持平（顛峰期）／📉 衰退（已過顛峰，含年數）；附可信度標註（≥75 高／≥60 中／其餘偏低）。樣式：`.trend-up/flat/down`。

### 7. 季中事件暫停時程（04＋03＋01，Mars 拍板四類全停）
- 新增中斷收集器：`pushSimInterrupt(msg)`／`consumeSimInterrupts()`（`S.simInterrupts`，封頂 30 筆防沉潛等原生迴圈膨脹）。`doSimulateDay/Week/ToEnd` 迴圈每天檢查，有事件即停並顯示「⏸️ 時程暫停：…」。
- 四類事件回報點：(a) **先發陣容傷兵**（03——受傷者屬打線棒次／先發輪值／牛棚配置任一集合）；(b) **AI 主動交易提案**（01 `generateAiProposalToUser`）；(c) **KPI 季中檢視**（04 `tickKpiMidSeason`——高層加碼與召見降標皆停）；(d) **交易風聲**（01 `tryMatchAiTrade`——lv≥1 情報現身且非休賽季批次時）。
- 沉潛年與 smoke 的原生 `while(simulateDay)` 迴圈不受影響（不讀中斷）。

### 8. 玩家隊開局預算固定（02＋04，Mars 拍板：玩家 6000 萬、AI 維持隨機）
- 新增 `USER_STARTING_BUDGET = 6000萬` 與 `fixUserStartingBudget()`；`beginInitialOffseason` 開局呼叫（重置不再隨機）。AI 隊維持 `randInt(4000,8000)萬`，保留聯盟生態差異。東山再起接手球隊不受影響（僅新開局）。

### 9. 存檔／讀取／重置（04＋05，Mars 拍板：3 槽＋匯出 JSON）
- 既有：IndexedDB 單槽自動存檔（`persist()`，鍵 `save1`）＋確認式重置——**保留不動**。
- 04 新增：`saveState/loadState/clearState` 參數化（預設 `save1` 向下相容）；`MANUAL_SLOT_KEYS`（slot1~3）、`buildSaveMeta`（隊名/GM/年/日/時間/版本）、`saveToSlot`／`loadSlotMeta`／`loadFromSlot`（讀檔後 `persist()` 同步自動槽）／`deleteSlot`；`exportSaveJson`（Blob 下載 `baseball_gm_save_y{年}.json`）／`importSaveJson`（相容 `{meta,state}` 包裝與裸 S 兩種格式，驗證 teams/players 存在）。
- 05 新增「存檔管理」畫面（主控台 💾 按鈕）：3 槽位顯示 meta、覆蓋/讀取/刪除皆有 inline 確認；匯出/匯入 JSON（手機 PWA 清瀏覽器資料或換裝置的保險）。
- 06 重構：init 的「載入→升級鏈→畫面還原」抽成 **`hydrateLoadedState()`**，開頁初始化、槽位讀取、JSON 匯入三處共用——匯入舊版存檔自動走完整 ensure 鏈。

### 存檔升級（`ensureV34`）
- 補 `S.simInterrupts = []`。升級鏈：ensureV25→…→33→**34**（06 `hydrateLoadedState` 依序呼叫）。

### 潛在缺陷根除：基準版回歸測試 ~1/5 機率閃爍當機
- **症狀**：`refillRoster` 讀到名單殘留、`S.players` 已無此人的 id → `TypeError`。基準版（v33 交付版）即可重現。
- **根因（測試碼，非遊戲邏輯）**：v29「新秀薪資上限」測試偽造 1 輪 1 順位假選秀；出價**成交**時（隨機約 4 成）`finalizeNegotiation → advanceDraftUntilUserTurn` 令假選秀立即完結、**意外觸發完整 `finishDraft`**（全聯盟 AI refill/capRoster 重整＋玩家隊裁員，裁員=直接 `delete S.players`）；測試清理再用「舊 roster2 快照」整條覆蓋還原 → 已刪 id 在名單上復活（missing）／期間變動的 id 重複（dup）。髒名單潛伏至下一次真 `finishDraft` 的 `refillRoster` 才當機。
- **修正**：①測試假選秀改鋪 **2 順位**（成交後仍輪玩家、不會完結）＋清理改「精準移除」該新秀（不用快照整條還原）；②遊戲碼 `refillRoster` 加一行防呆 `keepIds.filter(id => S.players[id])`（防禦性自清，根因仍在測試碼）。
- **連帶修復第二個既有閃爍**：v32 兩條 KPI 斷言（加碼不扣分／留察×1.5）用 `champion` 目標，年循環殘留的 `S.playoffs` 若恰由玩家隊奪冠（隨機）即誤判達成——結算前清空 playoffs、測後還原。
- 測試 harness 小升級：`document.getElementById` 依 id 快取 stub 元素，render 掛上的 onclick 可在測試中直接觸發（供 v34 導航測試）。

### 已知簡化（誠實記錄）
- 後援投球內容（三振/保送/失分）為登板附帶擲骰的近似模型，未逐打席模擬。
- 前瞻評估的球探誤差只作用於「顛峰年齡判讀」，潛力空間讀真值；可信度標註只反映精準度分檔。
- 中斷事件以「當天有無」判定；同日多事件合併為一次暫停訊息。
- 存檔槽固定 3 個；匯入 JSON 不做深度 schema 驗證（僅檢查 teams/players），畸形檔案由升級鏈與防呆兜底。
- 玩家隊固定預算僅作用於「全新開局」；東山再起接手球隊沿用該隊現況。

### 測試與驗收（v34）
- 回歸測試 **454 項**（v33 為 417；新增 37 項：後援 IP 累計×2、救援/中繼王頒發與顯示×2、成績區塊×3、應酬入口與返回導向×3、春訓標籤×3、票價彈性×4、上限放寬×1、FA 前瞻×4、中斷 API 與迴圈中斷×5、固定預算×2、存檔槽/匯出匯入/ensureV34×6…），**連續 38 次全過**（含針對原閃爍的加壓驗證）。
- 15 年 smoke＋全畫面渲染（新增 saveManager）**連跑 4 次全過**，其中一輪含解職→東山再起循環。
- 整合單檔沙盒啟動驗證：20 隊、v34 七函式就位、玩家預算 6000 萬、`ticketDemandRate(1000,1000,99)=0.745`、存檔管理與 FA 新畫面可渲染。

## 第三十五節：v35「二周目穩定與新手教學版」——真機二周目 bug 根因根除＋外援上限守衛＋新手教學

### 真機回報症狀（Mars，二周目實測）
1. 二周目（東山再起後）**教練不能更換**。
2. 賽季結束時，**更換球探選項無法選擇而當機**（`Uncaught Error: Script error.`）。
3. 中途按 X 跳出、以保留的存檔續玩後問題出現——懷疑存讀檔流程是肇因。

### 根因 A（主因）：`takeJobOffer` 未清空舊隊幕僚續約佇列（04）
- **機理**：解職發生在 `enterOffseason`，該函式**剛替「舊隊」建好** `S.pendingStaffRenewals`（實測情境殘留 19 筆）。`takeJobOffer` 換隊時清了 `pendingContractRenewals` 卻漏了幕僚佇列——殘留項目跟著玩家到新東家：
  - 玩家在殘留佇列裡「續約」→ `staffRef` 優先以 `item.staffId` 解析，**續到的是舊隊教練**；玩家隊教練文風不動 → 體感即「教練不能更換」。
  - 玩家點「不續約」→ `vacateStaff` 以 `S.userTeamId` 取隊，**誤刪新隊現任**教練/球探並立空缺旗標 → 新隊被打洞。
  - `hydrateLoadedState` 畫面還原見佇列非空即強制導向 staffRenewal → 按 X 離開再回來必被拉回殘留佇列（Mars 的懷疑方向正確：存讀不是肇因，但把損壞狀態忠實保存並強制導向，讓 bug 必現）。
- **修正**：`takeJobOffer` 清空 `pendingStaffRenewals`／`pendingCoachHires`／殘留 staffRenewal 談判；舊隊到期幕僚交回 AI 自動補人邏輯（次年 processCoach/ScoutContracts 處理）。

### 根因 B（當機主因）：v31「職位空缺」合法狀態未防呆（05＋06）
- **機理**：四處直接讀空缺職位屬性拋 `Cannot read 'name'`：
  - `coachRoleOptionsHtml`（05）——教練空缺 → **賽季結束的休賽季摘要渲染即當機**；且 `enterOffseason` 先 `persist()` 再 `render()`，當機狀態被存檔 → 重載必再當機（永久卡死）。
  - `renderDraft`／`renderFreeAgents`／`renderInternationalMarket`（06）——對應球探空缺 → 當機。玩家「不續約想換球探」→ 佇列走完進選秀 → 立即當機＝「更換球探選項無法選擇而當機」。
- **修正**：四處全數防呆——教練空缺顯示「職位空缺」、球探空缺顯示「（職位空缺・盲評）」（`effectiveScoutAccuracy` 對 null 本就回 50 盲評，僅顯示層漏防）。

### 防禦縱深（02＋04＋01＋06）
- **`staffRef` 隊伍歸屬檢查**：教練需 `.team === S.userTeamId` 且與職位吻合、球探需 `scoutId` 吻合；不符一律回 null（佇列自動略過）。
- **`declineStaffRenewal`**：staffRef 解析不到人 → 純略過佇列，**絕不** `vacateStaff`（杜絕誤刪現任）。
- **`sanitizeStaffRenewals()`**（04 新增）：過濾不屬於現任球隊的佇列項目；`proceedToStaffOrDraft` 與 `ensureV35` 皆呼叫（既有損壞存檔讀入即自癒）。
- **AI 補人清旗標**：`processCoachContracts`／`processScoutContracts` AI 分支補人時同步 `setCoach/ScoutVacancy(false)`（先前玩家離開的舊隊空缺旗標永不清除、加成永久歸零）。
- **重載還原強化**（06 `hydrateLoadedState`）：①解職中優先還原 `gameOver`（先前會被佇列劫走）；②休賽季進行中（`offseasonEnteredYear === seasonYear`）重載回摘要——先前落到 dashboard，可能經頒獎鈕**二次結算**；③清除跨存檔殘留的暫時性 UI（談判視窗/教練候選/確認框——槽位讀檔與 JSON 匯入不經整頁重載）。
- **`enterOffseason` 重入防護**：本年已結算過（`offseasonEnteredYear`＋摘要存在）直接回摘要，杜絕二次財務結算/球員二次老化。
- **`proceedToDraft` 防重辦**：`draftDoneYear === seasonYear` 時直接顯示選秀結束摘要。

### 外援上限守衛（01；v32 時代既有漏洞，煙霧加壓時揭露）
- **根因**：`executeTrade.moveIn` 與 `autoPromote` 皆無 1 軍外援上限檢查——AI↔AI 交易可把外籍 1 軍球員送進已滿編球隊（v34 基準版可直接重現：上限 4 → 交易後 5）。
- **修正**：①`moveIn` 為所有交易路徑（玩家/AI提案/AI互易/風聲攔截）共同出口——超限外援改落 2 軍（上限僅限 1 軍，規則一致）；②`autoPromote` 外援滿編時升格候選排除外籍球員。

### 新手教學（05 新畫面 `tutorial`，Mars 需求）
- 手風琴式九大章節：🎯 目標與基本規則／🗓️ 年度循環／🧢 陣容與養成／🧠 教練團與球探／💰 **財務經營與避免赤字（必讀）**／🔁 補強管道／🕵️ 事務所與人脈／📈 GM 生涯／💾 存檔與操作注意。
- 入口：主控台「📖 新手教學」按鈕＋初始休賽季摘要「第一次接手球團？」提示卡（`!S.gameStarted` 才顯示）；`UI.tutorialReturn` 記返回來源。解職鎖放行教學畫面（唯讀）。
- 財務章節具體給出六條避免赤字訣竅（薪資對照聯盟平均、票價與人氣關係、設施分期、合約年限策略、行銷開關、選配支出從簡），對應 Mars「如何避免財政預算赤字」的明確要求。

### 存檔升級（`ensureV35`）
- ①殘留幕僚佇列消毒（修復 Mars 現有存檔）；②全隊空缺旗標與實況同步——有人→清旗標、AI 隊空缺→直接補人清旗標（AI 不走空缺制）、玩家隊空缺→立旗標（合法狀態，畫面已防呆）；③`offseasonEnteredYear` 補值（以本年 KPI 是否已結算回推）。升級鏈：ensureV25→…→34→**35**。

### 已知簡化（誠實記錄）
- 殘留佇列消毒採「剔除」而非「轉譯到新隊」：舊隊到期幕僚的續約決策權交回 AI，玩家不再代舊隊決策（語意上也更合理）。
- 超限外援落 2 軍不另發通知新聞；玩家隊交易介面本就有名額提示。
- 教學為靜態章節文字，未做互動式引導（逐步 hand-holding 教學另議）。
- 二次結算防護以年度旗標為粒度；同年內「摘要→裁員→談約」各子階段的重載仍以既有佇列旗標還原。

### 測試與驗收（v35）
- 回歸測試 **493 項**（v34 為 454；新增 39 項：takeJobOffer 清佇列×5、staffRef 歸屬×4、decline 不打洞×2、sanitize×2、四畫面空缺防呆×4、ensureV35×4、AI 補人清旗標×2、重載還原×3、enterOffseason 重入×3、proceedToDraft 防重辦×1、教學×5、外援守衛×3…），加壓連跑多次全過。
- 15 年 smoke＋全畫面渲染加壓連跑全過（外援守衛前曾以加壓揭露既有漏洞並根因驗證於 v34 基準版）。
- 專用二周目 E2E 診斷：解職→東山再起→存讀→教練更換→季末球探續約→選秀全鏈路通過；四個當機點修復前後對照驗證。

## 第三十五・一節：v35.1「開機防護熱修（Hotfix）」——手機白屏（完全無法開啟）根治

### 真機回報症狀（Mars，手機）
1. 手機**完全無法開啟**；轉傳到其他手機開，出現兩種畫面：①取名（開局）畫面未渲染完、無法進入；②只剩綠色底圖、其餘空白。

### 根因診斷
- 全新開檔（無存檔）在近真瀏覽器環境（jsdom）**可正常渲染開局畫面、零錯誤**；程式亦無 `?.`／`??`／邏輯賦值等舊 iOS 解析期殺手語法。
- **主因**：開機 IIFE 的 `render()` 在 `try/catch` **之外**——`try` 只保護「讀檔＋還原」，但**還原成功後那次 `render()` 一旦拋錯，整頁白屏**（`#app` 空 → 只剩 body 綠底＝症狀②）。只要玩家存檔進到某個會在渲染時拋錯的狀態，重開即白屏、看似「完全打不開」。
- **傳輸因素（症狀①之一）**：611KB 單檔經訊息／雲端轉傳可能被截斷或 re-encode，導致 `<script>` 不完整而整段不執行。此非檔案內程式可完全根治，另於交付說明提示以「雲端下載原檔／zip」方式傳輸。

### 修法（安全模式・Safe Mode）
- **開機 IIFE 硬化**（06）：讀檔失敗當作無存檔；**暫存原始存檔** `UI.__rawSave`（即使還原失敗也能匯出給開發者）；還原拋錯 → 進 `bootRecovery` 安全模式而非靜默退開局（避免默默吞掉存檔）。
- **全域渲染防護**（05）：`render()` 改為薄包裝，內層 `renderScreen()` 一拋錯即落 `renderBootRecovery()`；再不濟也寫一段純文字錯誤提示，**杜絕任何白屏**。
- **安全模式救援畫面** `renderBootRecovery`（05，完全自給、不依賴 `S`）：顯示錯誤訊息＋①**匯出存檔**（下載 JSON／或整段文字全選複製，供開發者精準重現）②**清除存檔重新開始** ③**回開局畫面**。

### 驗收
- 回歸測試 **498 項**（v35 為 493，新增 5 項：`renderBootRecovery`／`renderScreen` 存在、畫面拋錯時 render 不拋出未捕獲例外、自動落安全模式、安全模式內容非空白）加壓全過。
- 15 年 smoke ＋全畫面渲染全過。
- jsdom E2E：①無存檔→開局畫面（零錯誤）；②S 形狀壞存檔（強制 render 拋錯）→**安全模式救援畫面（零未捕獲錯誤、畫面非空白、含匯出鈕）**，證實白屏已根治。

### 已知簡化（誠實記錄）
- 安全模式攔截的是「還原後渲染拋錯」與「還原拋錯」；若檔案在傳輸中被截斷導致 `<script>` 根本不執行，任何檔案內防護都無法運行（屬傳輸層問題），需以正確傳檔方式規避。
- 本熱修僅動開機／渲染防護與新增救援畫面，未更動任何遊戲邏輯；v36（④⑤⑥修正＋冠軍回饋五項＋第10階段事件系統）另版進行。

## 第三十五・二節：v35.2「iOS 開啟方式熱修（Hotfix）」——綠色空屏＝JavaScript 未執行之根因與後備

### 真機新證據（Mars，iPhone，截圖）
- v35.1 檔案（617KB，完整）以 iOS「檔案」App 開啟後**只剩深綠底、其餘全空**；安全模式也未出現。Mars：在開發端可執行，下載到手機後打不開、也看不到匯出 JSON。

### 根因診斷
- 只剩綠底＝body 的 CSS 背景有渲染、但 `#app` 完全是空的 → **整段 JavaScript 沒有執行**。既然 JS 沒跑，v35.1 的安全模式（本身也是 JS）當然不會出現。
- 檔案本身無誤（在真瀏覽器／node/jsdom 皆可執行、616KB 未截斷）。症狀來自**開啟方式**：iOS「檔案」App 的 Quick Look 預覽在此情境未執行頁面 JS（僅渲染 HTML/CSS）。
- 次要變因：整合檔 `<head>` 內含外部 Google Fonts 連結，於離線／預覽沙盒可能拖累或阻擋渲染。

### 修法（皆為 HTML 外殼層，零遊戲邏輯改動）
1. **移除外部字型連結**：改離線自足（CSS 已具系統字型後備），符合離線優先，並排除預覽環境被外部資源卡住的變因。
2. **靜態載入後備**：`#app` 內預置「載入中…／若持續停在此代表未執行 JS，請改用 Safari」靜態文字；JS 一旦執行即被 `render()` 覆蓋。→ 從此不再有「無資訊綠屏」，未跑 JS 時使用者看得到指引。
3. **可見錯誤攔截器**：主程式前置一段獨立 `<script>`，掛 `window` 的 error／unhandledrejection，將任何啟動期解析／執行錯誤**直接顯示在畫面上**（可截圖回報）。連主程式語法解析錯誤也能被此前置攔截器捕捉呈現。

### 驗收
- jsdom：JS 執行後 preboot 被正常取代為開局畫面、零未捕獲錯誤；靜態後備與錯誤攔截器存在於原始 HTML；外部字型連結已移除。

### 正解與durable方向
- 立即：以 **Safari**（真瀏覽器引擎）開啟本檔即可正常執行；「檔案」App 預覽不保證跑 JS。
- 長久：將單檔**掛到網頁空間**以 URL 於 Safari 開啟——同時可「加入主畫面」，銜接第 11 階段 PWA。

## 第三十六節：v36「④⑤⑥修正 ＋ 冠軍回饋五項 ＋ 第10階段事件系統 ＋ 設施性格開局 ＋ 開機強化」

> 本版一次整合六大塊，全部以「根因/現況診斷 → 實作 → 回歸＋15年smoke全綠 → 交付三件套」流程完成。回歸自 v35.2 的 498 增至 **546**，整合單檔本身亦通過同套回歸與 smoke（非僅模組版）。

### A. 遊戲初始硬體建設：AI 依「球團性格」開局（玩家白手起家）
- **現況診斷**：球場等級全隊固定 Lv1（不變、不加 Lv0）；其他設施（訓練11項／醫療／球探辦公室／宿舍／情蒐分析室／復健）玩家隊本就從 Lv0 開始；AI 隊原為「不分性格的平均亂數」（0~2 / 0~1）。
- **修法**：新增 `PERSONA_FACILITY_INIT` 表與 `personaFacilityRoll()`（02），`ensureFacilities` 六類設施全部改走此函式。玩家隊一律 0；AI 依 7 種性格給不同「起始強項與等級帶」，**上限一律封在 Lv2**（與玩家落差最多 2 級，避免開局差距過大玩不下去）。豪購全面偏高、精算重情報端（球探室/情蒐）、養成重訓練/宿舍、保守幾乎全 0…。
- **相容/範圍**：僅影響新開局；舊存檔與既有 `facilities` 不重擲。東山再起接手既有球團＝沿用該隊原設施、不歸零。

### B. 開機強化（storage-optional boot）
- **根因**：`openDB()` 在受限環境（無 IndexedDB／隱私模式）可能 `open` 既不觸發 onsuccess 也不觸發 onerror，`await loadState()` 永遠卡住 → `render()` 不執行 → 綠底空屏。
- **修法（04）**：`openDB` 加 `typeof indexedDB` 防呆（立即 reject）＋ **1500ms 逾時保險** ＋ `onblocked`，任何情況都在 1.5 秒內結束、開機必達 `render()`。獨立實證：不存在→1ms reject、卡死→1500ms reject、正常→resolve。
- **手機開啟結論**：iOS「檔案」預覽沙盒**完全不執行 JS**（查證：內建預覽器移除外部 CSS 與 JS），任何程式改動都無法在該模式跑起遊戲。**已由 Mars 確認：手機用 Edge 瀏覽器開啟可正常執行**（真瀏覽器＝完整 JS 引擎＋存檔）。本開機強化仍有效於任何真瀏覽器的受限存檔情境（可玩、暫不存檔）。

### C. ④ AI 主動提案卡：攤開雙方球探評估數據
- `renderAiProposalCard`（05）改為並列兩表：對方球員顯示**交易球探評估值**（重用 `scoutedEstimate`＋交易球探有效準確度，一次算好快取於 `proposal.scoutedCache`，重繪不跳動），自家球員顯示**真實值**，並附雙方 `tradeValue` 總值對比；保留接受/婉拒。

### D. ⑤ 選秀完整報告：選前可看 ＋ 新秀天花板數值
- `attachScoutedEstimates`（01）改存 `scoutedCeilingVal`（數值），`scoutedCeiling` 等級由**同一數值**推導（徽章與數字一致）。
- 選秀卡（06）由精簡行擴為**完整評估網格（選前即可見完整報告）**；新秀現況顯示（NN）、天花板顯示（約NN）；談判新秀報告與國際市場卡一併補上數值。

### E. ⑥ 球探換人：候選人比較 picker
- 移除「一按即隨機扣錢」的 `replaceScout`；新增 `openScoutPicker`/`generateScoutCandidates`/`hireScoutCandidate`（06），對齊教練 picker：一次列 3 位候選、並排比較準確度/專精/年薪/簽約金與現任差距，**簽下才扣款**、補實空缺同流程、簽下後不殘留暫存欄位。picker 暫存狀態於 `wireRosterNav`／`hydrateLoadedState` 清除。

### F. 冠軍回饋五項
- `applyChampionshipRewards(team, streak)`（02），由 `settleSeasonKPI` 於玩家奪冠時呼叫：① 奪冠獎金（基礎 8000萬）② 連霸遞增（`S.champStreak` 奪冠累加、未奪冠歸零，每連一次獎金 +4000萬、士氣/忠誠加碼）③ 全隊士氣＋忠誠（新 `loyalty` 值；忠誠高→續約期望薪資折扣，最多約 8%）④ 冠軍旗（`team.champBanners[]`）＋**博物館連動**（`stadiumEffects` 中博物館 popBoost 隨冠軍旗數放大，每面 +0.4、上限 5 面）⑤ 國際賽母國奪世界冠軍→玩家隊額外獎金 5000萬＋全隊士氣（於 `runIntlTournament`，疊加於既有隔年贊助/周邊加成）。

### G. 第 10 階段：事件系統
- 資料驅動 `EVENT_DEFS`（03）＋逐日加權觸發 `tickEvents`（掛於 `simulateDay` 末端）＋結算 `resolveEvent`＋主控台 `renderEventCard`（05）。
- **觸發控制**：每季上限 6 件、觸發後冷卻 14 天、每日基礎機率 9%、一次只處理一張卡（不洗版）；權重依球隊狀況/球員（如低士氣時「更衣室裂痕/求去」機率升高）。觸發時 `pushSimInterrupt` 中斷連續模擬讓玩家決定。
- **初版 6 種、涵蓋 4 類**：更衣室（領袖浮現、更衣室裂痕）、場外（球員場外風波）、營運（贊助臨時加碼）、球員（求去、年輕球員頓悟）。每事件 2~3 選項，各有代價與後果（動到士氣/忠誠/財務/人氣/能力/狀態）。
- **序列化安全**：`S.activeEvent` 僅存純資料（key＋選項＋ctx），效果於結算時以 key 重建，函式不進存檔；`activeEvent` 屬遊戲狀態、隨存檔保留（待決事件存讀後仍在）。

### 已知簡化（誠實記錄）
- AI 隊起始設施上限封 2、且 AI 仍沿用既有「不逐年升級其他設施」的簡化（球場等級會升）。
- 事件系統初版 6 種、效果為即時數值調整；更深的連鎖事件、C/D 級春訓事件深化、國際賽逐場化列為後續梯次 backlog。
- 忠誠（loyalty）目前效果為續約折扣一項；морale/loyalty 尚未接入比賽模擬數值（避免動到 sim 造成回歸風險），留待後續評估。

### 測試與驗收（v36）
- 回歸 **546 / 0**（自 498 新增：設施性格開局、球探 picker、④⑤、冠軍五項、事件系統各批斷言；並更新舊 v26「AI新設施0~1」→≤2、舊 `replaceScout` 清理呼叫→picker 流程）。
- 15 年 smoke ＋全畫面渲染全過（自然觸發事件、國際賽加碼、忠誠續約等路徑）。
- **整合單檔本身**（非僅模組版）抽出模組區以同套回歸＋smoke 驗證：546/0、smoke 全過。

---

## 第三十七節：v37「實測回饋十項 ＋ 第11階段：打磨與 PWA」

### 本版緣起
v36 交付後 Mars 於 Edge 真機實測（手機開啟已確認可正常執行、含存檔），提出十項回饋，經根因診斷後由 Mars 逐項拍板，與**第 11 階段（打磨＋PWA）**併為 v37 一次交付。診斷紀錄如下（誠實記載「原本是什麼」）：

| 項目 | 診斷結果（v36 實況） | v37 處置 |
|---|---|---|
| ① C/D 級國家事件 | v29 決策「海外春訓限 B 級以上」後，C/D 級 16 國僅剩國籍/國際市場/國際賽用途，**無主動互動** | 新增交流賽＋行銷企劃兩個平行活動（Mars：全要） |
| ② 選秀球探完整報告 | 選秀卡已有評估網格；「📋球探完整報告」在**有 scouted 時正常顯示**（node harness 印出實證）。Mars 所見「空白」＝**逐屬性的「預估值」欄位從未實作**（每屬性僅有現在值，天花板只給一個總評數字） | 新增逐屬性「現在→預估~天花板」兩段顯示，並將同一份完整報告掛到選秀卡（選前可見） |
| ③ 開局人氣 | `ensureFinance` 對**所有球隊**都給 `genRating(50,12)` 隨機人氣，固定 35 不成立 | 新增 `USER_STARTING_POPULARITY=35`，**只鎖玩家隊**（AI 維持隨機） |
| ④ 隊長 | **無持久隊長身份**；「任命為隊長」僅是 `leaderEmergence` 事件的一次性選項（士氣+4/忠誠+8），**無一季一次限制** | 建立真正的隊長系統（持久身份＋一季一次＋常駐效果＋**接進比賽**） |
| ⑤ 開季目標協商 | 開季 KPI **自動公告、不可協商**；僅季中檢視有降標/硬拚。重建期高標→未達扣信任、季中降標又減獎勵，兩頭虧（`rebuild` 委任只在解職後接新隊才有） | 新增開季「談條件式」協商（一季一次） |
| ⑥ 季中特訓生涯階段 | `growthPhaseLabel()` **已存在但只用於談約報告**，特訓卡未帶 | 特訓指派卡＋主控台特訓卡皆帶上階段標籤 |
| ⑦ 比賽換人 | 比賽為**盒分級模擬**（非逐打席）：投手先發→布局/中繼→終結調度**存在且運作**（疲勞>70 自動跳過改用次位）；**代打/代跑/代守不存在** | 以「賽前指定專員＋近戰觸發」抽象實作三種替補 |
| ⑧ 傷病遞補 | v34 已做一半：先發傷兵**會中斷連續模擬**讓玩家重排；玩家不理則 `autoLineup` 自動補位。**缺**：自動補位為「硬填」，守位湊不出來也硬塞，不會停下 | 補上守位缺口偵測→**停止並跳提示**要求手動處理 |
| ⑨ 獎項球隊名 | 確認為 bug：MVP 類有 `awardteam`，**金棒/金臂/最佳9人/金手套四類漏印** | 四類補上球隊名＋「你的球隊！」標記 |
| ⑩ 第 11 階段 | 未做 | 基礎 PWA（manifest／SW／icon／加入主畫面） |

### A. ① C/D 級國家平行活動（交流賽＋海外行銷企劃）
- `ensureCdActivities`／`runCdExchange`／`runCdMarketing`／`cdActNations`／`cdCostFor`（02）＋ `renderCdActivitiesCard`（05）。
- **開幕前（`S.currentDay===0`）可辦、各一季一次**（`S.cdActivities={year,exchangeDone,marketingDone}`）；對象＝**C/D 級國家與母國**。
- **交流賽**（偏球員/情報）：成本 C 800萬／D 500萬／母國 300萬。7 成成功＝人氣+4、全隊士氣+2，另 35% 機率以 `generateForeignPlayer(true, nation)` **發掘當地潛力股並列入國際市場獨家名單**（接回 C/D 的「國際市場」用途）；3 成水土不服＝人氣僅+1。
- **行銷企劃**（偏財務/人氣）：成本 C 1000萬／D 700萬／母國 500萬。7 成成功＝回收 1.6~2.2 倍、人氣+3；3 成冷淡＝回收 0.5 倍、人氣+1。

### B. ② 新秀球探報告：逐屬性「現在 → 預估值」
- `scoutedAttrProjection(cur, scoutedOverall, scoutedCeilingVal)`（01）：模型上各屬性皆朝**單一 `p.potential`** 收斂（見 `developPlayer`），故以「總體成長空間＝天花板−現況總評」為位移量把各屬性往上推估，**保留屬性間相對高低、封頂 99、不低於現況**；資料不足回 `null`（顯示端自動略過）。
- `scoutedAttrRows(p)`（02）為**報告與選秀卡共用**的逐屬性列（`現在(評估) → ~預估`），球速自動轉 km/h。
- 選秀卡（06）與 `negotiationScoutBlock`（02）皆改用共用列並加圖例，**選前即可看到完整報告**。

### C. ③ 玩家隊開局人氣固定 35
- `USER_STARTING_POPULARITY=35`，於既有 `fixUserStartingBudget()`（`newGame` 呼叫）內一併鎖定，比照 v34 預算固定做法；**AI 隊維持 `genRating(50,12)` 隨機**以保留聯盟生態差異。

### D. ④ 隊長系統（常駐＋接進比賽）
- `team.captainId`／`team.captainAppointedYear`；`appointCaptain`／`activeCaptain`／`teamHasCaptainBonus`／`canAppointCaptainThisSeason`／`pruneCaptain`（03）＋`renderCaptainSection`（06，球員詳情頁）＋名單 `Ⓒ隊長` 標記。
- **一季一次**：同季已任命則需等下季；例外——現任隊長已離隊/退休時允許本季改任（避免卡死）。
- **任命效果**：本人士氣+6、忠誠+10。**常駐**：每季在隊忠誠+5（長期高忠誠→續約讓利，接 v36 忠誠折扣）。
- **接進比賽（Mars 拍板）**：隊長在陣（一軍、未受傷）時 `teamComposureRating` **+4**（`CAPTAIN_COMPOSURE_BONUS`），經由既有「近戰（分差≤2）抗壓決勝」機制放大近戰勝率；另全隊每日狀況漂移偏正向（`dormBias += 0.06`）。這是 v36 backlog「士氣/忠誠接入比賽模擬」的**第一個正式掛鉤**。

### E. ⑤ 開季目標「談條件式」協商
- `KPI_LOWER_CHAIN`／`kpiNegotiableGoalIndex`／`canNegotiateKpi`／`negotiateKpiGoalDown`（04）＋ KPI 卡協商區塊（05）。
- **可協商窗口**：開季前期（`S.currentDay<=7`）、每季一次（`S.seasonKPI.negotiated`）、**委任目標與加碼目標不可協商**。
- **降階鏈**：奪冠→打進總冠軍賽→晉級季後賽→勝率5成→勝率4成5→勝率4成；分區前2→勝率5成；55勝→50勝。
- **交換條件（Mars 拍板「談條件式」）**：降階後標記 `reduced`（沿用達成獎勵減半）＋ `negotiated`；**若連降階後的目標仍未達成，額外扣信任＝該目標未達成基準扣分的一半**（`-(14-3*diff)*0.5`，四捨五入）。不協商＝完全無額外代價。

### F. ⑥ 季中特訓顯示生涯階段
- `renderMidTrainingSection`（06）兩個分支（特訓中／指派中）與主控台特訓總覽卡（05）皆帶 `growthPhaseLabel(p)`（成長期／巔峰期／衰退期＋說明），指派文案補上「成長期年輕球員練起來最划算、衰退期老將效益有限」。

### G. ⑦ 代打／代跑／代守（盒分級架構下＝賽前設定型）
- `benchSpecialist`／`applyBenchRoleNudge`／`creditBenchRoleAppearances`（03）＋`renderBenchRolesSection`／`setBenchRole`（06，先發打線頁）。
- **指派**：`team.benchRoles={pinchHit,pinchRun,defSub}`，須為**一軍、非先發打線、健康**的野手（清單依對應能力排序：代打重接觸/長打、代跑重速度/盜壘、代守重守備）。
- **觸發**：僅**近戰（分差≤3）**發動；代打 22%／代跑 16%（限落後或平手時追平・超前 1 分）、代守 22%（限領先 1~2 分時守下 1 分）。
- **板凳存在感**：`creditBenchRoleAppearances` 於每日賽後入帳專員的 G／AB／H／RBI／SB（獎勵陣容深度）。`__benchCredits` 為每日暫存、**不序列化**。
- AI 隊不設定 `benchRoles`，故對 AI 無效（維持對稱性由玩家調度決定）。

### H. ⑧ 傷病：守位湊不出來→停止並跳提示
- `lineupPositionGaps(team)`（03）＋ `simulateDay` 每日檢查（玩家隊）→ `pushSimInterrupt` 中斷連續模擬並提示。
- **判定門檻**：可用健康野手 < 8 名／**無人可守捕手(C)**／某守位僅能由 `effectivePositionFielding < 40` 的球員勉強補位。
- 維持 v34 既有安全網：玩家忘記處理時 `autoLineup` 仍會自動補位讓比賽進行；**但守位真的湊不出來時不再硬塞**，改為停下要求玩家手動處理。

### I. ⑨ 獎項顯示球隊名稱
- `renderAwards`（05）：金棒／金臂／最佳9人／金手套四類卡片補 `<div class="awardteam">`（沿用 MVP 類同款式與「你的球隊！」標記）。

### J. ⑩ 第 11 階段：打磨與 PWA
- 新增 `manifest.webmanifest`（standalone／portrait／theme #142B22／icons 192・512 maskable）、`sw.js`（App Shell 快取 `baseballgm-v37`，install 以 `Promise.allSettled` 略過缺檔、activate 清舊快取、fetch 走 cache-first＋navigate 後備 index）、`icon-512/192/180.png`（程式生成：球場綠底＋棒球＋金色 GM，無外部素材依賴）。
- `index.html`（模組版）補 manifest／apple-touch-icon／theme-color／`apple-mobile-web-app-title`，並以**防呆**方式註冊 SW：僅在 `http/https` 且瀏覽器支援時註冊，`file://` 或不支援時**靜默略過**，任何例外都不阻擋遊戲啟動（沿用 v35 開機防護原則：PWA 是加分、不是前提）。
- **整合單檔**：補上 PWA/主畫面 meta，但**不註冊 SW**（單檔已離線自足，且常以 file:// 開啟），維持「真瀏覽器開單檔即可玩」的既有保證。
- 上架與「加入主畫面」步驟另見交付說明（免費、全程手機可操作）。

### 已知簡化（誠實記錄）
- **② 逐屬性預估值是「等位移」推估**：因成長模型只有單一 `p.potential`（無逐屬性潛力），各屬性預估＝現況＋相同成長空間，故屬性間相對高低固定不變。若日後要「投手球速幾乎不漲、控球漲很多」這類差異化，需先在資料層引入逐屬性潛力（屬較大改動，未做）。
- **⑦ 代打/代跑/代守是機率化的近戰結果調整**，非逐打席換人演出：不會有「第8局2出局換代打」的實際場面，也不影響先發該場其他成績；此為盒分級架構下的必然折衷（逐打席化屬引擎級改寫，未做）。
- **④ 隊長比賽加成走既有近戰抗壓管道**（+4 抗壓），刻意保守以免破壞既有平衡；隊長對其他數值（守備、投手疲勞）無影響。
- **⑧ 守位缺口判定**採「捕手無人／健康野手<8／熟悉度<40」三條規則，非完整的守備適性矩陣；`autoLineup` 自動補位本身邏輯未改。
- **⑤ 協商僅降「成績類主目標」一階**，經營類目標（財務、人氣、養成）不可協商。
- **⑩ SW 僅快取 App Shell**（不預快取存檔）；存檔仍走 IndexedDB。iOS 需以 Safari「加入主畫面」才會以 standalone 模式執行。
- 承 v36：AI 隊設施上限封 2、不逐年升級其他設施；事件系統仍為 6 種（連鎖事件、C/D 級春訓事件深化、國際賽逐場化仍列 backlog）。

### 測試與驗收（v37）
- 回歸 **571 / 0**（自 546 新增 25 項 v37 斷言：③人氣鎖定與 AI 隨機、②逐屬性預估與投影邊界、④隊長任命/一季一次/抗壓加成值、⑤協商降階與一季一次、⑥板凳專員辨識/近戰nudge/出賽入帳/非近戰不發動、①交流賽與行銷企劃一季一次與扣款、⑧守位缺口偵測與無捕手情境、⑦階段標籤、⑨獎項球隊名欄位）。
  - 註：v37 斷言區塊開頭以 `newGame` 重開乾淨局，因先前章節測試已模擬多年、人氣等狀態早已漂移。
- 15 年 smoke ＋全畫面渲染全過。
- **整合單檔本身**：抽出模組區經 `node --check` 語法驗證＋功能驗證 8/8（人氣35、隊長、逐屬性預估、C/D交流賽、協商函式、守位缺口、板凳專員、獎項球隊名）全過。

---

## 第三十八節：v38「逐屬性潛力 ＋ 事件系統深化 ＋ 國際賽逐場化 ＋ C/D 國家互動」

### 開發背景
v37 交付後 Mars 指示「暫時不要再輸出 PWA 檔，先把未完成的任務完成」。經盤點，12 階段路線圖已於 v37 全數完成，
真正未完成者為 backlog 第四梯次。Mars 拍板 v38 範圍為：**逐屬性潛力＋事件系統深化＋國際賽逐場化**，
並就 C/D 級國家指示：「**不需要春訓**，已有行銷企劃、交流賽，補足**與 C/D 國家的互動**即可」。
國際賽逐場化的玩家介入程度由 Mars 拍板為「**選人＋排陣（兼任國家隊管理層）**」。

### 診斷紀錄（誠實記載「原本是什麼」）

| 項目 | 原本的實作（根因） | v38 做法 |
|---|---|---|
| ① 逐屬性潛力 | 全部屬性共用單一 `p.potential` 作天花板，`ageAdjustRating(rating, p.potential, …)` 讓每項能力朝同一數字收斂 → 球探報告的逐屬性預估只能「等位移」推估，屬性相對高低永遠不變（v37 已知簡化） | 每位球員生成「工具型態」＋逐屬性天花板 `p.pots{}`，以核心屬性歸零校正確保總潛力語意不變 |
| ② 事件系統 | `EVENT_DEFS` 僅 6 種 4 類，事件單發、選擇不留後果 | 擴充至 **16 種 6 類**（新增媒體球迷／國際），並導入**連鎖事件**（5 條後果鏈） |
| ③ 國際賽 | `runIntlTournament()` 一次跑完 40 國：小組賽只用實力值擲勝負（無比分、無球員成績），淘汰賽比分是亂數，玩家只拿到一張結果報告 | 母國賽事**逐場化**（選人→排陣→逐場打），走既有 `simulateGame` 引擎產生真比分與逐場成績；其餘 39 國維持快速結算 |
| ④ C/D 互動 | v37 的交流賽／行銷企劃是「一次性擲骰」，辦完即止；`S.nationFriendship`（名門友誼）只有 B 級以上海外春訓拿得到、單一國家、效期一年 → C/D 完全沾不到長線經營 | 導入持久累積的**國家友好度 0~10**，分級解鎖（3/5/7/10） |
| 文件 | §19 進度表仍把第 10／11 階段標為未完成（實際已於 v36／v37 完成） | 已補正並註明路線圖完成、v38 起以版本號續推 |

### A. ① 逐屬性潛力（資料層地基）
- **資料**：`p.pots = { contact, power, … }`（投手另有 `breaking`＝所有變化球種共用天花板）、`p.potArch`（工具型態）、`p.potBase`（潛力錨點）。
- **工具型態**：打者 5 型（強打／巧打／速度／守備／五拍子）、投手 5 型（力量／控球／變化球／耐力／平衡），各給一組天花板偏移量，再加 ±4 個體亂數。
- **關鍵設計——核心歸零校正**：偏移量以 `trueOverall` 採計的核心屬性（打者 contact/power/eye/fielding；投手 velocity/control）平均值歸零，
  並在 20~99 夾擠後**把殘差回填給核心屬性**（`ensurePlayerPots` 內迭代 4 次），確保「核心天花板平均＝`p.potential`」。
  因此 `tradeValue`、AI 判斷、球探總評天花板全部沿用舊語意、行為不變，**只有屬性間的分佈變成差異化**。
- **相容性——惰性生成**：`ensurePlayerPots(p)` 在第一次被用到時才補算，任何來源的球員物件（舊存檔／選秀池／國際市場／退休名冊）都自動涵蓋，不需走訪式遷移；
  `p.potential` 事後被調整（選秀分級位移、國家分級、事件潛力+1）時，以 `p.potBase` 差額**同步位移**所有屬性天花板，呼叫端零修改。
- **成長**：`developPlayer` 的 19 處 `ageAdjustRating` 呼叫改吃 `potFor(p, key)`。
- **球探**：`attachScoutedEstimates` 新增 `p.scoutedPots`（逐屬性各自霧化，且不低於同屬性現況評估值）；
  `scoutedAttrRows` 優先採用，舊存檔無此欄位時退回 v37 等位移推估（畫面不開天窗）。

### B. ② 事件系統深化
- **新增 10 種事件**（總計 16）：老將帶新秀、加練成果驗收、主力低潮、媒體開砲、球迷抗議、球迷感謝祭、外籍球員水土不服、國際球探情報、設施突發故障、贊助商長約。
- **新增 2 類**：媒體球迷、國際（原為更衣室／場外／營運／球員 4 類）。
- **連鎖事件**：`def.chain(team, opt, ctx)` 回傳 `{key, afterDays, ctx}` → `scheduleEventChain` 排入 `S.eventChains`（純資料、可序列化、重載後照常引爆）。五條後果鏈：
  - 更衣室裂痕「不介入」→ 14天後「球員求去」
  - 場外風波「力挺球員」→ 20天後「媒體開砲」
  - 媒體開砲「強硬反擊」→ 15天後「球迷抗議」
  - 贊助加碼「接受」→ 30天後「贊助商長約」
  - 年輕球員頓悟「加練」→ 25天後「加練成果驗收」（成功則潛力+2，逐屬性天花板同步上調）
- **規則**：連鎖事件**不佔每季上限、不吃每日9%機率**（後果本來就該來），但仍遵守「一次一卡」與 14 天冷卻；跨季未引爆者過期丟棄。UI 標示為「🔗 後續發展」。

### C. ③ 國際賽逐場化（選人＋排陣）
- **階段機**：`stage: squad → lineup → play → report`（舊存檔無 `stage` 時視為 report，維持相容）。
- **選人**：全聯盟本土健康球員可選（不限自家），編制 24 人／投手≥9／捕手≥2／野手≥9，附「自動推薦24人」。
- **排陣**：9 棒先發打線（球員＋守位下拉，守位不可重複、必須有捕手、投手不可排入）＋先發輪值最多4人，附「自動排陣」。
- **逐場**：母國小組賽 4 場＋淘汰賽最多 3 場，每場走 `simulateGame`，對手以**影子球員**（依該國實力值校準生成、僅存活於本屆賽會物件、結算後清除、不進 `S.players`）組隊。
- **成績**：`intlCreditStats` 記入 `S.intlTournament.stats`（本屆）與 `p.intlCareer`（生涯）**專軌**，不寫入聯盟 `seasonStats/careerStats`。
- **其餘 39 國**：維持實力值快速結算；母國分組的非母國對戰於建構時一次算完。
- **防卡關**：`intlAutoResolveAll()` 一鍵模擬到落幕；`finishIntlTournament()` 若賽事未完成會自動結算後才進休賽季。
- **獎懲**：沿用 v25/v36 規則（奪冠/4強→全聯盟人氣＋明年收入加成、國手抗壓+2~3、玩家隊奪冠加碼5000萬；小組出局→國手開季低迷鎖），改由逐場結果推得。

### D. ④ 國家友好度（C/D 互動補足）
- `S.nationBonds = { 國名: 0~10 }`，累積管道：交流賽成功+2／普通+1、行銷企劃成功+1、名門友誼+3、國際賽交手過+1、事件「接家人來台」+1、「國際球探情報」+1。
- 分級解鎖：**3** 交流賽發掘當地潛力股 35%→55%／**5** 國際市場該國獨家名額+1（必出該國人選）／**7** 該國活動成本-25%／**10** 莫逆之交（每季穩定供輸該國菁英）。
- UI：C/D 活動卡顯示各國友好度與已解鎖好處，國家下拉選單直接標示友好度。

### 已知簡化（誠實記錄）
- **① 極端潛力值仍受 20~99 夾擠**：殘差回填後核心天花板平均與 `p.potential` 的偏離已壓在 ≤1，但個別屬性（如潛力93的力量型投手球速）仍會頂到 99 封頂。
- **① 工具型態不對玩家揭露**：只透過逐屬性天花板數字間接呈現（維持球探霧化的資訊設計）。
- **③ 中立場以主客交替近似**：世界賽本應中立場，但 `simulateGame` 只有主/客兩種身分，故以逐場交替代之（主場優勢+0.15分的殘留影響）。
- **③ 逐場化只做母國**：其餘 39 國之間仍是實力值擲勝負（無比分意義），淘汰賽非母國場次的比分仍為亂數產生。
- **③ 國際賽不影響傷病與疲勞**：逐場比賽不累積疲勞、不判定受傷（避免季末賽事反噬聯盟養成），僅結算抗壓成長。
- **③ 代表隊無教練團／設施加成**：合成球隊物件的 facilities 一律 0，隊長加成亦不適用（隊長是球團身份）。
- **② 連鎖只有單層**：後果事件本身不再往下鏈（`breakthroughReview`／`sponsorRenewal` 無 chain），避免無限鏈。
- **④ 友好度不會自然衰退**：一旦累積便永久保留（未做時間衰減）。
- 承 v37：代打/代跑/代守仍為機率化近戰結果調整；隊長加成走近戰抗壓管道；守位缺口為三條啟發式規則；協商僅降成績類主目標；SW 僅快取 App Shell。

### 測試與驗收（v38）
- 回歸 **636 / 0**（自 571 新增 65 項：①逐屬性天花板/工具型態/核心均值鎖定/潛力位移同步/惰性補算/值域/球探逐屬性評估、②16種6類/連鎖排程/到期引爆/不佔上限/跨季過期/留痕、③階段機/編制驗證/排陣驗證/逐場比分/專軌成績/影子球員不污染/自動模擬/不卡關/三畫面渲染、④友好度累積/上限/分級解鎖/成本折扣/獨家名額/活動卡顯示），**連跑4次全過**。
- 15 年 smoke ＋全畫面渲染全過；smoke 內三屆國際賽皆走完整互動路徑（選人→排陣→逐場）。
- **存檔安全**：比賽打到一半存檔→重載，可正確回到 `intlTournament` 畫面續打至落幕（影子球員與賽會狀態皆可序列化）。
- **整合單檔本身**：`node --check` 語法驗證＋功能驗證全過。

## 第三十九節：v39「v38 真機回饋四項：代表隊擴編 ＋ 天花板差異放大 ＋ 連鎖間隔拉開 ＋ 說明摺疊」

### 背景與範圍拍板
Mars 對 v38 真機實測回饋四點：代表隊 24 人太綁、逐屬性天花板 ±13 不夠有感、連鎖事件 14~30 天太密、畫面文字太多。原 v39 初版曾同時做「檔案瘦身（單檔壓縮）＋事件美術 SVG ＋死碼移除」，Mars 檢視後**否決該三項**（不喜歡檔案減少的內容），指示從 v38 基準重做；經逐項確認拍板：**美術取消、單檔壓縮取消、不移除任何程式碼**（含手機除錯時期加入的載入提示／錯誤攔截器／安全模式／開機逾時防呆，全數保留原樣），只保留「次要說明文字摺疊」。本節即重做後的正式 v39。

### A. ① 代表隊 30 人規則
- 常數：`INTL_SQUAD_SIZE 24→30`、`INTL_SQUAD_MIN_PITCHERS 9→14`、`INTL_SQUAD_MIN_CATCHERS=2`；其餘名額不設限。
- `intlSuggestSquad`：14 投手 → 2 捕手 → 野手保底 9 人 → 剩餘名額**全池擇優**（不分守位取綜合最強）。
- `intlSquadIssues` 沿用常數自動更新提示文；「野手不足 9 人」守衛保留（打線可行性底線）。
- UI：候選清單 60→90 名；「自動推薦」按鈕與各處文案改用 `${INTL_SQUAD_SIZE}` 動態值（不再硬編碼 24）。
- 此組規則將於 v40「國際賽教練自動選人」直接沿用為自動入選約束。

### B. ② 天花板差異放大＋高段軟上限
- 工具型態偏移振幅 **±13→±18**（各型態主屬性 18、副屬性等比例放大），個體噪音 **±4→±5**。
- 新增 `softPotClamp(v)`：`POT_SOFT=90` 起算、超出部分以 `POT_SOFT_RATE=0.5` 壓縮——**要拿到 99 需壓縮前值 ≥108**。回應 Mars「不是人人都能頂到 99，要區隔開」：潛力 88 的力量型投手，最強屬性頂到 99 者僅約 2~3 成，其餘落在 88~98 形成階梯。
- 殘差回填改為**只灌給仍有空間的核心屬性**（已頂 99 或壓至下限者跳過，且按名額比例分攤），迭代 4→6 次；200 名抽樣核心天花板平均偏離 `p.potential` 仍 ≤1，交易估值／AI／球探總評語意不變。

### C. ③ 連鎖事件間隔拉開＋跨季存活一次
- 五條連鎖間隔：裂痕求去 14→**25**、力挺挨轟 20→**35**、反擊抗議 15→**28**、加練驗收 25→**45**、贊助長約 30→**55** 天。
- 跨季規則從「一律過期丟棄」改為**存活一次**：季末未引爆的連鎖以「排定日−本季賽程天數」換算剩餘天數帶進新球季，標 `carried:true`；第二次跨季仍未引爆才丟棄。排程結構 `S.eventChains=[{key,day,ctx,year,carried?}]` 仍為純資料、可序列化。

### D. ⑥ 次要說明文字摺疊
- 新增 `foldNote(html,label)`：以 `<details class="fold"><summary>詳情</summary>…</details>` 包裹，預設收合、點擊展開；CSS `▸/▾` 箭頭、字色繼承＋降透明度以同時適配深淺底卡片。
- 共 **19 處**規則說明類段落收進摺疊：選秀談約規則、教練/球探談約規則×2、到期續約清單說明、行銷說明、收入來源說明、傷兵說明、春訓介紹、春訓★專長與連動說明×2、存檔槽位說明、傳承條件、KPI 協商、風聲插隊/慫恿說明、交易評估說明、經紀人事務所說明、國際賽召集說明、板凳專員說明、打線排序說明。
- 收摺原則：**只收「規則說明」**；狀態、結果、警告類訊息（如安全模式、傷病通知、赤字警示）維持直接顯示。

### 已知簡化（誠實記錄）
- **② potBase 事後位移不重套軟上限**：潛力事後 +N 時全屬性等量位移（硬夾 20~99），已壓縮過的值不重新計算軟上限曲線。
- **② 高潛力平衡型仍可能多項逼近 99**：平衡型偏移為 0，潛力 97+ 者核心屬性天生集中於高段（語意上合理，非 bug）。
- **③ 跨季換算以本季長度近似上季長度**：賽程長度逐年幾乎不變，誤差可忽略。
- **⑥ 摺疊狀態不持久化**：`<details>` 開合不記進存檔，重新渲染即回到預設收合。
- 承 v38 各項（中立場近似、逐場化只做母國、國際賽不影響傷疲、單層連鎖、友好度不衰退）不變。

### 測試與驗收（v39）
- 回歸 **660 / 0**（自 636 新增 24 項：①編制常數/自動推薦30人/投手14/捕手2/野手保底/投手不足擋下/候選90/按鈕文案、②振幅18/軟上限參數/壓縮曲線/均值鎖定/潛力88頂規稀有化/屬性差異>18、③五條間隔/跨季存活一次/二次跨季丟棄、⑥foldNote結構/無模板逸出/行銷與經紀人畫面摺疊；另修訂 3 項受規則變更影響的 v38 斷言），**連跑 4 次全綠**。
- 15 年 smoke ＋全畫面渲染全綠。
- **整合單檔本身**：與模組版相同原始碼直接串接（**無壓縮、完整可讀**），通過 `node --check` ＋同套回歸與 smoke。

## 第三十九之一節：v39.1「熱修：第1季休賽季選秀被開幕選秀殘骸吞掉」

### 真機回報與症狀
Mars 回報：**第二年選秀都會自動選一個人後就放棄選秀**——進入第二屆選秀畫面時，直接看到「選秀會結束」摘要（顯示的其實是開幕選秀的舊成果：選進 1 人＋放棄其餘名額的紀錄），沒有任何選人機會就結束了。

### 根因診斷（node harness 重現）
時間軸上兩屆選秀撞在同一個 `seasonYear`：
1. 開局流程：開幕選秀 → `finishDraft()` 設 `S.draftDoneYear = 1`。
2. `beginFirstSeason()` **刻意不跑年度轉換**（第1季賽程在 `newGame()` 已建好），因此 `seasonYear` 整季維持 1、`S.draft` 物件（開幕選秀、已結束）一路殘留在狀態裡（`finalizeNewSeason` 才會歸還 `S.draft=null`，而它整個第1季都沒被呼叫）。
3. 第1季休賽季 → `proceedToDraft()` 的 **v35 重入守衛**（「本年度已辦過選秀→直接顯示結束摘要，不重辦」）判定 `draftDoneYear(1)===seasonYear(1)` 且 `S.draft` 存在且已結束 → 誤判成立，直接渲染開幕選秀的舊摘要，`startDraft()` 從未執行。**第二屆選秀被整個吞掉（全聯盟都沒選，不只玩家）。**
4. 附帶影響：`tradeWindowOpen()` 的休賽季判斷 `draftDoneYear===seasonYear` 在第1季休賽季**一開始就成立**，交易窗口在選秀前提早開放，違反 v32「選秀會結束後才重新開放」的語意。
5. 只有這一屆會撞：從第2季休賽季起 `seasonYear` 已遞增、`S.draft` 已被 `finalizeNewSeason` 歸還，守衛運作正常——與 Mars「第二年」的回報完全吻合。
6. 測試為何沒抓到：smoke 逐年一律 `confirmSkipAllRemaining()` 跳過選秀且未斷言選秀新鮮度；在殘骸上跳過只是把 pickIndex 推出界後再次收尾，表面無異狀。

### 修法（三處，均帶 v39.1 註解）
- `beginFirstSeason()`（01）：開季翻頁時 `S.draft = null; S.draftDoneYear = 0;`——開幕選秀殘骸清除，第1季休賽季可正常舉辦自己的選秀；`draftDoneYear=0` 同步回復 v32 交易窗口語意（第1季休賽季需等當年那屆選秀結束才開放）。
- `startDraft()`（01）：選秀物件新增 `opening: !S.gameStarted` 標記，明示「開幕選秀」與「休賽季選秀」的身份差異，供守衛與遷移辨識。
- `hydrateLoadedState()`（06）：**舊檔遷移**——載入存檔時若「已開打＋仍在第1年＋掛著已結束且**無 opening 欄位**的選秀物件」，即為舊版 bug 殘骸 → 清除並歸零 `draftDoneYear`，讓玩家重載後能正常補辦第二屆選秀。新版辦完的休賽季選秀一律帶 `opening:false`，不會被誤清。

### 已知簡化（誠實記錄）
- **舊檔補辦選秀的時點**：帶殘骸的舊存檔需「重新載入」才觸發遷移；已用舊版程式把摘要按過去、進到第2年的存檔，第二屆選秀的缺額無法回溯補辦（該年全聯盟共同缺一屆新秀，競爭上仍屬公平）。
- 其餘承 v39 各項不變。

### 測試與驗收（v39.1）
- 回歸 **673 / 0**（自 660 新增 13 項：開幕選秀 opening 標記／開季翻頁清殘骸／選秀前交易窗口關閉／第1季休賽季選秀正常開打且停在玩家順位／互動選人談約成交後選秀持續進行（不再一人後直接結束）／選秀結束後交易窗口重開／年度轉換正常／舊檔殘骸遷移清除／新版選秀不被誤清 等），**連跑 4 次全綠**。
- 15 年 smoke 新增**逐年守衛**：每年休賽季斷言選秀「active 且停在玩家順位」——此守衛在修正前的程式上會於第 1 年即刻紅燈（防再犯哨）。全 15 年綠。
- **整合單檔本身**：同原始碼直接串接（無壓縮、完整可讀），通過 `node --check` ＋同套回歸與 smoke。

## §40 v40：真機修正×2＋A案分頁去雜亂＋④守位硬缺口＋⑤權責下放教練（2026-07）

### 40.1 真機修正A：談判畫面「球探完整報告」徽章空白（v29 起潛伏）
**根因**：`.gradebadge` 基礎樣式是 24×24 圓形白字（為單字母等級章設計）；選秀/自由球員/國際球員卡都包在 `.draftgrades` 容器（覆寫為膠囊、width:auto）故正常，唯獨 v29 的 `negotiationScoutBlock()` 把「現況/預測天花板」兩枚徽章放在無 class 的普通 div——文字被壓進 24px 圓圈、溢出直排、白字印在牛皮紙底＝肉眼不可見。**修法**：容器補上 `class="draftgrades"`（含 flex-wrap 容錯），四種談判（新秀/續約/FA/國際）共用同函式一併修復。回歸新增斷言鎖住容器存在。

### 40.2 真機修正B：玩家隊開局「訓練基地有等級」（teamSelect 路徑白手起家破功）
**根因**：`newGame()` 在玩家選隊**之前**呼叫 `ensureAllFinance()→ensureFacilities()`，當下 `userTeamId` 仍為 null，`personaFacilityRoll` 的玩家判定不成立→全聯盟（含玩家日後選中那隊）都被依性格擲了 0~2 級起始設施。自訂隊名路徑（開局即 T0）不受影響，故此 bug 只在「從八隊中挑一隊」流程出現。**修法**：`pickTeam()` 選定後呼叫新函式 `resetUserFacilities()` 將六類設施歸零、球場維持 Lv1。**已知簡化**：舊存檔不回溯歸零（無法區分免費擲骰與玩家付費升級的等級）。

### 40.3 A案分頁（Mars 拍板）：uiTabs 輕量分頁元件
設計原則：**所有面板同時渲染進 DOM、僅以 CSS class 切換**——換頁不重繪、表單輸入不遺失、既有 getElementById 掛線邏輯零改動（元素恆存在，掛線不落空）。分頁停留記在 `UI.tabs[key]`（不入存檔）；badge 為頁籤紅點數字。`wireUiTabs()` 統一在 `render()` 尾端掛線（try/catch 包覆，不影響開機防護）。
- **主控台四分頁**：⚾賽況（比分/上一戰/模擬按鈕/擋模擬警示/春訓卡）｜📋待辦（KPI/事件/風聲/AI提案/手術/傷兵/特訓/球探報告/各式提醒，紅點=決策卡估數）｜📰新聞（跑馬燈+贊助商）｜📂選單（全部導覽與重置）。
- **財務四分頁**：📊總覽（預算/預估損益/財務提醒）｜🎟️票價｜📺合約（轉播·贊助）｜📜上季報告。
- **談判三分頁**：💰談判桌（出價操作/提案紀錄）｜📋球探報告｜🕵️情報（經紀人/事務所/新秀薪資上限）。計分板常駐頁首。
**已知簡化**：待辦紅點以卡片 class 估算、非精確決策計數；分頁停留狀態重開回預設頁。

### 40.4 選前資訊完整化（根治「開盲盒」）
與談判桌**同一份**資料來源，決策前即可全覽：選秀卡與自由球員卡補「📈 生涯階段」；國際球員卡逐項能力改用共用 `scoutedAttrRows`（現在(評估)→該項預估天花板），並明示與談判桌同源。選秀卡原有的逐項天花板列、成熟度、信心度不變。

### 40.5 v40④：守位缺口只在「真的湊不出合法陣容」時中斷
新增 `lineupHardGaps()`：僅 (a)健康野手<8 (b)無人可蹲捕 (c)強制填補後仍有守位無人 三種**硬缺口**會逐日中斷連續模擬；「僅能勉強補位」等軟缺口不再中斷，改由打線頁警告卡顯示（`lineupPositionGaps` 原函式保留）。

### 40.6 v40⑤：權責下放教練
- **打線交給教練（預設）**：`team.lineupMode="coach"|"manual"`；教練模式下 `simulateDay` 開打前呼叫 `coachDailyLineup()` 依方針重排＋`coachAssignBenchRoles()` 指派板凳專員。**戰術方針6種**（均衡/強攻火力/速度小球/上壘至上/守備優先/手感優先，`TACTICS_OFFENSE` 各帶評分函式）×**輪休策略5種**（不輪休/老將輪休/狀態保護/全員輪替/傷病防護，`TACTICS_REST`）。輪休以確定性雜湊近似「每N天」；排除後湊不滿9人自動放棄當日輪休（開機防護：不因輪休打不了球）。`autoLineupBy()` 為 autoLineup 的加權泛化版，原 autoLineup 保留不動（AI隊沿用）。GM 可隨時切回手排固定打線。
- **板凳專員改教練指派**：教練模式每日依專長自動指派代打/代跑/代守，UI 改唯讀顯示；手排模式維持 v37⑥ 手動下拉（`setBenchRole` 保留）。
- **隊長改教練提名制**：`coachCaptainCandidates()` 依忠誠/士氣/抗壓/實力/資歷綜合評分提出最多3位人選，GM 在打線頁圈選任命（`appointCaptain` 一季一次限制沿用）；球員詳情頁任命入口改依提名 gating。
- **國際賽玩家不選人**：選人畫面預設顯示國家隊教練擇優的30人名單（沿用 v39 的 30/14投/2捕約束＝`intlSuggestSquad`），本隊入選者⭐標示並提示；一鍵 `intlAutoSelectSquad()` 出征。**國家隊配教練**：`pickNationalCoach()` 借調全聯盟教學評價最高的一軍總教練（僅敘事與名單責任歸屬，無數值加成）。原手動選人介面完整保留為「進階：手動調整名單」備援。
**已知簡化**：教練模式下玩家對打線的當日手動微調隔天會被教練重排（要固定請切手排）；輪休為近似輪替非出賽數統計；natCoach 無加成、借調不影響原隊；隊長提名為評分公式、未含更衣室化學。

### 40.7 測試
回歸 673→**701 項**（+28：徽章容器/設施歸零/硬缺口/6方針合法打線/輪休排除/板凳教練指派/隊長提名/國際賽自動30人+教練/uiTabs/三畫面分頁渲染/選前生涯階段/掛線不落空守衛），連跑4次全綠；15年 smoke 全綠（全畫面渲染涵蓋分頁版）。整合單檔本身另以同套回歸+smoke+node --check 驗證通過。三個受規格變更影響的舊斷言（國際賽選人畫面）已更新指向新語意並保留手動備援檢查。


---

## §41 北極星第一梯次：GM／教練分權（v41）

> **本版定位**：北極星憲法收錄後的第一個實作版本。核心命題——「GM 的權力邊界」：把「每天排打線」從玩家的義務變成玩家的**選擇**，並讓每一次越權都有帳單。對應憲法 §5（GM/教練分權）、§6.2（需求單）、§14.3（純GM驗收門檻）。

### 41.1 六項根因診斷（v40 真機回饋 → 系統性病因）
1. **權責混沌**：v40 的 lineupMode 只是 UI 開關，沒有身分敘事——玩家不知道自己「是誰」，導致手排變成義務而非選擇。
2. **教練是空殼**：教練只有 teaching 數字，沒有哲學、沒有意見、不會頂嘴——委任出去像丟給自動排序器，不像交給一個「人」。
3. **缺口靠中斷惩罰玩家**：v40 硬缺口逐次中斷模擬，把「名單管理失誤」變成流程騷擾，而不是遊戲內的敘事後果。
4. **遊戲不推玩家**：所有補強動機都來自玩家自省，系統從不主動開口——沒有需求單，就沒有「被現場需要」的張力。
5. **視覺與邏輯耦合**：emoji 直接寫死在模板字串裡，未來要換美術資產得全文搜尋替換。
6. **民意無處落地**：球迷情緒只存在於新聞文案，對經營結果毫無後果。

### 41.2 Mars 拍板紀錄
①三招提示**首次強制**閱讀（之後不擋）；②接管時效＝**本季剩餘**，跨季須續期（再付代價）；③客串守位懲罰＝**×0.6**；④教練哲學「**需要更多變化性**」→ 由提案的 3 種擴為 **6 種**；⑤球迷耐心**含 fanPatience 先埋**（單一數字、唯一出口）。其餘依提案建議值執行。

### 41.3 十項設計實作
- **① 開局身分模式**：`S.gameMode = "pure_gm" | "gm_coach"`。開局（newGame 指定隊名／pickTeam）在休賽季狀態備妥後蓋上 `gameModePick` 身分選擇畫面。**不對稱切換**：GM兼教練→純GM 走 `delegateLineup()` 免費正面事件（教練信任+10、貼 `delegates` 標籤）；反向只能「接管」。`canManualLineup()` 為手排權的單一判定口；純GM未接管時，打線頁的手排入口**不存在**（不是 disable）。舊存檔一律遷移為 `gm_coach`（v40 玩家本來就在手排，零破壞）。
- **② 接管 Takeover**：`startTakeover()` → 教練信任 -30、媒體開炮、貼 `hands_on` 標籤（留存不清零）、lineupMode=manual。首次發動強制閱讀「三招提示」（調方針／換教練／移走爛選項）。時效本季剩餘；跨季 `tickTakeover()` 到期 → 待辦決策卡「續期（信任-15、高層觀感-3）或還權（信任+15）」。
- **③ 需求單核心迴圈**：`scanTeamGaps()`（守位硬缺／輪值<4／牛棚<3／打線火力<52）→ `genDemand()`（教練專長×哲學口味×缺口；**爛教練 teaching<45 → 爛需求**：門檻誇大、一律 high）→ 三鍵 `resolveDemand()`：接受（追蹤）／駁回（信任-5，累積3次→不滿事件）／協商（一次限，成功率＝受教性×0.6＋GM聲望/250，成功→門檻-10、優先度降、期限+15）。`checkDemandFulfilled()` 以**名單快照**判定新戰力達標 → 信任+8；過期未回應 → 信任-8。觸發：開季診斷、交易截止前15天、30天以上重傷、硬缺口。需求卡掛既有📋待辦分頁，open 卡逐卡計入紅點。
- **④ 守位缺口三層防線**：軟缺口→打線頁警告（現況不動）；硬缺口→**不再中斷**，改為 high/30天需求單＋客串頂替（無本職捕手蹲捕→該格守備×0.6 接入 teamDefenseRating，其他守位沿用既有移防家族懲罰）＋22%機率捕逸/失誤敘事新聞；只有**數學無解**（健康野手<9）才保留中斷。
- **⑤ 教練哲學×6＋執行度**：`COACH_ARCHETYPES`＝小球流／轟炸流／投手王國流／均衡流／養成流／老將流，各帶預設方針與六方針認同度表。`agreement`（進攻75%＋輪休25%）→ `execution = clamp(0.5 + ag×0.25 + (teaching-50)/100×0.15 + (trust-50)/100×0.2, 0.5, 1.0)`。**確定性折射門檻**：≥0.78 完全照辦；0.62〜0.78 輪休改用哲學預設；<0.62 教練我行我素。`coachDailyLineup`／`coachRestSet` 僅換輸入來源改讀 `effTacticsOf()`，autoLineupBy 內部一行不改。GM 硬壓相斥方針 → 每日磨教練信任 → <35 抱怨新聞、<18 求去新聞＋史冊。
- **⑥ 百年史冊**：`chronicle(type,text,meta)` 單一寫入口、append-only（封頂5000）。寫入點：季記／退休／總教練聘任／接管三態／重大交易／需求冷處理。本版只寫不顯，回顧畫面 v45。
- **⑦ 資產插槽登錄表**：`ASSET_SLOTS` 九插槽＋`assetSlot(key)` 單一取值口（有資產回資產、無資產回 emoji fallback、未知鍵回空字串、永不 throw）；`S.assets` 為 v45+ 資產包掛載點；body 掛 `data-skin`。示範遷移：主控台⚾賽況頁籤改走 `team.logo` 插槽（fallback 即⚾，畫面與 v40 完全相同）。
- **⑧ 球迷耐心（先埋）**：`S.fanPatience` 0〜100 起始60。輸入：勝負 vs KPI 期待門檻（diff→0.50〜0.58）、票價>600 緩慢消磨、送走 trueOverall≥80 球星 -6。**唯一出口**：季末考核負向扣分 × `fanPatienceOwnerMult()`（≤25→1.3／≤40→1.15／≥75→0.85）。季末向50回中20%。三維度化排 v44。
- **⑨ 測試**：回歸 701→**776 項**（+75），15年 smoke 全綠，**新增 smoke_puregm.js**（憲法 §14.3 驗收門檻）：純GM 15 年全自動，斷言無荒謬陣容（打線9人守位不重複／頂級野手未冷凍／守位錯置≤3格）、戰績合理、身分不漂移、接管完整生命週期演練（第3年接管→跨季到期→續期→還權）。整合單檔本身以同套測試驗證通過。
- **⑩ 歸檔**：v39 及以前交付檔未在本次會話中提供，`archive_v39_and_before.zip` 待 Mars 補上傳後產出（或由 Mars 本地歸檔）。

### 41.4 整合點清單（最小侵入）
`03`：coachDailyLineup／coachRestSet 換讀 effTactics（各一行）、teamDefenseRating 乘 improvisePenaltyFor、simulateDay 硬缺口區塊改三層防線＋尾端掛 tickV41Daily、檔尾附加 v41 核心區塊（約400行）。`04`：newGame 狀態容器＋gameModePick 蓋屏、settleSeasonKPI 插入耐心乘數一行。`01`：enterOffseason 掛 v41OnSeasonEnd、hireCoachCandidate 補人格＋史冊、executeTrade 掛 v41OnTradeExecuted。`06`：hydrate 掛 ensureV41、renderLineup 身分感知重構、附錄三個新畫面/卡片。`05`：gameModePick 路由、待辦掛需求/接管卡＋掛線、data-skin、插槽示範。

### 41.5 已知簡化（誠實記錄）
- **S1** `hands_on`／`delegates` 標籤僅記錄＋新聞；教練市場的實際影響（好教練拒聘）排 v42。求去僅到新聞層，實際離隊 v42。
- **S2** 接管續期的「再付聲望」以高層觀感（gmCareer.trust）-3 近似；聲望系統本體不動。
- **S3** 折射為確定性門檻制——類別型方針無法連續 blend；認同度為距離公式，無更衣室化學修正。
- **S4** 客串懲罰 ×0.6 為經驗值（Mars 拍板），依 smoke 戰績分布校準；未逐守位細分。
- **S5** 需求單掛既有待辦分頁；郵件中樞完全體 v43+。紅點＝既有 regex 估算＋需求 open 卡逐卡精確；計數管線全面重構延後。
- **S6** fanPatience 單維；明星異動輸入僅涵蓋玩家交易路徑（AI互易不觸發）。三維度＋更多出口 v44。
- **S7** 模式選擇畫面重載後以 gm_coach 續行（hydrate 不回 gameModePick）；可於打線頁一鍵放權轉純GM。
- **S8** 教練自動到期替換的史冊記載 v42 補；本版一軍總教練異動史冊僅涵蓋玩家主動聘任。
- **S9** 承 v40 以前的各項簡化不變。

### 41.6 測試
回歸 701→**776 項**（+75：模式/遷移/放權/接管生命週期/哲學6種折射/需求三鍵與達成過期/爛教練/三層防線/數學無解中斷/史冊/插槽/耐心乘數/純GM畫面入口不存在），連跑全綠；15年 smoke（GM兼教練基準）全綠；**新增純GM 15年 smoke** 全綠（含接管跨季生命週期演練）。整合單檔另以 node --check＋同套回歸＋雙 smoke 驗證通過，主 script 與模組原文位元組一致（不壓縮）。

---

## §42 北極星第二梯次：教練市場與標籤後果（v42）

### 42.1 定位
承 §41 已知簡化 **S1**（標籤市場影響）、**S2/S8**（求去與到期史冊）落地，讓「教練是人不是函式」再進一步：教練是**有限資源**、會**挑雇主**、會**真的求去離隊**。本梯次同時吸收 Mars 於 v42 追加的四項真機需求（釋出後拉人指引、年輕人上場機會、輪替/戰術更多變化、反應/互動更多樣）。

### 42.2 Mars 拍板
- 併入 v42 的四項新需求：①釋出一軍選手後要知道從二軍拉誰上來 ②讓年輕選手多上場機會的戰術 ③輪替/戰術方式更多變化 ④反應、互動更多樣。
- 原八項範圍與五拍板點採提案建議值：慰留兩選項（加薪30%＋還權承諾）都上／AI搶人機率25%／hands_on 衰減永留疤（地板-4）／代理教練 teaching-10／每任教練生涯僅可慰留 1 次。

### 42.3 設計實作（十二項）
- **① 聯盟教練市場池 `S.coachMarket.pool`**：教練是聯盟共用有限資源。退役好手 `harvestRetireesToMarket()`（不消耗共享RNG）＋新面孔 `refillCoachMarket()`（呼叫共享 generateCoach，僅在玩家開啟選人器時補，不在確定性模擬路徑）。選人器改抽市場池 `generateCoachCandidatesV42()`；池空退回 v41 舊制（畫面永不開天窗）。**稀缺性**：聘走即 `v42OnCoachHired()` 從池移除。搶手教練（teaching≥65）每休賽季 25%（`V42_POACH_RATE`）被 AI 隊搶走。
- **② 意願系統 `willingnessOf()`**：基礎55＋標籤/戰績/舊帳修正。**只有 teaching≥60 的好教練會挑雇主**（爛教練不挑，延續「爛教練開爛單」美學）。`delegates` +15（甜頭）、`hands_on` 罰則（帳單）、近三季勝率±10、舊帳-30衰減。<40 拒絕（`v42HireGate` 攔下，附理由）／40〜60 要價+25%／≥60 照原價。**不對稱**：無標籤零罰則，新玩家永不受罰。
- **③ 教練實際求去離隊**：`tickCoachResignation()` 每日心跳——信任<18 起算，14天內未回升至25 → 遞辭呈事件（7天時效）。慰留（生涯一次）：`v42RetainCoach('pay')` 加薪30%／`v42RetainCoach('promise')` 還權承諾（立即結束接管，60天內再接管→信任歸零，`tickV42Daily` 背信判定）／`accept` 接受辭呈。離隊 → `v42CoachLeaves()`：原教練進市場帶「不歡而散」舊帳（隔離RNG）、板凳教練 teaching-10 代理至季末（人格以隔離RNG填、不呼叫 ensureCoachPersona）。
- **④ 續約要價受信任＋約滿史冊**：`v42RenewTrustMult()` 一軍總教練信任≥60 ×0.95／40〜60 ×1.15／<40 ×1.35，插 `staffMarketSalary` 一行。續約成交/破局/不續三路徑均補 `chronicle`（補 S9）。
- **⑤ hands_on 衰減表**：`handsOnMarketPenalty()` 未接管季數 0/1/2/3+ → -20/-14/-8/-4（**永留疤地板-4**）；`delegates` 不衰減（好名聲是複利）。
- **⑥ 史冊教練篇**：聘任/辭呈/慰留/離隊/約滿/AI搶人六型皆入 chronicle（type:"coach"），為 v45 回顧畫面備料。
- **⑦ UI 最小整合**：候選卡加意願徽章（願意/要價/拒絕＋理由）＋薪資；辭呈決策卡 `renderResignationCard`、拉人提示卡 `renderCallupHintCard`、釋出確認卡嵌影響評估 `renderReleaseImpact` 皆掛主控台待辦頁，複用既有卡片框架，不新增畫面（郵件中樞 v43 再收攏）。
- **⑧ 升級鏈**：`ensureV42` 串在 `ensureV41` 尾端（…→ensureV41→ensureV42）；舊檔補 `S.v42` 容器＋空 `coachMarket`＋`team.tactics.rotation="five"`，零破壞。
- **⑨ 釋出→二軍拉人建議**（Mars 需求①）：`releaseImpactOf()` 在釋出前算出守位/投手深度缺口；`suggestCallups()` 依同守位/同投手角色/年輕潛力給二軍前三推薦（帶適配分數與理由）。釋出確認卡即時顯示「釋出後會缺什麼、二軍誰能補」；釋出後留拉人提示卡（3天自動收起），一鍵升上一軍。
- **⑩ 青年育成戰術**（Mars 需求②）：進攻方針新增 `youth`「青年育成」（25歲以下潛力新秀優先上場）；養成流哲學我行我素預設改為 youth、認同度最高，老將流極度排斥（aff.youth 0.15）。季末 `v42YouthGrowthOnSeasonEnd()`：整季出賽≥60場的25歲以下球員隨機一項核心屬性+1（潛力天花板內、隔離RNG）。
- **⑪ 輪替/戰術更多變化**（Mars 需求③）：輪休策略新增 `rookie`「新秀保護」（25歲以下野手約每6天輪休）；新增**投手輪值方針** `TACTICS_ROTATION`×3（標準/四本柱強投+疲勞/六人養護-疲勞），`v42ApplyRotationPolicy()` 裁切自動輪值人數、`v42RotFatigueDelta()` 調整先發疲勞，掛打線頁選單（v42 簡化：不入折射系統，見 §42.5）。
- **⑫ 反應/互動更多樣**（Mars 需求④）：`V42_QUOTES` 語錄庫（抱怨/求去/辭呈/釋出反應/升降/釋出球員七類，每類3〜5變體）＋`v42Quote()`（隔離RNG抽選、填入名字、未知key回空字串不炸）。抱怨/求去新聞、升上一軍/下放/釋出球員事件全部改走語錄庫。

### 42.4 整合點清單（最小侵入）
`03`：TACTICS_OFFENSE 加 youth、TACTICS_REST 加 rookie、coachRestSet 加 rookie 分支、YOUTH_DEV/VETERAN 認同度更新、getRotationPitchers 套輪值方針、先發疲勞加 rotation delta、抱怨/求去新聞改語錄、simulateDay 尾端掛 tickV42Daily、ensureV41 尾端掛 ensureV42、檔尾附加 v42 核心區塊（含隔離RNG、約350行）。`01`：openCoachPicker 改抽市場池、hireCoachCandidate 加意願閘門＋聘後移池、promote/demote/releaseActivePlayer 掛語錄與影響評估、enterOffseason 掛 v42OnSeasonEnd。`02`：staffMarketSalary 插信任乘數一行、declineStaffRenewal/finalizeStaffRenewal/failStaffRenewal 補史冊。`06`：候選卡意願徽章、戰術面板加輪值選單＋onchange、釋出卡嵌影響評估、檔尾附加 v42 UI 區塊。`05`：待辦頁掛辭呈卡/拉人提示卡、demand-btn 後掛 wireV42Cards。

### 42.5 已知簡化（誠實記錄）
- **T1** 投手輪值方針（⑪）為 GM/教練共治層，v42 **不納入教練折射系統**（不受執行度影響）；納入折射待 v43+。
- **T2** 意願系統與市場池僅適用**一軍總教練**；一般教練組與二軍沿用 v41/舊制自動處理。
- **T3** 教練市場「新面孔」在玩家開啟選人器時才以共享 generateCoach 補足——這是 UI 動作、不在確定性模擬路徑上，故安全；季末市場運轉只做 AI 搶人＋退役 harvest（皆隔離RNG）。
- **T4** 慰留代價：加薪版信任回補至40地板、還權承諾版回補至35；未做多年期結構化談判（用「+X%」近似，承 v41 S2 精神）。
- **T5** 代理教練季末一律卸任懸缺（走既有空缺機制）；代理→轉正的續任談判未特化。
- **T6** 青年育成成長為季末單屬性+1，非連續模型；未做出場時間對衰退曲線的反向影響。
- **T7** 承 v41 及以前各項簡化不變（fanPatience 單維 v44、郵件中樞 v43、史冊回顧畫面 v45）。

### 42.6 亂數紀律（重要工程決策）
v42 的敘事型抽選（語錄、AI搶人、育成屬性、離隊教練欄位、代理教練姓名/人格）一律走**自有隔離 PRNG**（`v42rng`/`v42Int`，xorshift、依 seasonYear/currentDay/demandSeq 播種），**完全不動用共享 `Math.random`**——確保回歸沙盒與比賽模擬的既有亂數序列不受 v42 新事件影響（記憶錨點：回歸沙盒與宿主共用 Math）。同時，回歸與雙 smoke 的沙盒 `Math.random` 改為**固定種子覆寫**，使結構不變式測試跨程序完全可重現（測試基礎設施強化，不影響遊戲程式）。

### 42.7 測試
回歸 776→**817 項**（+41：三戰術方針就位與合法排線／意願值域與 delegates 甜頭/hands_on 只罰好教練/衰減20→4/新玩家零罰則／市場池稀缺性與聘後移池/拒絕閘門／辭呈→加薪慰留/慰留過無法二次/接受辭呈代理上任＋原教練帶舊帳進市場/還權承諾＋背信歸零／續約信任乘數／釋出影響評估與拉人建議≤3／語錄多樣化與填名與未知key不炸／升級鏈串接/隔離RNG/心跳與季末掛勾不炸），連跑三次穩定全綠；15年 smoke（GM兼教練基準）＋純GM 15年 smoke（含 v42 教練市場/辭呈生命週期演練）皆連跑三次全綠。整合單檔另以 node --check＋同套 817 回歸驗證通過，主 script 與模組原文位元組一致（`\n\n` 串接、不壓縮）。

### 42.8 v42 待辦與 v41 遺留
- ⑩ `archive_v39_and_before.zip` 仍待 Mars 補上傳 v39 及以前交付檔（或本地自行歸檔）。
- R1（v41 遺留）：純GM 教練 AI 手感與需求單頻率仍待 Mars 真機把關。
- 路線（憲法附錄B）：v43 郵件中樞（教練事件收攏）／v44 球迷三維度／v45 史冊回顧畫面＋資產包掛載（S.assets）。

## §43 郵件中樞＋交易資訊補全＋掛牌系統＋純GM全教練化＋自由度提升（v43，2026-07）

### 43.1 定位
v43 承接北極星憲法附錄B「v43 郵件中樞（教練事件收攏）」，並一併落地 Mars 於本版提出的六項真機需求。核心是把 v41–v42 累積的教練/需求/市場事件**收攏進郵件中樞**，同時補齊長期缺漏的交易資訊、開放掛牌釋出、把純GM 的投打守調度**完全交給教練**，並以「選項式＋可自填數值」提升全體互動自由度。

### 43.2 Mars 拍板（六點指示）
1. **純GM 投打守全教練化**：純GM 未接管時，投手輪值也由教練安排（不只打線）；傷兵時教練從現有選手提遞補人選（不限同守位），一切由教練需求自行提出。
2. **提高全體自由度**：問題不只一個答案——採 **A案**（選項式＋可自填數值，例如交易自填現金），不採文字輸入關鍵字判讀（離線單機無語意模型，體驗差）。
3. **交易畫面雙方完整資料**：比照選秀/外籍，補年齡、薪資、合約年限、狀況、能力、潛力、特質（v36 只攤開能力數值，基本資料是漏做）。
4. **掛牌釋出系統**：AI 主動開條件（純錢/選手/選手加錢/多換一/一換多），玩家可全拒；一定要有雙方完整資料。**順位交易**因需動選秀核心，依 Mars「選項一」**排 v44**（本版先做選手＋現金自由組合）。
5. **需要決定的事直接在對話問，不放文件、未回覆要再問、不自行猜測**（已納入協作規則）。
6. **v39 及以前 Mars 自行存檔**，不再打包（v41 遺留待辦⑩解除）。

### 43.3 設計實作
- **完整球員資料元件（單一真相）**：`v43PlayerFullCardHtml(p, {scoutView})`——自家真實值、對方球探評估值（帶準確度誤差）；`v43SalaryLabel`／`v43ContractLabel`／`v43PotentialTier`／`v43BuildScoutCache`。交易雙表、掛牌報價卡、AI 主動提案卡（`renderAiProposalCardV43`）三處共用。
- **現金交易維度**：`V43_CASH_PER_VALUE`（每 1.0 交易價值≈120萬，真機可調）；`v43EvaluateTradeWithCash`（把雙方現金換算價值併入比率重判；要對方現金超過其預算直接回絕）；`v43ExecuteTradeWithCash`（球員移動沿用 executeTrade，再結算雙方預算）。交易畫面 `renderTradeBuilderV43` 加現金雙向自填輸入。
- **掛牌系統**：`S.v43.listings`／`S.v43.offers`；`v43ListPlayer`／`v43UnlistPlayer`；`v43GenerateOfferFor`（AI 依 personaTradeValue 開條件，型態 cash/player/multi/player_cash，現金受買方預算上限）；`v43TickListings`（每日每位掛牌球員 35% 機率被前五眼緣隊開價，同一目標未回應報價上限2）；`v43AcceptOffer`／`v43DeclineOffer`。UI：`renderListingScreen`（名單導航新增「掛牌交易市場」）＋報價卡 `v43OfferCardHtml`。
- **純GM 全教練化**：`coachDailyRotation`（純GM 未接管時，simulateDay 尾端由教練依綜合能力＋折射方針重排先發輪值與牛棚順序，唯讀）；輪值頁 `renderRotationCoachManaged`（手排入口不存在，只留輪值方針設定）。
- **輪值折射（解除 v42 T1）**：`v43EffRotationKey`（純GM 玩家隊：exec≥0.78 照 GM／0.62–0.78 激進方針緩和一級／<0.62 用教練哲學偏好 `v43CoachRotationPref`；**AI 隊與 GM兼教練一律照 GM，且不呼叫 headCoachOf** 以免擾動共享RNG）；`v43ApplyRotationPolicyRefracted`／`v43RotFatigueDelta` 供 getRotationPitchers 與先發疲勞使用。
- **傷兵遞補（純GM，准駁制）**：`v43MakeInjuryProposal`（先發陣容成員受傷→教練從現有健康非先發球員提前三人選，不限同守位，跨守位會註明）；`v43ResolveInjuryProposal('approve'|'next'|'dismiss')`（批准升上一軍＋信任小回／要教練換下一位口袋人選／擱置信任小扣）。
- **郵件中樞**：`S.v43.mail`＋`v43PushMail(category,title,body,meta)`（category：coach/trade/injury/system）；主控台新增「✉️ 郵件」分頁（`dashMailPanel`，帶未讀紅點 `v43UnreadMailCount`），可展開內文與內嵌「前往處理」卡（報價卡/遞補提案卡）。教練提案、掛牌報價、傷兵遞補、聯盟通知統一入口。
- **每日/升級**：`tickV43Daily`（simulateDay 尾端，全 try-catch：純GM 傷兵遞補提案＋掛牌報價）；`ensureV43`（串在 ensureV41→…→ensureV42 之後，補 S.v43 容器，零破壞）。

### 43.4 整合點清單（最小侵入）
- 03-simulation：simulateDay 尾端加 `coachDailyRotation(__ut)`（純GM）與 `tickV43Daily(results)`；getRotationPitchers 純GM 玩家隊改用折射方針；先發疲勞改吃 `v43RotFatigueDelta`；ensureV41 尾端串 ensureV43。
- 05-ui-dashboard：renderScreen 的 rotation 純GM→`renderRotationCoachManaged`、tradeBuilder→`renderTradeBuilderV43`、新增 listing 路由；dashTodoPanel 插入傷兵提案卡＋掛牌報價卡、AI 提案卡改 v43 版；主控台加「✉️ 郵件」分頁；wireV43Cards 綁定。
- 06-ui-roster：renderRosterNav 加「掛牌交易市場」。

### 43.5 已知簡化（誠實記錄）
- **U1** 順位（選秀權）交易未實作——依 Mars「選項一」排 **v44**（需動選秀核心）。本版掛牌與交易為「選手＋現金」自由組合。
- **U2** 傷兵遞補提案與純GM 輪值折射僅適用**玩家隊**；AI 隊沿用既有自動調度（不折射、不觸發人格初始化，工程安全）。
- **U3** 掛牌 AI 報價現金計價用線性當量（`V43_CASH_PER_VALUE`）近似，未做多年攤銷或帳面/實付分離。
- **U4** 郵件中樞為收攏入口＋跳轉，未做分類過濾器/搜尋/封存分頁（可排後續）。
- **U5** 自由度採 A案（選項＋自填數值）；文字自由回答（語意判讀）非離線單機可靠實作，不做。
- **U6** 承 v42 及以前各項簡化不變（fanPatience 單維 v44、史冊回顧畫面 v45、資產包 v45）。

### 43.6 亂數紀律（延續 v42 鐵則＋本版新處置）
v43 敘事型抽選（掛牌 AI 報價的型態與籌碼、每日開價機率）一律走 v42 的隔離 PRNG（`v42rng`/`v42Int`），不動共享 Math.random。本版另修一枚工程隱患：輪值折射原會對 AI 隊呼叫 `headCoachOf`→`ensureCoachPersona`，在尚未初始化人格的 AI 教練上消耗共享 Math.random，擾動全域序列致回歸偶發 3 項漂移；已改為**折射與教練排程僅對玩家隊生效**，AI 隊完全不走該路徑，817→856 連跑三次穩定全綠。

### 43.7 測試
回歸 817→**856 項**（+39：ensureV43 與升級鏈串接／完整資料卡含年齡薪資合約潛力／球探評估快取／現金換算與帶現金評估與雙方預算結算／要超額現金回絕／掛牌與重複掛牌擋下與撤牌／AI 報價型態合法與摘要／接受成交撤牌與婉拒續掛／郵件 push 與未讀計數與分類與面板／純GM 教練排輪值不炸／輪值折射低執行度偏離·高信任照辦·AI隊照GM／純GM 輪值唯讀畫面／傷兵遞補提出·換人·批准升一軍·擱置／每日心跳與掛牌市場與交易完整畫面渲染），連跑三次穩定全綠；15年 smoke（GM兼教練基準）＋純GM 15年 smoke（新增 v43 輪值教練化·掛牌成交·傷兵遞補生命週期演練）皆連跑三次全綠。整合單檔另以 node --check＋同套 856 回歸驗證通過，主 script 與模組原文位元組一致（`\n\n` 串接、不壓縮）。

### 43.8 v43 待辦與遺留
- **U1（本版最大待辦）**：選秀順位交易 → **v44**（含順位加錢加選手的掛牌條件）。
- R1（v41 遺留）：純GM 教練 AI 手感與需求單/遞補頻率仍待 Mars 真機把關。
- v43 拍板點採建議值待真機把關：現金當量 120萬/價值、AI 開價機率 35%/日、報價 10 天時效、傷兵遞補跨守位加權——手感若不對可調。
- 路線（憲法附錄B）：v44 選秀順位交易＋球迷三維度（期待/耐心/認同）／v45 史冊回顧畫面＋資產包掛載（S.assets）。

## §44 教練體諒溝通：需求單第四路徑（v44，2026-07）

### 44.1 定位
Mars 真機回饋：遊戲初期沒有太多經費與選手來源，教練提出的補強需求單只有「接受」（多半因湊不到人而過期 -8「石沉大海」）或「駁回」（-5＋連續駁回連鎖）兩條實際出路，兩條路教練都會因為「誤會 GM 沒按需求做事」而不滿。缺的是一個讓 GM **向教練說明現況**的管道——「現在是重建期／目前沒有經費／市場上沒有合適人選」等教練能夠**體諒**的內容（又是高自由度）。本版即為需求單補上第四條路徑。與 v44 路線並列（選秀順位交易 U1、球迷三維度另行拍板；本版先落地教練體諒）。

### 44.2 設計原則（憲法對齊）
- **資訊不對稱**：教練看得到帳本、戰績與市場。說辭若與真實狀態相符→查證屬實、體諒撤下需求（不罰信任、不算過期）；與帳本／戰績／市場不符→被識破（信任 -6，需求維持），不能靠謊話免費消災。
- **人的意志**：教練會識破不實說辭，也會磨損耐心——同一位教練同季第 2 次以上體諒→信任 -2，杜絕無限找藉口把需求單變成「一鍵消音」。
- **高自由度（A案／選項式）**：三顆選項按鈕（重建期／沒有經費／市場沒合適人選）。承 §43.5 U5，離線單機不做文字語意判讀，故以選項式呈現而非自由文字。

### 44.3 可信度查證（`demandExcuseCredible(team, d, reasonKey)`）
- **重建期（rebuild）** 屬實條件（任一成立）：`mandateActive()==="rebuild"`（重建委任）／一軍平均年齡 < `V44_REBUILD_YOUNG_AGE`(26.5)（年輕陣容＝實質重建）／戰績落在聯盟後 40%（`v44LeagueRankFrac`≥0.6；賽季初出賽 <15 場不以名次論斷）。
- **沒有經費（nobudget）** 屬實條件：可用預算 `team.finance.budget < V44_NOBUDGET_THRESHOLD`(3000 萬)。
- **市場沒合適人選（noplayer）** 屬實條件：`v44NoSuitablePlayer` 掃「二軍＋自由球員＋國際自由球員＋他隊掛牌」皆無人滿足需求（`v44PlayerMeetsNeed` 逐一比對守位與屬性門檻）。

### 44.4 回應機制（`resolveDemand(id,"understand",reasonKey)`）
- 屬實且本季首次體諒：信任不變、`d.status="excused"`（撤下需求、之後不判過期、不扣分），推播教練體諒語錄＋史冊＋郵件中樞（category=coach）。
- 屬實但同季重複：信任 `V44_EXCUSE_FATIGUE_TRUST`(-2)，教練「耐心磨損」語錄。
- 不實（被識破）：信任 `V44_EXCUSE_FALSE_TRUST`(-6)，需求維持 open，教練點名矛盾（帳上有錢／戰績中上游／市場明明有人）。
- 體諒次數以 `coach.excuseYear`/`coach.excuseCount` 逐季重置記錄（惰性欄位，ensureV44 不預填）。

### 44.5 整合點清單（最小侵入）
- 03-simulation.js：附加 v44 區塊（常數＋`v44PlayerMeetsNeed`/`v44NoSuitablePlayer`/`v44LeagueRankFrac`/`v44RosterAvgAge`/`demandExcuseCredible`/`V44_EXCUSE_LINES`/`v44ExcuseLine`/`ensureV44`）；`resolveDemand` 簽名加 `reasonKey` 並插入 `understand` 分支；`ensureV41` 尾端串接 `ensureV44`（升級鏈 …→ensureV43→ensureV44）。
- 06-ui-roster.js：`renderDemandCards` 需求卡增設「體諒說明」三顆選項按鈕（`data-act="understand"` data-reason=rebuild/nobudget/noplayer）。
- 05-ui-dashboard.js：`.demand-btn` 綁定改傳 `b.dataset.reason`（向後相容，其餘三鍵 reason 為 undefined）。
- sw.js：cache 版本 baseballgm-v43 → **baseballgm-v44**。

### 44.6 亂數紀律（延續 v42/v43 鐵則）
教練體諒／識破的回話語錄一律走隔離 PRNG `v42rng`/`v42Int`，不動共享 Math.random；可信度查證為純狀態判讀，無亂數。測試沙盒 Math.random 維持固定種子化以保跨程序可重現。

### 44.7 測試
回歸 856→**871 項**（+15：ensureV44 容器與升級鏈串接／需求比對屬性與先發投手守位／重建可信度（委任·年輕陣容）／體諒重建屬實撤需求信任不變／體諒次數記錄／同季第 2 次耐心 -2／謊稱沒錢被識破 -6 需求維持／沒經費屬實撤需求／市場沒人屬實撤需求／已體諒需求過期不再扣分不判過期／需求卡體諒按鈕在 DOM），連跑三次穩定全綠；15年 smoke（GM兼教練基準）＋純GM 15年 smoke（新增 v44 教練體諒屬實撤需求·識破 -6 生命週期演練）皆連跑三次全綠。整合單檔另以 node --check＋同套 871 回歸驗證通過，主 script 與六模組原文位元組一致（`\n\n` 串接、不壓縮；char 750468／byte 897455，整合 HTML byte 923251）。

### 44.8 v44 已知簡化（誠實記錄）
- **X1** 體諒為「本季撤下需求」的一次性處置；不追蹤「日後補進人選時教練回心轉意」（可排後續）。
- **X2** 可信度為狀態門檻判讀（預算線性門檻／名次比例／平均年齡），非情境語意理解；門檻值為建議值待真機把關。
- **X3** 三種說辭為固定選項；未做「自由文字說明＋語意判讀」（承 §43.5 U5，離線單機不可靠）。
- **X4** 耐心磨損以「同教練同季體諒次數 ≥1」為界、固定 -2；未做隨次數遞增的疲勞曲線（可排後續）。

### 44.9 v44 拍板點採建議值待真機把關
- 沒經費門檻 `V44_NOBUDGET_THRESHOLD`=3000 萬、重建年輕線 `V44_REBUILD_YOUNG_AGE`=26.5、後段班比例 0.6、耐心磨損 -2、識破 -6——手感若不對可調。
- 設計取捨（可信度查證＝說辭需屬實才免罰）為誠實對齊憲法之選擇；若 Mars 偏好「說明一律被體諒（純敘事、不查證）」，可於真機後改為無條件路徑。

### 44.10 v44 待辦與路線
- **U1（延續最大待辦）**：選秀順位交易（需動選秀核心）——待 Mars 拍板納入 v44.x 或 v45。
- 球迷三維度（期待/耐心/認同）——憲法附錄B 原列 v44，待 Mars 拍板是否與順位交易一批或延 v45。
- v45：史冊回顧畫面＋資產包掛載（S.assets）。
- R1（v41 遺留）：純GM 教練 AI 手感、需求單/遞補/體諒頻率仍待 Mars 真機把關。

---

## §45 v45：選秀順位交易（U1）＋球迷三維度＋教練需求求購市場

> 本版一次落地北極星附錄B v44 順延的兩大待辦（U1 選秀順位交易、球迷三維度），並依 Mars 真機回饋新增「依教練需求的求購市場（#5）」與交易畫面選秀權可見化（#6）。回歸 871→**898**（+27）全綠、GM兼教練＋純GM 兩套 15 年 smoke（含 v45 演練）全綠、**整合單檔抽出主 script 亦通過同套三道測試**。升級鏈 …→ensureV44→**ensureV45**。

### 45.1 病灶與需求（Mars 拍板）
- **U1（憲法附錄B v44 順延）**：交易只能換「選手＋現金」，不能交易未來選秀順位——GM 缺少「賣未來、換現在」或「囤未來」的長線籌碼（時間複利支柱）。Mars 拍板 v45 做，且要**看得見、能實際交易**。
- **球迷三維度（憲法 6.7・v44 順延）**：球迷只有 v41 預埋的單一 `S.fanPatience`（唯一出口＝老闆耐心乘數）。憲法要求拆為期待/耐心/認同三維、各接自己的出口。Mars 拍板「直接開發最終版、不要替用品」——本版即為**永久設計**：認同**只看球迷認得的傳統數據＋在地出身**（憲法「第三雙眼睛不看數據」），不看球探天花板/進階數據；L3 進階數據引擎為**另一條獨立特徵**，到位後只是「加深套利張力」，不會取代認同的傳統數據基礎（故非替用品、不會被丟棄重做）。
- **#5 求購市場（Mars 新需求）**：教練需求單原本只有接受/駁回/協商/體諒四路徑，GM 無法「主動去市場找人」證明努力。新增依教練需求張貼求購→AI 球團回覆「願給的球員＋要價條件（選手／選手加錢／純錢／多換一）」；張貼即對教練展現努力（過期減罰 -8→-3）。
- **#6 交易/選秀看不見細節（Mars 回報）**：真正使用的交易畫面是 `renderTradeBuilderV43`（v43 完整資料卡＋現金），但選秀權完全沒有 UI＝玩家看不到、無從交易。本版把選秀權接進此畫面。

### 45.2 U1 選秀順位交易（動選秀核心）
- **歸屬模型**：`S.pickOwnership[年][輪][原隊id] = 擁有者id`；缺項＝原隊自持（舊檔零破壞、天然相容）。
- **核心函式（01-data-engine）**：`ensureV45PickState`／`pickBaseDraftYear`（下一屆選秀年度）／`tradeablePickYears`（本屆＋未來2年＝3年窗）／`pickOwnerOf`／`setPickOwner`（歸還原隊時清記錄不膨脹）／`teamOwnedPicks`（含自持與收購）／`pickTokenKey`／`pickLabel`／`pickTradeValue`（輪次基礎 R1=8.0…R6=0.5 × 原隊弱度 clamp(1.4−勝率×0.8) × 未來折現 1/0.85/0.72）／`picksTotalValue`。
- **選秀核心改動**：`buildDraftOrder` 產生原隊順位序後，逐 slot 導向 `pickOwnerOf(draftYear,輪,原隊)`——**原隊決定順位、擁有者實際選人**（一隊一輪可有 0~多個選位）。`finishDraft` 用畢清掉該年度歸屬記錄。
- **交易攜帶**：`v45EvaluateTradeWithPicks`（沿用 v43 現金評估邏輯，選秀權為純資產、不受對方現金上限限制）／`v45ExecuteTradeWithPicks`（先做 v43 現金交易，再 setPickOwner 轉移＋史冊）。
- **UI**：`renderTradeBuilderV43` 新增「🎫 選秀權」區塊（雙方可交易 token 勾選＋估值），併入計分板總值；`submitTradeV43` 改走 v45 帶選秀權評估/執行。

### 45.3 球迷三維度（期待／耐心／認同）
- **三維（03-simulation）**：`S.fanExpect`（期待，預設50）／`S.fanPatience`（耐心，v41 既有，預設60）／`S.fanIdentify`（認同，預設55）。`ensureV45Fans` 惰性補欄位。
- **認同來源**：`playerFanAppeal(p)`＝傳統數據（野手打擊率/HR/RBI/SB；投手 W/SV/SO）＋在地出身（!foreign ×1.15）；`teamFanAppeal(team)`＝一軍前12名認同貢獻→0~100。**不觸及球探天花板/進階數據**。
- **四個認同出口（NEW）**：`fanIdentifyPopBond`（季末人氣成長綁定・票房）／`fanIdentifySponsorMult`（贊助收入乘數 0.9~1.12）／`fanIdentifyFaMult`（本土FA期望薪資折讓 0.94~1.06）／`fanIdentifyHomeEdge`（主場期望得分 −0.03~+0.05）。分別接在 02-finance 季末贊助/人氣、02-finance FA desiredSalary、03-simulation simulateGame 主場計分。
- **期待出口**：`fanExpectPressure()`（本季戰績相對期待落差→0.7~1.8 放大耐心/認同下滑）。**耐心出口**：沿用 `fanPatienceOwnerMult()`（v41）。
- **掛勾**：`tickV45Fans`（每日，掛 tickV41Daily 尾）／`v45FansSeasonEnd`（季末，掛 v41OnSeasonEnd 尾：期待向戰績目標靠攏＝王朝詛咒、認同向 teamFanAppeal 靠攏、耐心疊期待壓力）／擴充 `v41OnTradeExecuted`（送走高 fanAppeal 門面球員→認同重挫，在地子弟兵另推播球迷失望）／`v45FanMail`（三維度跨門檻→球迷來信進郵件中樞＋跑馬燈，不做新畫面）。
- **UI**：財務總覽新增三維度唯讀讀數列（期待/耐心/認同＋出口說明），非新畫面。

### 45.4 #5 教練需求求購市場
- **狀態**：`S.v45Wants[]`（每筆含 AI 回覆清單）＋`S.v45WantSeq`；`ensureV45WantState`。
- **函式（03-simulation）**：`v45PlayerMeetsDemand`（standalone 需求比對）／`v45GenerateWantResponse`（AI 用自家球員換 GM 資產的要價，四型：cash/player/player_cash/multi，沿用 personaTradeValue＋v42rng）／`v45PostWant`（掃全聯盟合適且非招牌<88 球員，生成回覆；標記 `d.wantPosted`）／`v45AcceptWantResponse`（GM 交出要價換回 AI 球員，落一軍填補需求，觸發 checkDemandFulfilled）／`v45CancelWant`。
- **教練努力回饋**：`checkDemandFulfilled` 過期分支——`d.wantPosted` 為真時信任罰 **-8→-3**、語錄改「知道你去市場找過了」。呼應 Mars「證明我有去找人」。
- **UI**：需求卡新增「🔍 求購市場找人」按鈕（`data-act="want"`，攔在 resolveDemand 前導向 wantMarket）；`renderWantMarket` 畫面列出求購與 AI 報價（願給/要價/接受/撤下），註冊於 renderScreen。

### 45.5 驗收
- 回歸 871→**898**（+27：U1 選秀權/估值/交易攜帶、球迷三維度/四出口/交易掛勾、#5 求購/過期減罰、升級鏈）連跑全綠。
- **GM兼教練 15年 smoke**（+v45 三維度值域/選秀權交易/求購畫面）＋**純GM 15年 smoke**（+v45 選秀權交易轉手還原/buildDraftOrder 歸屬導向/求購生命週期）全綠。
- 整合單檔以策略C（六模組雙空行串接、位元一致）組裝，抽出主 script 跑同套回歸+雙 smoke 全綠。sw cache v44→**v45**。

### 45.6 v45 已知簡化（誠實記錄）
- **Y1** 選秀權交易窗＝本屆＋未來2年、6輪、**無保護條款/條件式選秀權**（避免膨脹；真機後可加）。
- **Y2** `pickTradeValue` 以原隊「當前戰績」近似未來順位（未知未來戰績）；未做順位保護與樂透加權。
- **Y3** AI 尚未在既有掛牌（v43 offers）與 AI 互易（v32）主動夾帶選秀權——本版選秀權交易由**玩家在交易畫面主動發起**；AI 主動開選秀權條件列為 backlog（Mars 第3點「目前還沒遇到過」→低頻率納入待後續）。
- **Y3.1** #5 求購由 AI「賣方」開要價；AI 主動性以 trueOverall<88（招牌不賣）門檻近似，未含更細的建隊需求判讀。
- **Y4** 認同以傳統數據＋在地出身計，**未含兌現公開承諾、球風掛鉤**（憲法列項，排後續）；L3 進階數據引擎為獨立特徵，到位後加深套利張力（非替用品，本版認同基礎為永久設計）。
- **Y5** 期待/耐心/認同的參數（期待門檻線、認同映射係數、四出口乘數幅度、過期減罰 -3）採建議值，待 Mars 真機把關手感。
- 承 v44 及以前各項簡化不變。


---

# §46　全能力完整資訊卡 ＋ 純GM傷兵提醒修正 ＋ 教練需求重建漸進寬限（v46）

> 交付狀態：**v46 已完成交付**。回歸 898→914 全綠（新增 16 項 v46 測試）、純GM 15年 smoke ＋ 15年 smoke 全綠、整合單檔本身亦過三道。承接 Mars v46 兩大需求：①「完整資訊到處都要看得見（尤其變化球）」②「教練需求頻率過高、第一年撐不住」。

## 46.1　根因盤點（Mars 反覆提出、先前未解）
- **沒有單一權威完整卡**：談約報告、選秀卡、交易卡、掛牌卡、求購市場各自手刻一份「能力子集」，導致「這個畫面有、那個畫面沒有」，且每加一個新功能就自帶更小的子集（＝「越改越少」）。
- **變化球從未在任何評估介面出現**：選秀卡甚至印「球路 5 種」卻一顆變化球的球威/控球都不顯示。
- **純GM傷兵提醒邏輯錯誤**：純GM 不能手排陣容，系統卻跳「請盡快到球員名單調整先發」。
- **教練需求早年壓力過載**：爛陣容觸發大量硬缺口需求，第一年沒錢沒籌碼卻被逼交易。

## 46.2　① 全能力完整資訊卡（單一權威元件）
- 新建 `v46FullPlayerCard(p, opts)`：一張卡涵蓋 Mars 指定全部欄位——年齡/守位/薪資合約 ＋ 球速、控球、體力、抗壓、耐久、疲勞、**變化球逐球種（球威／控球）**、接觸、長打、選球、對左/右投、跑壘、盜壘、觸擊、守備、臂力、**配球/接捕/阻殺（捕手）**、潛力（天花板）、特質。
- 兩種模式：`opts.scoutView`／`opts.scouted` ⇒ 球探評估（全欄位霧化）；否則真實值。對齊北極星「資訊不對稱」：對手仍是評估值，**但欄位不再缺**。
- **一處升級、處處生效**：`v43PlayerFullCardHtml` 改為委派到 `v46FullPlayerCard`，交易畫面（雙方）、掛牌報價卡、傷兵遞補提案、AI 主動提案全部同步升級；**求購市場**改為展開「對方願給（球探評估完整卡）＋你要付出（自家真實完整卡）」；**選秀卡＋新秀/自由/國際球員談約報告**（`scoutedAttrRows`／`negotiationScoutBlock`）補齊變化球與所有缺漏欄位。
- **確定性霧化 `v46Fog`（重要）**：評估值噪聲改由「球員ID＋屬性鍵＋季＋精準度」雜湊產生，**完全不觸碰共享 `Math.random`**（遵守亂數紀律鐵則；渲染時大量呼叫也不污染模擬確定性序列，且評估值天生穩定不跳動）。`v46ScoutViewFor` 每季＋精準度快取；優先沿用既有 `p.scouted` 以與選秀畫面數字一致。

## 46.3　② 純GM傷兵提醒修正
- 主控台「先發陣容提醒」以 `canManualLineup()` 分流：可手排（GM兼教練／已接管）→ 維持原本「去名單分頁調整」提醒；純GM不可手排 → 改為「先發/輪值由總教練全權排定、傷兵自動由教練遞補，你不需要也無法手動調整；一軍出現本職缺口教練會另以補強需求/遞補提案請示」。

## 46.4　③ 教練需求「重建漸進寬限期」（Mars 拍板：前5年、且不設懸崖）
- Mars 補充關鍵：「教練約長度不定，硬切 N 年會有懸崖」→ 採**漸進式、綁『加盟年資』而非教練約**。
- `v46FranchiseYear()`＝加盟年資（1 起算，錨點 `S.franchiseStartYear`，舊檔一律視為第1年起家）。
- `v46DemandPressure()`＝壓力係數（第1年 0.20→第6年起 1.0，逐年遞增、**無懸崖**）。
- 出口：`v46DemandCap()` 同時掛單上限（年資淺=1、中段=2、第6年起=3，取代原本寫死 `<3`）；`genDemand` 期限在寬限期加長（第1年約 1.7–1.8 倍）、措辭轉「重建方向」並標記 `d.rebuild`；`handleHardGapsV41` 硬缺口開單受上限節流、寬限期沿用加長期限；`checkDemandFulfilled` 過期懲罰依壓力減輕（第1年約 -4／已張貼求購約 -2）。

## 46.5　④ 球探盤點內部人選 ＋ 選秀方向（Mars：否則還是無解）
- `v46ScanInternalCandidates(team, d)`：依需求 `d.need`（守位/角色＋能力門檻）掃自家一二軍，回傳最符合的人選（標示「已達門檻／差 N 點」、是否可拔擢）。**遵守 `demandMatches` 規則：只有需求快照外的新血才算達成 ⇒ 優先回傳可拔擢的二軍新血**。
- `v46DraftDirectionNote(d)`：球探建議本屆選秀鎖定的類型與條件。
- 需求卡加「🔎 球探盤點」面板：列候選、可一鍵「拔擢上一軍」（`demand-btn` 新增 `promote` 動作 → `promotePlayer` → 自動 `checkDemandFulfilled`）、附選秀方向。

## 46.6　已知簡化（§46 尾・待 Mars 真機把關）
- **Z1** 球探盤點目前掃「自家陣中＋指出選秀方向」；**「球探主動在自由球員/國際市場搜尋符合需求條件的人選」尚未做**（Mars 提出、範圍較廣）→ 列為 v46 收尾／v47 首要（見待辦 R-scout）。
- **Z2** 重建壓力係數（0.20/0.40/0.55/0.72/0.88/1.0）、寬限期限倍率、過期懲罰縮放採建議值，待真機把關。
- **Z3** 完整卡「疲勞」在球探模式顯示「—」（臨場資訊、成交後於名單頁可見）；如 Mars 希望對手也給估計值可後續調整。
- **Z4** `v46FullPlayerCard` 潛力以單一天花板等級呈現；逐屬性天花板（新秀已有 `scoutedPots`）尚未在完整卡逐項展開。
- 承 v45 及以前各項簡化不變。

---


## §47 可導入美術系統 ＋ Z1 球探市場找人（已交付・回歸948全綠）

**背景**：Mars 拍板將美術／美編／UI/UX／背景／按鍵／圖示全面改為「可導入模式」，交由 Codex 獨立產出圖檔資產包後導入；同時落地北極星附錄B §13.x 資產包掛載，並完成 v46 列為 v47 首要的 Z1。

### A. 可導入美術層
- 新增前置模組 `00-theme.js`（排六模組之前）：語意取圖口 `icon('名稱')`、**89 名**語意圖示登錄表（預設 glyph＝現用 emoji 後備）、資料欄位解析器 `iconVal()`、主視覺 `themeHero()`、`applyThemePack(pack)` 整包導入器、**14 槽位**、`loadThemePackFromJSON`／`exportThemePack`／`themeManifest`。承接並橋接 v41⑦ `ASSET_SLOTS/assetSlot` 與 `S.assets`。
- **圖示全語意化**：以檔案級狀態掃描器（支援跨行樣板字串／`${}` 巢狀／區塊註解／正則字面）一次轉換 **218 處**；設施／行銷 config 的 `icon` 欄位改存語意名、渲染時經 `iconVal()` 解析（載入期資料也能反映導入包）。剩餘未轉者僅註解與 v41⑦ 登錄表自身的 fallback（各有其機制）。
- **14 槽位全部接線**：全域背景／卡片材質／主視覺hero／六分頁背景（`body[data-screen]`）／按鍵四型／分頁圖示條。未導入資產包時全為 `none`，外觀不變。
- **等價性驗證**：未導入資產包時，21 個畫面渲染輸出與改造前**逐位元完全相同**。
- 交付《v47 美術製作規格書》＋範例包 `theme_pack_example.json`。

### B. Z1 球探市場主動找人
- 需求單除了 v46④ 的「陣中盤點」，新增「派球探到**國內自由市場／國際市場**查訪符合條件者」。
- **成本＝時間×名額**（Mars 拍板，不花錢）：占用該區域球探（domestic／international）約 14 天，期間該球探無法接受其他委託；到期回報並釋放名額，同時推播球探新聞。跨年亦視為到期（避免季末派遣卡住）；休賽季無日推進時由 UI 端延遲結算。
- **資訊不對稱**：候選以**球探估值**（`v46Fog` 確定性霧化，不污染共享 `Math.random`）比對需求門檻，球探準度低時可能誤報／漏報——球探能力因此有實質價值。
- **動作**：回報清單＋一鍵導到簽約／報價流程（`startNegotiation`），延續 v46 拔擢的一鍵手感。
- 升級鏈 `ensureV47` 串在 `ensureV46` 之後；狀態容器 `S.v47.missions`。

### C. 修正
- **Stage-1 缺陷修正**：Stage-1 的逐行轉換器在「巢狀於樣板字串內的引號字串」誤產生字面 `"${icon('check')}"`（畫面會顯示原始字串），共 29 處。Stage-2 改用檔案級狀態掃描器**從乾淨原始碼重做**，已全數消除並以逐位元等價性驗證把關。

### 已知簡化
- **Z8** Z1 出勤天數固定 14 天、回報上限 6 名，未依球探能力/區域差異調整——待真機手感把關。
- **Z9** Z1 目前只掃自由市場與國際市場兩池（不含他隊在籍球員；他隊球員走 v45 #5 求購市場／交易）。
- 承 v46 及以前各項簡化不變。
